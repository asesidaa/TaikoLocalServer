# Shop, Reward, and Catalog ID Evidence

## Scope

Endpoints: `getitemshopinfo.php`, `itempurchase.php`, `rewardexecution.php`, `rewardcardcheck.php`, and related BAID/userdata selected IDs.

## Evidence Table

| Field | IDA evidence | Constraint | 0 valid? | Catalog namespace | Server behavior at audit baseline/current tree | Recommendation |
|---|---|---|---|---|---|---|
| shop `item_no` | IDA-proven field strings `0xF00D62` and `0xF02F0F`; item domain source-inferred. | Must match an advertised/catalog-backed Green shop entry. | Not valid for purchase unless catalog contains it. | Green item shop key. | Current tree validates `green.ItemShop.TryGetValue(request.ItemNo, out item)` in `Application/Handlers/ItemPurchaseCommand.Green.cs:16`; item shop loader is empty at `Infrastructure/GameDataCatalog/Green/GreenItemShopLoader.cs:10`. | Keep handler validation; implement `GreenItemShopLoader` before advertising item shop rows. |
| shop `item_type` | IDA-proven field strings `0xF00D73` and `0xF02F20`. | Must equal catalog item type for `item_no`; optional presence should be preserved. | Not found. | Shop item type namespace. | Current tree compares `request.ItemType != item.ItemType` in `ItemPurchaseCommand.Green.cs:17`, but `ItemPurchaseController.cs:13` passes the protobuf value without checking `ShouldSerializeItemType()`. | Keep current catalog guard; preserve optional presence in `ItemPurchaseController.cs:13` or make `ItemPurchaseCommand` carry presence explicitly. |
| shop `item_id` | IDA-proven field strings `0xF00D86` and `0xF02F33`. | Must equal catalog item ID for `item_no`; optional presence should be preserved. | Not found. | Type-specific item ID. | Current tree compares `request.ItemId != item.ItemId` in `ItemPurchaseCommand.Green.cs:18`, but controller value access can collapse omitted optional fields. | Preserve optional presence in `ItemPurchaseController.cs:13`; keep catalog match at `ItemPurchaseCommand.Green.cs:18`. |
| shop `item_price` | IDA-proven field strings `0xF00D97` and `0xF02F44`. | Must equal server catalog price; price must be nonzero and affordable; optional presence should be preserved. | `0` not valid for purchase. | Shop economy price. | Current tree compares `request.ItemPrice != item.Price`, rejects `item.Price == 0`, and checks available medals in `ItemPurchaseCommand.Green.cs:19-21`; controller value access can collapse omitted optional fields. | Preserve optional presence in `ItemPurchaseController.cs:13`; current economic guard in `ItemPurchaseCommand.Green.cs:19-21` is otherwise correct. |
| reward `release_song_no` | IDA-proven request/response strings `0xF01A5D` and `0xF02BD0`; behavior not implemented. | Source-inferred: valid Green catalog song ID and `<1024`. | `0` not valid song. | Green song catalog. | Current `RewardExecutionCommand.Green.cs` ignores `release_song_no`; playresult stage song validation exists in `UpdatePlayResultCommand.Green.cs:68`. | Add reject-or-persist logic for release songs in `RewardExecutionCommand.Green.cs`; validate against `IGreenCatalog.GreenMusicInfos`. |
| reward tone IDs | IDA-proven strings `get_tone_no` at `0xF01A74` and `0xF02BE7`. | Source-inferred: `<128` and catalog/unlock eligible. | `0` is current default/unlocked. | Tone flag bitset. | Current tree rejects IDs not already unlocked in `RewardExecutionCommand.Green.cs:12` via `AllAlreadyUnlocked`; this blocks forged new unlocks but also means rewards cannot grant new tone items. | Replace "already unlocked" placeholder with catalog/eligibility validation when reward catalogs exist. |
| reward costume IDs | IDA-proven strings `get_costume_no_1..5` at `0xF01A8B..0xF01AF3` and `0xF02BFE..0xF02C66`. | Source-inferred: `<256` and catalog/unlock eligible per slot. | `0` is current default/unlocked. | Costume slot bitsets. | Current tree rejects not-already-unlocked IDs in `RewardExecutionCommand.Green.cs:13-17`. | Add slot-aware catalog/eligibility validator in `RewardExecutionCommand.Green.cs`. |
| reward title IDs | IDA-proven strings `get_title_no` at `0xF01B0D` and `0xF02C80`. | Source-inferred: `<1024` and catalog/unlock eligible. | `0` source default/no title. | Title flag bitset. | Current tree rejects not-already-unlocked IDs in `RewardExecutionCommand.Green.cs:18`. | Add title catalog/eligibility validator in `RewardExecutionCommand.Green.cs`. |
| selected tone/titleplate IDs | IDA-proven related strings `default_tone_setting` `0xF011DE` and `titleplate_id` `0xF00F3B`; exact selected-domain semantics not found. | Source-inferred: selected value must be default or unlocked/catalog-valid. | `0` valid source default. | Tone and titleplate namespaces. | Current tree echoes `TitlePlateId` at `BaidQuery.Green.cs:46` and `DefaultToneSetting` at `BaidQuery.Green.cs:66`. | Add output normalization in `BaidQuery.Green.cs:46` and `BaidQuery.Green.cs:66`. |

## Artifact Files

- `.tools/green-field-evidence/shop_reward_strings.json`
- `.tools/green-field-evidence/shop_reward_xrefs.json`
