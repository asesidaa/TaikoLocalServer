# UserData and BAID Field Evidence

## Scope

Endpoints: `userdata.php`, `baidcheck.php`.

Fields: `option_flg`, `disp_dan_type`, `got_dan_max`, `got_dan_flg`, `got_danextra_flg`, costume/tone/title flags, default selected IDs, titleplate, Don colors, difficulty played fields.

## Evidence Table

| Endpoint | Field | IDA evidence | Constraint | 0 valid? | Omit when unknown? | Server behavior at audit baseline/current tree | Recommendation |
|---|---|---|---|---|---|---|---|
| `userdata.php` | `option_flg` | IDA-proven field strings at `0xF015E7`, `0xF02082`, `0xF02E88`; length/content not found. | Not found by IDA. Treat length/content as unknown. | Not found. | Yes; empty `[]` is not client-proven. | Current tree assigns `saveData.OptionFlg` in `Application/Handlers/UserDataQuery.Green.cs:35`; mapper serializes at `Adapters.GameProtocol.Green/Mappers/UserDataMappers.cs:17`; default is `[]`. | Make `CommonUserDataResponse.Green.OptionFlg` nullable/presence-aware and omit empty/unknown in `UserDataMappers.cs:17`. |
| `userdata.php` | `difficulty_played_course` | IDA-proven field strings at `0xF0182E` and `0xF01DB7`; range not found. | Source-inferred likely course `0..4` when the value was present in playresult. | `0` can mean Easy, but persisted default `0` is ambiguous. | Yes unless presence is known. | Current tree preserves playresult presence in `PlayResultMappers.cs:53` and stores only if present in `UpdatePlayResultCommand.Green.cs:38`; userdata still always serializes at `UserDataMappers.cs:38`. | Add output presence storage/guard for `UserSaveDataGreen.DifficultyPlayedCourse` and omit unknown default in `UserDataMappers.cs:38`. |
| `userdata.php` | `difficulty_played_star` | IDA-proven field strings at `0xF01850` and `0xF01DD9`; range not found. | Not found by IDA; require a bounded source-defined star domain before accepting. | Not found. | Yes unless presence is known. | Current tree preserves playresult presence in `PlayResultMappers.cs:54` and stores only if present in `UpdatePlayResultCommand.Green.cs:43`; userdata still always serializes at `UserDataMappers.cs:39`. | Add range validation at `UpdatePlayResultCommand.Green.cs:43` and omit unknown default in `UserDataMappers.cs:39`. |
| `baidcheck.php` | `disp_dan_type` | IDA-proven field string `0xF01156`; enum domain not found. | Source-inferred: `0` is safe default/no display; nonzero values need domain evidence. | Yes, as current default. | Normalize unknown nonzero to `0` or omit. | Current tree echoes save value at `Application/Handlers/BaidQuery.Green.cs:62`; mapper serializes `GetValueOrDefault()` at `BaidResponseMapper.cs:40`. | Guard or normalize in `BaidQuery.Green.cs:62` before mapping. |
| `baidcheck.php` | `got_dan_max` | IDA-proven field string `0xF0116D`; max domain not found. | Source-inferred from dan flag storage: should not exceed represented dan slots. | Yes, none. | Clamp/omit unknown out-of-range values. | Current tree echoes save value at `BaidQuery.Green.cs:64`; mapper serializes at `BaidResponseMapper.cs:41`. | Add max guard in `BaidQuery.Green.cs:64` after the exact dan max domain is chosen. |
| `baidcheck.php` | `got_dan_flg` | IDA-proven field string `0xF01182`; length source-inferred. | Source-inferred exact length: `GreenProtocolBytes.DanFlagBytes == 18`. | All-zero valid none. | Emit only sanitized fixed-length bytes. | Current tree pads/truncates at `BaidQuery.Green.cs:63` and fallback mapper at `BaidResponseMapper.cs:42`. | Keep length normalization; add semantic two-bit-state sanitizer when dan persistence is implemented. |
| `baidcheck.php` | `got_danextra_flg` | IDA-proven field string `0xF01199`; length source-inferred. | Source-inferred exact length: `GreenProtocolBytes.DanExtraFlagBytes == 36`. | All-zero valid none. | Emit only sanitized fixed-length bytes. | Current tree pads/truncates at `BaidQuery.Green.cs:65` and fallback mapper at `BaidResponseMapper.cs:43`. | Keep length normalization; add semantic sanitizer when extra-dan persistence is implemented. |
| `baidcheck.php` | costume, tone, and title selected IDs | IDA-proven related strings: `costume_flg_1..5` `0xF00FFF..0xF0105B`, `default_tone_setting` `0xF011DE`, `titleplate_id` `0xF00F3B`; selected costume slot names not found. | Source-inferred bit capacities: tone `<128`, costume `<256`, title `<1024`; catalog membership/unlocked state required. | `0` is current default/unlocked for tone and costume; titleplate `0` is source default. | Normalize unknown nonzero to safe defaults. | Current tree validates current costume is unlocked in `UpdatePlayResultCommand.Green.cs:94`; selected/default values still echo through `BaidQuery.Green.cs:46` and `BaidQuery.Green.cs:66`. | Add output guards for titleplate/default tone in `BaidQuery.Green.cs:46` and `BaidQuery.Green.cs:66`; keep costume unlock guard and add catalog membership when catalog data exists. |
| `baidcheck.php` | Don colors | IDA-proven strings: `color_face` `0xF00F52`, `color_body` `0xF00F66`, `color_limb` `0xF00F7A`; palette domain not found. | Not found by IDA. Source defaults are face/body/limb `0/1/3`. | Face `0` only source-proven; body/limb `0` not source default. | Normalize unknown colors to source defaults. | Current tree echoes save values at `BaidQuery.Green.cs:47-49`; defaults live at `UserSaveDataGreenExtensions.cs:26-28`. | Centralize palette validator/defaults and call it in `BaidQuery.Green.cs:47-49`. |

## Artifact Files

- `.tools/green-field-evidence/userdata_baid_strings.json`
- `.tools/green-field-evidence/userdata_baid_xrefs.json`
