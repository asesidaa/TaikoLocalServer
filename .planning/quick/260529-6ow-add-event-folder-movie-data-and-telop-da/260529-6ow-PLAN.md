---
quick_id: 260529-6ow
status: complete
created: 2026-05-28
---

# Quick Task 260529-6ow: Add event_folder, movie_data and telop data support to Blue

## Goal

Bring Blue optional event-folder, movie, and telop behavior up to the Green-equivalent runtime surface while keeping Blue-owned paths, handlers, mappers, data file names, and tests.

## Must Haves

- Blue `getfolder.php` returns requested Blue event-folder rows through `GetFolderQuery`.
- Blue `gettelop.php` returns requested Blue telop rows through `GetTelopQuery`.
- Startup movie permissions resolve Blue `HddVer` values to `IBlueCatalog.Movies`.
- Blue uses `blue_event_folder_data.json`, `blue_telop_data.json`, and `blue_movie_data.json` runtime files, with Host output copy entries matching those names.
- The provided original Blue `featureboard.bin` cache is represented as committed Blue event-folder JSON.
- Tests cover the Blue protocol behavior and loader/runtime data files.

## Tasks

1. Add failing Blue tests for folder lookup, telop lookup, startup movie era resolution, and default Blue optional data files.
2. Implement Blue-owned query partials, mappers, and controller wiring for folders and telops; extend startup movie era resolution to Blue.
3. Add or correct Blue runtime JSON files and Host project copy entries.
4. Run focused tests and a Host build, then write summary and update `.planning/STATE.md`.
