# Quick Task 260628-3ef: Fix MOMOIRO startup movie support before Phase 43 WebUI work

## Scope

Enable MOMOIRO startup movie permissions through the same AC15 sidecar and raw `data/movie` discovery path used by adjacent eras.

## Plan

1. Add failing tests for MOMOIRO movie catalog loading and `GetStartupMovieDataQuery` HDD version `4xx` dispatch.
2. Add MOMOIRO catalog `Movies` support backed by `momoiro_movie_data.json` and `data/movie`.
3. Wire `hdd_ver / 100 == 4` to `GameEra.Momoiro` in startup movie resolution.
4. Add the committed MOMOIRO movie sidecar and Host copy rule.
5. Run focused MOMOIRO/startup movie tests and a Host build.

## Guardrails

- Do not touch `Host/wwwroot/data/momoiro/data` except read-only test fixture input copying into test output.
- Do not broaden unsupported MOMOIRO routes or WebUI/AdminApi scope in this task.
