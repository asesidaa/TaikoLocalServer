---
status: in-progress
quick_id: 260623-2ff
created: 2026-06-23
task: support-murasaki-final-v06r01
---

# Quick Task 260623-2ff: Murasaki Final `/v06r01` Support

## Goal

Add Murasaki final support under `/v06r01/chassis/*` as a narrow delta on top of the existing v1.5 Murasaki implementation. Preserve current `/v06r00/chassis/*` Murasaki compatibility behavior, keep Murasaki state era-owned, and implement only final-version behavior proven by the dumped final proto, final binary evidence, and changelog context.

## Source Facts

- Existing Murasaki support is already implemented through Phase 34 automated verification; this quick task is not greenfield era support.
- Current Murasaki game routes live under `/v06r00/chassis/*`; the final-version route prefix for this task is `/v06r01/chassis/*`.
- `proto/murasaki-final/vsinterface.proto` matches current `proto/murasaki/vsinterface.proto`; shared startup/version behavior remains under `/v01r00/chassis/*`.
- `proto/murasaki-final/taiko.proto` differs from current `proto/murasaki/taiko.proto` only in `getfolder.php`:
  - `GetfolderRequest.folder_id` changes from a required scalar to repeated folder IDs.
  - `GetfolderResponse` changes from top-level `folder_id` plus `song_no` to repeated nested `EventfolderData` rows.
- The public changelog page supplied by the user says MURASAKI 0.11 expanded favorites from 5 to 10 and added special folders; current Murasaki already has favorite cap 10 and event folder catalog/readback.
- The same changelog says MURASAKI 9.05 made Dani Dojo and Taikojuku unavailable and stopped high-score/self-best-update display on the client. Because the final binary still exposes the route surface and assets remain present, this is not a server-disable boundary; keep final Dani/Taikojuku handled through the existing Murasaki handlers.
- The same changelog says MURASAKI 10.01 details are unknown and likely farewell-song timing only; do not infer server behavior from it.
- `.tools/murasaki-final/EBOOT.ELF.i64` is present and nonzero. It is evidence material only, not runtime code. Confirm route strings with IDA or existing repo route-evidence workflow before widening the implementation beyond the proto/changelog delta.

## Source Coverage

- GOAL: final Murasaki `/v06r01` support is covered by Tasks 1 and 2.
- REQ: final/legacy Murasaki protocol separation is covered by Tasks 1 and 2.
- REQ: final `getfolder.php` batched event-folder readback is covered by Task 2.
- REQ: final Dani/Taikojuku route parity is covered by Task 3.
- CONTEXT: era-separated Murasaki state, common DTO mapping, direct protobuf transport, generated wire ownership, and evidence-gated unsupported surfaces are enforced in every task.

## Tasks

1. Confirm final-version evidence and regenerate the versioned Murasaki wire surface.
   - Files:
     - Modify: `Adapters.GameProtocol.Murasaki/Wire/Game.cs`
     - Create: `Adapters.GameProtocol.Murasaki/LegacyWire/Game.cs`
     - Create: `Adapters.GameProtocol.Murasaki/LegacyWire/LegacyFolderDataMappers.cs`
     - Modify if needed: `Adapters.GameProtocol.Murasaki/GlobalUsings.cs`
     - Read only: `proto/murasaki/taiko.proto`
     - Read only: `proto/murasaki-final/taiko.proto`
     - Read only: `proto/murasaki/vsinterface.proto`
     - Read only: `proto/murasaki-final/vsinterface.proto`
     - Read only: `.tools/murasaki-final/EBOOT.ELF.i64`
   - Action: Reconfirm the proto diff before code changes: `vsinterface.proto` is unchanged, and `taiko.proto` changes only `GetfolderRequest` / `GetfolderResponse`. Confirm the final binary route prefix/suffix inventory using the existing IDA or route-string workflow; if it proves additional final callable suffixes beyond the current implemented Murasaki set, update this quick plan before implementation instead of silently broadening scope. Regenerate final Murasaki game wire DTOs from `proto/murasaki-final/taiko.proto` into `Adapters.GameProtocol.Murasaki/Wire/Game.cs` with namespace `TaikoLocalServer.Adapters.GameProtocol.Murasaki.Wire`. Generate current compatibility DTOs from `proto/murasaki/taiko.proto` into `Adapters.GameProtocol.Murasaki/LegacyWire/Game.cs` with namespace `TaikoLocalServer.Adapters.GameProtocol.Murasaki.LegacyWire`. Do not edit dumped proto inputs. Do not regenerate or manually edit `VsInterface.cs` unless the diff check proves it changed.
   - Verify:
     - `git diff --no-index --stat -- proto/murasaki proto/murasaki-final`
     - `rg -n "FolderIds|AryEventfolderDatas|EventfolderData" Adapters.GameProtocol.Murasaki/Wire/Game.cs`
     - `rg -n "FolderId|SongNoes" Adapters.GameProtocol.Murasaki/LegacyWire/Game.cs`
     - `dotnet build Adapters.GameProtocol.Murasaki/Adapters.GameProtocol.Murasaki.csproj`
   - Done: Final Murasaki wire exposes repeated folder request/response rows; compatibility wire preserves scalar `getfolder` shape; dumped proto inputs remain unchanged; no unsupported route family is added from proto names alone.

2. Add `/v06r01` final routes while preserving `/v06r00` compatibility.
   - Files:
     - Modify: `Adapters.GameProtocol.Murasaki/MurasakiRoutePrefixes.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/BaidController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/BookkeepingController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/CrownsDataController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/DefaultSongController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/FolderCheckController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/GetFolderController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/GetTelopController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/HeartbeatController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/MainichiSongController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/MyDonEntryController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/PlayResultController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/RecommendController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/SelfBestController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/TaikojukuController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/TelopCheckController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/UserDataController.cs`
     - Modify: `Adapters.GameProtocol.Murasaki/Mappers/FolderDataMappers.cs`
     - Modify: `Host/Program.cs`
     - Modify if needed: `Host/README.md`
     - Modify: `Tests/Tests.csproj`
     - Create: `Tests/Murasaki/MurasakiProtocolVersionCompatibilityTests.cs`
   - Action: Replace the single Murasaki route constant with `Final = "/v06r01/chassis"` and `Compatibility = "/v06r00/chassis"`; keep `Game = Compatibility` only as a transitional alias if it reduces churn. Add final route attributes to existing supported Murasaki controllers where final proto shape is unchanged, and keep compatibility route attributes for `/v06r00`. Update `Host/Program.cs` direct-protobuf fallback to accept both Murasaki route prefixes. Split only `GetFolderController` by schema: final `/v06r01/chassis/getfolder.php` uses final `Wire.GetfolderRequest.FolderIds` and returns repeated `AryEventfolderDatas`; compatibility `/v06r00/chassis/getfolder.php` uses `LegacyWire.GetfolderRequest.FolderId` and returns the old scalar `FolderId` plus `SongNoes`. Add `Tests/Tests.csproj` reference to `Adapters.GameProtocol.Murasaki` if the new protocol tests need adapter DTO/controller access. Do not add `bestscore.php`, `songhash.php`, `shoppingresult.php`, `challengecompe.php`, Banacoin, Tokkun, battle, Yellow shop, or global-score persistence.
   - Verify:
     - `dotnet build Adapters.GameProtocol.Murasaki/Adapters.GameProtocol.Murasaki.csproj /p:EmitCompilerGeneratedFiles=true`
     - Inspect emitted Mapperly generated files under `Adapters.GameProtocol.Murasaki/obj/**/generated/**/Riok.Mapperly/` if folder mappers use Mapperly.
     - `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MurasakiProtocolVersionCompatibilityTests|FullyQualifiedName~MurasakiRuntimeHandlerTests"`
   - Done: `/v06r01/chassis/*` reaches the supported Murasaki final endpoints, `/v06r00/chassis/*` remains compatible, final `getfolder.php` can return multiple event folders in one response, compatibility `getfolder.php` keeps the old wire shape, and missing-content-type direct-protobuf posts work for both prefixes.

3. Preserve final Dani/Taikojuku support and regression coverage.
   - Files:
     - Modify: `Adapters.GameProtocol.Murasaki/Controllers/TaikojukuController.cs`
     - Modify if needed: `Adapters.GameProtocol.Murasaki/Controllers/PlayResultController.cs`
     - Modify if needed: `Application/Handlers/UpdatePlayResultCommand.Murasaki.cs`
     - Modify: `Tests/Murasaki/MurasakiRuntimeHandlerTests.cs`
     - Modify: `Tests/Murasaki/MurasakiProtocolVersionCompatibilityTests.cs`
   - Action: Preserve current `/v06r00` Dani/Taikojuku behavior and route `/v06r01` through the same Murasaki handlers because the final binary supports these surfaces. Final `taikojuku.php` should return catalog-backed packs. Final Dan-mode `playresult.php` should create/update Murasaki-owned Dan rows the same way compatibility does. Treat the high-score/self-best display changelog item as client-side unless a final capture proves a server contract change.
   - Verify:
     - `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MurasakiRuntimeHandlerTests|FullyQualifiedName~MurasakiProtocolVersionCompatibilityTests|FullyQualifiedName~MurasakiAdminApiTests"`
     - `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~WhiteProtocolVersionCompatibilityTests|FullyQualifiedName~WhiteRuntimeHandlerTests"`
   - Done: Final Murasaki advertises and handles catalog-backed Taikojuku/Dani under `/v06r01`; legacy `/v06r00` still supports the already-implemented Murasaki Dani path; normal playresult, userdata, self-best, crowns, favorites, recent songs, rewards, event folders, telops, and AdminApi/WebUI surfaces remain Murasaki-owned and do not touch other eras.

## Final Verification

Run these before summarizing the quick task:

1. `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~Murasaki"`
2. `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~WhiteProtocolVersionCompatibilityTests|FullyQualifiedName~WhiteRuntimeHandlerTests"`
3. `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"`

Manual RPCS3/cabinet verification is still the final compatibility gate. Automated tests and Host builds prove server behavior only; they do not prove cabinet acceptance.

## Implementation Notes

- Stage only files required by this quick task; the working tree may gain unrelated user edits while the task is running.
- Keep controllers thin: deserialize final or compatibility wire DTOs, map to common Application DTOs, call Mediator where behavior is stateful, and map back.
- Keep generated final and compatibility wire DTOs schema-separated once `getfolder.php` diverges. Do not force final repeated-folder fields onto `/v06r00`.
- Use behavior tests for protocol serialization, handler state transitions, and no-cross-era/no-cross-version boundaries. Avoid source-text or controller-attribute string tests.
- Existing Phase 33 decisions remain binding: proto-only `bestscore.php`, `songhash.php`, `shoppingresult.php`, challenge arrays, Don Challenge, ChallengeCompe, shop authority, Banacoin, Tokkun, battle, and global-score behavior stay absent unless final binary/cabinet evidence proves a specific callable route and state contract.
- If the final route audit proves additional callable route suffixes, amend this plan first with the new evidence and a bounded implementation task for each suffix.
