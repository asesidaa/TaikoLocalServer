---
status: complete
quick_id: 260603-20d
date: 2026-06-02
commit: 679aacea
---

# Quick Task 260603-20d Summary

Removed the redundant Blue battle release observation table after the latest binary evidence clarified the byte-array state ownership.

## Completed

- Deleted `BlueBattleReleaseState` and removed `BlueBattleReleaseStates` from `ITaikoDbContext` and the Blue DbContext mapping.
- Removed append-style `ReleaseBattleData` row creation; release deltas now update the existing battle-owned masks and keyed token/NPC state only.
- Added the `RemoveBlueBattleReleaseState` EF migration and updated the current model snapshot.
- Updated persistence, handler, shape, and source-guard tests so the current schema no longer creates or depends on `BlueBattleReleaseStates`.
- Updated current GSD state/resolution text to say release deltas update per-user/per-NPC/per-token state directly, with no separate release observation row.

## Verification

- `dotnet test Tests\Tests.csproj --filter "FullyQualifiedName~BlueBattlePersistenceTests|FullyQualifiedName~BlueBattlePersistenceShapeTests|FullyQualifiedName~BlueBattlePlayResultHandlerTests" --no-restore`: passed, 16 tests.
- `dotnet test Tests\Tests.csproj --filter BlueBattle --no-restore`: passed, 41 tests.
- `dotnet build Host\Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build-blue-release-state-cleanup" --no-restore`: passed, 0 warnings/errors.
- `git diff --check`: passed; only line-ending warnings were reported.
- `dotnet test Tests\Tests.csproj --no-restore`: passed, 615 tests.

## Notes

- Source commit: `679aacea`.
- `release_info_flg` remains the clip-seen bitmask and `release_battle_stage_flg` remains the stage-unlocked bitmask per `.tools/blue/battleuserdata-response-xrefs.md`.
- Stage play results still use append-style history because each play-result stage record is a distinct observation.
