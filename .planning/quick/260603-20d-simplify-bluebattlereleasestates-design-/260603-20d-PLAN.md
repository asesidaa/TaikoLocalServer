---
status: ready
quick_id: 260603-20d
date: 2026-06-02
---

# Quick Task 260603-20d: Simplify BlueBattleReleaseStates design

## Scope

- Remove the redundant append-style `BlueBattleReleaseStates` persistence table and model.
- Keep the evidence-backed battle release state in the byte-array/state tables that now have proven semantics: `BlueBattleUserStates`, `BlueBattleNpcStates`, and `BlueBattleTokenStates`.
- Preserve append-style history only for battle stage play results, where the repeated play-result records are still distinct observations.
- Update tests and GSD state so the current design no longer treats release IDs as separate per-play rows.

## Evidence

- `.tools/blue/battleuserdata-response-xrefs.md` now proves `release_info_flg` is the clip-seen bitmask and `release_battle_stage_flg` is the stage-unlocked bitmask.
- `ReleaseBattleData` IDs are client-reported deltas used to update those masks or keyed token/NPC state, so a separate ID observation table duplicates the same user state.

## Verification

- Run focused Blue battle persistence and handler tests.
- Run a broader Blue battle test filter.
- Build `Host/Host.csproj` to a temp output path if focused tests pass.
