---
status: in-progress
quick_id: 260613-ny9
created: 2026-06-13
---

# Quick Task 260613-ny9: Older Red `/v08r00` Compatibility

## Goal

Support the slightly older Red revision under `/v08r00/chassis/*` while sharing the existing Red generation state, handlers, tables, and catalog behavior.

## Tasks

1. Generate older Red wire DTOs from `proto/red-int/taiko.proto`.
   - Files: `Adapters.GameProtocol.Red/Wire/V08R00/Game.cs`
   - Action: Use repo-local `protogen` with the `TaikoLocalServer.Adapters.GameProtocol.Red.Wire.V08R00` namespace.
   - Verify: Generated `BAIDResponse` has no `got_danextra_flg`, and shifted trailing BAID fields match `proto/red-int/taiko.proto`.
   - Done: New generated file compiles without changing the existing Red `/v08r01` wire types.

2. Add `/v08r00` Red route support with shared state.
   - Files: `Adapters.GameProtocol.Red/Controllers/*.cs`, `Adapters.GameProtocol.Red/Mappers/*`
   - Action: Add route aliases for unchanged endpoints. For `baidcheck.php`, add a duplicate method using `Wire.V08R00` request/response types and map from the same `BaidQuery(GameEra.Red, ...)` result.
   - Verify: `/v08r00/chassis/baidcheck.php` serializes the older BAID response shape while other `/v08r00` routes reuse existing Red behavior.
   - Done: No new Red persistence tables, no duplicated handlers, and no change to `/v08r01`.

3. Add focused regression coverage and verify.
   - Files: `Tests/Red/*`
   - Action: Add tests for the older BAID wire shape and `/v08r00` route metadata where useful.
   - Verify: Run focused Red tests and a temp-output Host build.
   - Done: Tests/build pass or any unrelated blocker is documented in the summary.
