# Quick Task 260609-7gk: Yellow Final Proto And Route Update

## Goal

Update Yellow AC15 support after switching local Yellow game data from the initial version to the final version:

- Regenerate Yellow game protocol wire types from the final Yellow proto.
- Move Yellow game routes from `/v09r00/chassis/*` to `/v09r02/chassis/*`.
- Add Yellow handling for final-version fields that are already handled for Blue/Green where the Yellow direct-protobuf schema now exposes them.

## Tasks

1. Treat `proto/` as read-only dumped client evidence. Do not edit files under `proto/`.
2. Regenerate `Adapters.GameProtocol.Yellow/Wire/Game.cs` from the dumped final Yellow proto using repo-local `protogen`.
3. Update Yellow adapter mappings and tests for fields exposed by the generated final Yellow wire types:
   - Include item-shop date/countdown fields in Yellow `getitemshopinfo` responses.
   - Serialize BAID WaiWai tutorial state when present in Yellow save data.
   - Map optional WaiWai stage facts and playresult tutorial flag from final Yellow wire payloads.
   - Keep Yellow userdata limited to proven existing readback fields and preserve no-battle guarantees.
4. Change Yellow game route prefix and fallback protobuf detection from `/v09r00` to `/v09r02`.
5. Verify with focused Yellow route/wire/shop/WaiWai tests and a temp-output Host build.

## Notes

- Existing worktree changes are present before this quick task. Stage only the files required by this change.
- Do not introduce Yellow battle routes, persistence, or Blue battle wire surfaces.
