---
phase: 05-blue-battle-runtime-support
plan: "05-06"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - Application/Catalog/Blue/BlueBattleCatalog.cs
  - Application/Abstractions/IBlueCatalog.cs
  - Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs
  - Infrastructure/GameDataCatalog/Blue/BlueBattleDataLoader.cs
  - Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs
  - Tests/Blue/BlueHandlerFixture.cs
  - Tests/Blue/BlueBattleCatalogLoaderTests.cs
autonomous: true
requirements:
  - BTL-01
  - BTL-06
user_setup: []
must_haves:
  truths:
    - "Raw battle XML can be inventoried without advertising battle or deriving runtime behavior."
    - "D-15 D-16 D-17: XML file presence, row counts, stage 33, boss-life, token, and reward semantics stay blocked unless the row matrix approves exact behavior."
  artifacts:
    - path: "Application/Catalog/Blue/BlueBattleCatalog.cs"
      provides: "Raw battle XML inventory surface"
    - path: "Infrastructure/GameDataCatalog/Blue/BlueBattleDataLoader.cs"
      provides: "Battle XML loader that records bounded metadata only"
    - path: "Tests/Blue/BlueBattleCatalogLoaderTests.cs"
      provides: "No semantic-consumer regression tests"
  key_links:
    - from: "Application/Abstractions/IBlueCatalog.cs"
      to: "Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs"
      via: "BattleCatalog property"
      pattern: "BattleCatalog"
---

<objective>
Add the raw Blue battle catalog boundary.

Purpose: IDA proves local battle XML files are loaded by client battle code, but Phase 5 must not infer runtime semantics from XML alone.
Output: Raw battle catalog, loader, Blue catalog wiring, fixture update, and loader tests.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-blue-battle-runtime-support/05-CONTEXT.md
@.planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
@.planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
@.planning/phases/04-blue-battle-evidence-and-design/04-02-BATTLE-DATA-INVENTORY.md
@Application/Abstractions/IBlueCatalog.cs
@Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs
@Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs
@Infrastructure/GameDataCatalog/Blue/BlueItemShopLoader.cs
@Tests/Blue/BlueHandlerFixture.cs
</context>

<scope>
In scope: raw file inventory, bounded XML parse status, and catalog contract wiring.

Out of scope: first-stage, next-stage, last-stage, stage 33, boss-life, token reward, NPC row-count, or battle advertisement behavior.
</scope>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Battle data foundations | Covered by raw local XML inventory only. |
| REQ | BTL-01 | Covered as input boundary for later battle-owned state, not semantics. |
| REQ | BTL-06 | Covered by Blue-owned catalog files and tests. |
| RESEARCH | IDA proves all five local battle XML files are loaded but field semantics remain gated | Covered exactly: inventory yes, semantics blocked. |
| CONTEXT | D-15, D-16, D-17 | Covered by no semantic consumers and no XML-derived defaults. |
</source_audit>

<tasks>

<task id="05-06-T1" type="auto" tdd="true">
  <name>Task 1: Add raw battle catalog and loader tests</name>
  <files>Application/Catalog/Blue/BlueBattleCatalog.cs, Application/Abstractions/IBlueCatalog.cs, Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs, Infrastructure/GameDataCatalog/Blue/BlueBattleDataLoader.cs, Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs, Tests/Blue/BlueHandlerFixture.cs, Tests/Blue/BlueBattleCatalogLoaderTests.cs</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-blue-battle-runtime-support/05-CONTEXT.md
    - .planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
    - .planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
    - .planning/phases/04-blue-battle-evidence-and-design/04-02-BATTLE-DATA-INVENTORY.md
    - Application/Catalog/Blue/BlueBattleCatalog.cs
    - Application/Abstractions/IBlueCatalog.cs
    - Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs
    - Infrastructure/GameDataCatalog/Blue/BlueBattleDataLoader.cs
    - Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs
    - Infrastructure/GameDataCatalog/Blue/BlueItemShopLoader.cs
    - Host/wwwroot/data/blue/data/config/S10100-1/battle
    - Tests/Blue/BlueHandlerFixture.cs
    - Tests/Blue/BlueBattleCatalogLoaderTests.cs
  </read_first>
  <behavior>
    - Test 1: the loader inventories `battleadjsetting.xml`, `battlenpcinfo.xml`, `battlestageinfo.xml`, `battlesupportinfo.xml`, and `battletokeninfo.xml` when present.
    - Test 2: missing battle XML produces a raw-unavailable catalog state and does not enable battle advertisement.
    - Test 3: no production consumer reads the battle catalog to set `is_battleplay`, battleuserdata rows, stage assignment, tokens, boss life, stage 33, or rewards.
  </behavior>
  <action>
    Add `BlueBattleCatalog` as raw candidate-data inventory. Add `BattleRoot` and file-specific paths to `BlueGameDataPaths`. Implement `BlueBattleDataLoader` to inspect the five known battle XML files, record file presence, parse success/failure, bounded row counts, and stable file identifiers without committing local XML or derived JSON. Wire `BattleCatalog` into `IBlueCatalog`, `BlueEraGameDataCatalog`, and the test fixture. Do not add consumers in protocol controllers, mappers, or handlers that derive runtime values from this catalog.
  </action>
  <acceptance_criteria>
    - `BlueBattleDataLoader` inventories the five known battle XML files as raw candidate data when present.
    - Missing battle XML yields a raw-unavailable catalog state and does not enable battle advertisement.
    - `IBlueCatalog`, `BlueGameDataPaths`, `BlueEraGameDataCatalog`, and Blue test fixtures expose the raw catalog without deriving runtime semantics.
    - Tests and guard command prove no production consumer derives `is_battleplay`, row counts, stage assignment, tokens, boss life, stage `33`, or rewards from `BattleCatalog`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleCatalogLoaderTests</automated>
    <automated>$forbidden = 'IsBattleplay','AssignStageId','LastBossLife','BattleBondsLvCap','ReleaseBattleStageFlg','ReleaseBattleSpecialFlg'; $files = 'Application/Handlers/GetInitialDataQuery.Blue.cs','Adapters.GameProtocol.Blue/Mappers/BattleUserDataMappers.cs','Adapters.GameProtocol.Blue/Controllers/BattleUserDataController.cs','Adapters.GameProtocol.Blue/Mappers/PlayResultMappers.cs','Application/Handlers/UpdatePlayResultCommand.Blue.cs'; foreach ($file in $files) { if (Test-Path -LiteralPath $file) { $source = Get-Content -LiteralPath $file -Raw; foreach ($token in $forbidden) { if ($source -match 'BattleCatalog' -and $source -match $token) { throw "$file derives $token from BattleCatalog" } } } }</automated>
  </verify>
  <done>Blue battle XML has a raw catalog boundary with tests proving it does not drive runtime battle semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local XML -> runtime catalog | Operator-supplied battle XML enters server catalog loading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | XML semantic overreach | mitigate | Loader exposes raw inventory only and tests no runtime consumers derive behavior from XML. |
| T-05-06-02 | Information Disclosure | local XML | mitigate | Catalog records bounded metadata and does not commit local XML or full dumps. |
| T-05-06-SC | Tampering | package installs | accept | No npm, pip, cargo, or NuGet package install is planned. |
</threat_model>

<verification>
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleCatalogLoaderTests`
- PowerShell no-BattleCatalog-semantic-consumer check in task verify.
</verification>

<success_criteria>
- Raw battle XML inventory is available through Blue catalog.
- No runtime behavior is derived from XML alone.
- Local operator data remains local.
</success_criteria>

<output>
Create `.planning/phases/05-blue-battle-runtime-support/05-06-SUMMARY.md` when done.
</output>
