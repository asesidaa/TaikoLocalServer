# Phase 16.1: AC15 Mapperly Mapper Rewrite and Presence Semantics - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md.

**Date:** 2026-06-10
**Phase:** 16.1-ac15-mapperly-mapper-rewrite-and-presence-semantics
**Areas discussed:** Mapperly usage, protobuf optional primitive presence, phase boundary

---

## Major Refactor Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Inline cleanup | Treat the mapper issue as a small follow-up inside existing Yellow work. | |
| Dedicated phase | Insert a standalone phase before Yellow closeout because the issue spans Green, Blue, and Yellow AC15 adapters. | yes |

**User's choice:** Dedicated phase.
**Notes:** The user identified this as a major rewrite, not an inline step.

---

## Mapper Implementation Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Keep hand-written mappers | Continue annotating mapper classes while manually copying fields. | |
| Use real Mapperly generation | Add bodiless partial methods for mechanical projection and reserve manual code for protocol-specific transforms. | yes |

**User's choice:** Use real Mapperly generation.
**Notes:** The user called hand-written AC15 Mapperly classes a major anti-pattern now that Mapperly is in the project.

---

## Optional Primitive Presence

| Option | Description | Selected |
|--------|-------------|----------|
| Use `ShouldSerialize*` in mappers | Continue using generated protobuf helper methods as presence checks. | |
| Regenerate nullable wire properties | Use protogen nullable primitive support and map nullable values directly. | yes |

**User's choice:** Regenerate nullable wire properties.
**Notes:** The user rejected `ShouldSerialize*` mapper calls as protobuf-internal helper leakage.

---

## the agent's Discretion

- The agent may choose the exact order of mapper conversion as long as wire regeneration happens first and the build proves Mapperly generation is real.
- The agent may add reflection and serialization tests to prove nullable optional primitive behavior without source-text assertions.

## Deferred Ideas

None.
