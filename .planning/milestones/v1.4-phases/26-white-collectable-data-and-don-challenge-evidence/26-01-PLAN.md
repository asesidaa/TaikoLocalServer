# Phase 26 Plan 01: White Collectable Provenance Binding

## Tasks

1. Add typed catalog records for White present rows and special BAID rows.
2. Add raw XML loaders for `present.xml` and `spacialbaid.xml`.
3. Expose parsed present/special BAID data through `IWhiteCatalog` and `WhiteEraGameDataCatalog`.
4. Make the White required-data check include the two collectable/provenance XML files.
5. Add focused White loader tests using local data when present.
6. Record Don Challenge as absent/data-only unless current White evidence proves the full runtime contract.

## Verification

- `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~White"`
- `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"`

## Manual Verification

Skipped for this phase by user instruction; Phase 27 stops before final manual RPCS3/WebUI verification.
