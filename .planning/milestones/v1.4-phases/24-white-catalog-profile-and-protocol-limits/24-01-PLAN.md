# Phase 24 Plan 01: White Catalog Profile and Sidecar Binding

## Tasks

1. Add `IWhiteCatalog` and White catalog loader/path/required-file classes.
2. Register White in `CatalogExtensions`, `Infrastructure.DependencyInjection`, and `Ac15CatalogSnapshotFactory`.
3. Add `Ac15EraProfiles.White` with Red-like no-shop features and a White-specific limit factory.
4. Commit default White sidecars for event folders, telops, recommendations, movies, and Taikojuku verup metadata.
5. Update `Host.csproj` to copy White sidecars while continuing to exclude raw operator data.
6. Add focused White catalog/profile tests that exercise real local data when available and sidecar contract parsing.

## Verification

- `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~WhiteCatalogLoaderTests|FullyQualifiedName~Ac15ProfileCapabilitiesTests"`
- `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"`

## Manual Verification

Skipped for this phase by user instruction; Phase 27 will stop before final manual RPCS3/WebUI verification.
