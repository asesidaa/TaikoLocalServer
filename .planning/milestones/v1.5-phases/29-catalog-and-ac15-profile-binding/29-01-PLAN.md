# Phase 29 Plan: Catalog And AC15 Profile Binding

## Implementation

1. Add `IMurasakiCatalog` and `Infrastructure/GameDataCatalog/Murasaki` path/required-file/catalog implementations.
2. Register the catalog in `Infrastructure/DependencyInjection.cs` and expose `IGameDataCatalog.Murasaki()`.
3. Add `Ac15CatalogSnapshotFactory.FromMurasaki` and `Ac15EraProfiles.Murasaki`.
4. Add committed empty/default Murasaki sidecars for event folders, telops, movies, and Taikojuku verup data.
5. Wire Murasaki startup movie Hdd version mapping.

## Verification

- Build solution with generated Mapperly output.
- Runtime tests consume a Murasaki test catalog through `FileGameDataCatalog`.
