# Phase 34 Plan: AdminApi/WebUI and Runtime Closeout

## Implementation

1. Add Murasaki to the existing WebUI era model:
   - Include Murasaki in supported and known eras.
   - Treat Murasaki as AC15 for page rendering and profile display-name routing.
   - Keep `SupportsOlderAc15DonChallenge` limited to Red and White.
2. Add Murasaki branches to existing AdminApi era-routed controllers:
   - `GameDataController` for music and Dani catalog data.
   - `CustomizationCatalogController` for costume/title/neiro catalog slices.
   - `Ac15ProfileSettingsController` for Murasaki profile read/write.
   - `PlayDataController`, `PlayHistoryController`, `SongLeaderboardController`, `DanBestDataController`, and `FavoriteSongsController` for Murasaki-owned persistence.
3. Keep unsupported surfaces absent:
   - Do not add AdminApi/WebUI controls for global score, shopping, Don Challenge, ChallengeCompe, or Phase 33 proto-only request families.
   - Do not edit `proto/`.
4. Update operator-facing adapter/WebUI documentation to include Murasaki parity and unsupported-surface boundaries.

## Verification

- Focused WebUI tests cover Murasaki era normalization, route helpers, catalog request paths, AC15 classification, and Don Challenge exclusion.
- Focused AdminApi tests cover Murasaki profile settings, score/history, favorites, leaderboard, Dani, catalog/customization, and no-cross-era persistence behavior.
- Full solution build runs with `EmitCompilerGeneratedFiles=true`; if source-generator output relevant to changed mappings exists, inspect it.
- Full test suite passes.
- Phase closeout records that cabinet/RPCS3 and WebUI acceptance are user-observed gates and are not claimed from automated tests alone.
