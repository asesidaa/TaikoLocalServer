# Phase 33 Plan: Evidence-Gated Special Capabilities

## Implementation

1. Capture the Phase 33 evidence record for Murasaki special surfaces:
   - `bestscore.php`, `songhash.php`, and `shoppingresult.php` remain absent because current route evidence does not prove client route calls.
   - Existing AC15 byte families use the established shared limits already assigned through `Ac15EraProfiles.Murasaki`.
   - Challenge arrays remain upload facts only and do not enable Red or White Don Challenge behavior.
2. Add focused regression tests for the evidence-backed guardrails:
   - Murasaki protocol limits match the existing AC15 byte sizes used by current Murasaki readback.
   - Murasaki playresults containing challenge ids still do not write Red/White Don Challenge state or unsupported Murasaki challenge state.
3. Avoid runtime feature expansion:
   - Do not add `bestscore.php`, `songhash.php`, `shoppingresult.php`, ChallengeCompe, Don Challenge, Yellow shop, Banacoin, or global-score persistence.

## Verification

- Focused Murasaki tests cover byte limits and challenge no-write boundaries.
- Source/evidence inspection verifies proto-only Murasaki routes stay absent.
- Full solution build verifies route/controller compilation still excludes proto-only Murasaki routes.
- Full test suite confirms existing Murasaki support and other eras are unchanged.
