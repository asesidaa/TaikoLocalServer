# Phase 28 Plan: Murasaki Evidence And Era Foundation

## Scope

Add first-class Murasaki identity and adapter foundation from local Murasaki evidence:
- `GameEra.Murasaki`
- adapter-local generated wire DTOs from `proto/murasaki`
- `/v06r00/chassis/*.php` game route ownership
- shared `/v01r00/chassis/*` startup/version ownership only through existing shared controllers
- Host/DI/settings/application-part gating
- no White-style `initialdatacheck.php`

## Implementation

1. Generate Murasaki wire classes into `Adapters.GameProtocol.Murasaki/Wire`.
2. Add Murasaki adapter project, route constants, DI extension, marker type, and thin controllers in separate files.
3. Register Murasaki in Host and remove the adapter application part when disabled.
4. Keep dumped `proto/murasaki` inputs unchanged.

## Verification

- `dotnet build TaikoLocalServer.slnx /p:EmitCompilerGeneratedFiles=true`
- `dotnet test Tests/Tests.csproj --no-build`
