# Phase 30 Plan: Split Metadata Readback

## Implementation

1. Reuse `GetInitialDataQuery(GameEra.Murasaki)` internally as the common split metadata source, but do not expose `initialdatacheck.php`.
2. Add separate controllers for default song, mainichi song, folder check/get folder, telop check/get telop, heartbeat, and bookkeeping.
3. Add Murasaki-specific mappers for single-folder response shape and telop/folder data.

## Verification

- Handler test verifies split metadata rows and conservative defaults.
- Full solution build verifies generated wire and controller compilation.
