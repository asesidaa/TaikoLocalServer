---
phase: 18-red-evidence-and-capability-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - Host/Program.cs
  - Host/Host.csproj
  - Host/Configurations/ServerSettings.json
  - Application/Settings/ServerSettingsOptionsValidationExtensions.cs
  - Tests/Red/RedServerSettingsValidationTests.cs
autonomous: true
requirements:
  - RFND-02
  - RFND-03
must_haves:
  truths:
    - "Red adapter services and application parts are available only when `ServerSettings:Eras:Red:Enabled` is true per D-16, D-17, RFND-02, and T-18-01."
    - "Red direct-protobuf game requests under `/v08r01/chassis` receive the existing missing-content-type fallback per D-03 and T-18-03."
    - "Red can be enabled in committed local settings without requiring unsupported shop or payment settings per D-12, D-16, and RFND-03."
  artifacts:
    - path: "Host/Program.cs"
      provides: "Red adapter DI registration, application-part removal, and `/v08r01/chassis` protobuf fallback"
      contains: "AddGameProtocolRed"
    - path: "Host/Configurations/ServerSettings.json"
      provides: "Committed local Red settings"
      contains: "\"Red\""
    - path: "Tests/Red/RedServerSettingsValidationTests.cs"
      provides: "Behavior test that Red does not inherit AC15 shop setting requirements"
      contains: "RedEnabledDoesNotRequireShopSettings"
  key_links:
    - from: "Host/Program.cs"
      to: "Adapters.GameProtocol.Red/DependencyInjection.cs"
      via: "conditional AddGameProtocolRed call"
      pattern: "enabledEras.Contains\\(GameEra\\.Red\\)"
    - from: "Host/Host.csproj"
      to: "Host/wwwroot/data/red/data"
      via: "operator data exclusion and Debug data junction"
      pattern: "wwwroot\\data\\red\\data"
---

<objective>
Wire Red into Host settings, DI, application-part gating, and local build data handling without adding Red gameplay/catalog state.

Purpose: RFND-02 requires Red route availability to be controlled by enabled-era settings, while RFND-03 requires Red settings not to inherit unsupported shop/payment requirements or alter existing era behavior.
Output: Host Red registration/gating, committed Red local settings, Red data-root build handling, and a focused Red settings validation test.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-CONTEXT.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-RESEARCH.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-PATTERNS.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-VALIDATION.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-RED-EVIDENCE.md
@AGENTS.md
@Host/Program.cs
@Host/Host.csproj
@Host/Configurations/ServerSettings.json
@Application/Settings/ServerSettingsOptionsValidationExtensions.cs
@Tests/Yellow/YellowServerSettingsValidationTests.cs
@Tests/Blue/BlueServerSettingsValidationTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Host Red settings, adapter gating, and data-root build handling</name>
  <files>Host/Program.cs, Host/Host.csproj, Host/Configurations/ServerSettings.json</files>
  <read_first>
    - Host/Program.cs
    - Host/Host.csproj
    - Host/Configurations/ServerSettings.json
    - Adapters.GameProtocol.Red/DependencyInjection.cs
    - .planning/phases/18-red-evidence-and-capability-foundation/18-RED-EVIDENCE.md
    - .planning/phases/18-red-evidence-and-capability-foundation/18-PATTERNS.md
    - AGENTS.md
  </read_first>
  <action>Add the Red adapter using directive, conditional `builder.Services.AddGameProtocolRed()` registration, disabled-era removal for `TaikoLocalServer.Adapters.GameProtocol.Red`, and `/v08r01/chassis` in `ShouldAssumeProtobufRequest`. Update the no-enabled-era fatal message so Red is named as an enableable era. Add `Adapters.GameProtocol.Red/Adapters.GameProtocol.Red.csproj` to `Host/Host.csproj`, exclude `wwwroot\data\red\data\**` and `_ContentIncludedByDefault` for Red operator data, and add a Debug Red data junction target matching the Green/Blue/Yellow pattern. Add a committed `ServerSettings:Eras:Red` block with `Enabled: true`, `AutoExtractCatalog: true`, `GameDataPath: "wwwroot/data/red/data"`, and empty `CustomizationNameDataPath`, but do not add `EnableShop` or `ActiveShopSeasonId` for Red. This implements D-03, D-04, D-16, D-17, and RFND-02 while preserving D-12, D-18, D-19, and RFND-03.</action>
  <verify>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"</automated>
  </verify>
  <acceptance_criteria>
    - Build assertion: temp-output Host build succeeds.
    - Source assertion: `Host/Program.cs` conditionally registers `AddGameProtocolRed` only when `enabledEras.Contains(GameEra.Red)`.
    - Source assertion: `Host/Program.cs` removes `TaikoLocalServer.Adapters.GameProtocol.Red` when Red is disabled.
    - Source assertion: `ShouldAssumeProtobufRequest` includes `/v08r01/chassis` and does not broaden to all `/v08r01` paths.
    - Source assertion: `Host/Configurations/ServerSettings.json` contains Red enabled with `GameDataPath` exactly `wwwroot/data/red/data` and no Red `EnableShop` / `ActiveShopSeasonId`.
    - Source assertion: `Host/Host.csproj` references the Red adapter and excludes `wwwroot\data\red\data\**` from content output while creating a Debug junction when local Red operator data exists.
  </acceptance_criteria>
  <done>Host can build with Red as an enableable adapter and Red operator data handling without advertising unsupported Red shop or gameplay behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect Red settings from unsupported shop validation</name>
  <files>Application/Settings/ServerSettingsOptionsValidationExtensions.cs, Tests/Red/RedServerSettingsValidationTests.cs</files>
  <read_first>
    - Application/Settings/ServerSettingsOptionsValidationExtensions.cs
    - Tests/Red/RedServerSettingsValidationTests.cs (planned new output path)
    - Tests/Yellow/YellowServerSettingsValidationTests.cs
    - Tests/Blue/BlueServerSettingsValidationTests.cs
    - .planning/phases/18-red-evidence-and-capability-foundation/18-CONTEXT.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Test 1: Red enabled with no `EnableShop` and no `ActiveShopSeasonId` validates successfully.
    - Test 2: Existing Yellow and Blue settings validation tests still enforce shop settings for eras that actually support shop behavior.
  </behavior>
  <action>Add `Tests/Red/RedServerSettingsValidationTests.cs` using the current Yellow settings validation helper style. The Red test should build a JSON configuration with only `ServerSettings:Eras:Red:Enabled = true`, bind `ServerSettings`, call `ValidateStartupSettings(new HashSet&lt;GameEra&gt; { GameEra.Red })`, and assert options resolve without `OptionsValidationException`. Keep `Application/Settings/ServerSettingsOptionsValidationExtensions.cs` unchanged unless the new test fails; if it must change, keep `Ac15ShopEras` limited to Green, Blue, and Yellow and do not add Red to shop validation per D-12 and D-16. Do not add source-string tests, enum numeric tests, controller route inventory tests, or project-file tests.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~RedServerSettingsValidationTests|FullyQualifiedName~YellowServerSettingsValidationTests|FullyQualifiedName~BlueServerSettingsValidationTests"</automated>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: Red settings validate without shop settings.
    - Behavior assertion: Yellow and Blue settings validation tests still pass.
    - Source assertion: `GameEra.Red` is not added to `Ac15ShopEras`.
    - Source assertion: no Red item-shop, medal, wallet, coupon, settlement, receipt, or transaction setting is introduced.
  </acceptance_criteria>
  <done>Red settings are enabled for route-probe smoke while preserving unsupported-shop and existing-era validation boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host config -> runtime routes | `ServerSettings:Eras` decides which protocol adapter controllers can be routed. |
| Missing content type -> protobuf model binding | Cabinet requests without content type are normalized into protobuf binding. |
| Build output -> operator data tree | Debug junctions expose local operator data in build output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of privilege / Information disclosure | `Host/Program.cs` application parts | mitigate | Add Red to the same enabled-era DI and disabled application-part removal pattern as Green/Blue/Yellow. |
| T-18-02 | Tampering | Route probe mutation | mitigate | This plan adds no controllers and no gameplay services; Plan 18-04 handles no-state probes. |
| T-18-03 | Tampering / Denial of service | `/v08r01/chassis` request binding | mitigate | Extend the existing direct-protobuf fallback only to `/v08r01/chassis`, not the whole version prefix. |
| T-18-04 | Repudiation / Information disclosure | Red settings and Banacoin/payment authority | mitigate | Do not add Red shop/payment settings or payment authority config. |
| T-18-05 | Tampering | Dumped proto evidence | mitigate | This plan does not modify `proto/red`; Plan 18-02 already owns generation verification. |
</threat_model>

<verification>
Run:
- `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~RedServerSettingsValidationTests|FullyQualifiedName~YellowServerSettingsValidationTests|FullyQualifiedName~BlueServerSettingsValidationTests"`
- `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"`
</verification>

<success_criteria>
- Red route exposure is gated by `ServerSettings:Eras:Red:Enabled`.
- Red local settings are present and smoke-ready.
- Red direct-protobuf fallback is scoped to `/v08r01/chassis`.
- Red settings do not require or imply item-shop/payment behavior.
</success_criteria>

<output>
Create `.planning/phases/18-red-evidence-and-capability-foundation/18-03-SUMMARY.md` when done.
</output>
