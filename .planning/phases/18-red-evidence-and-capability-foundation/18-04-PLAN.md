---
phase: 18-red-evidence-and-capability-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 18-02
  - 18-03
files_modified:
  - Adapters.GameProtocol.Red/Controllers/RedRouteProbeControllers.cs
  - .planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md
autonomous: false
requirements:
  - RFND-01
  - RFND-02
  - RFND-03
must_haves:
  truths:
    - "IDB-known Red game suffixes route under `/v08r01/chassis/*` as no-state probes for manual request-routing smoke per D-07, D-13, D-14, D-20, RFND-01, and RFND-02."
    - "Route probes deserialize generated Red wire DTOs and do not call gameplay Mediator handlers, EF, shared AC15 gameplay services, or payment authority code per D-14, D-18, D-19, T-18-02, and T-18-04."
    - "The phase closes only after automated tests/build plus user-run RPCS3/cabinet routing smoke are recorded per D-20, D-21, RFND-03."
  artifacts:
    - path: "Adapters.GameProtocol.Red/Controllers/RedRouteProbeControllers.cs"
      provides: "No-state Red route probes under `/v08r01/chassis/*`"
      contains: "Red PlayResult request"
    - path: ".planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md"
      provides: "Manual Red route-routing smoke checklist and evidence capture record"
      contains: "RPCS3"
  key_links:
    - from: "Adapters.GameProtocol.Red/Controllers/RedRouteProbeControllers.cs"
      to: "Adapters.GameProtocol.Red/Wire/Game.cs"
      via: "generated request/response DTOs"
      pattern: "PlayResultRequest"
    - from: ".planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md"
      to: "Host/Program.cs"
      via: "manual server run against Red enabled route probes"
      pattern: "/v08r01/chassis"
---

<objective>
Expose the IDB-known Red game route suffixes as no-state route probes and create the manual smoke handoff artifact.

Purpose: Phase 18 must make Red request-routing observable without implementing gameplay state. RFND-01 and RFND-02 need route probes for manual RPCS3/cabinet evidence, while RFND-03 requires full preservation checks and a manual close gate.
Output: Red no-state route probes and `18-RUNTIME-SMOKE.md`.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-CONTEXT.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-RESEARCH.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-PATTERNS.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-VALIDATION.md
@.planning/phases/18-red-evidence-and-capability-foundation/18-RED-EVIDENCE.md
@AGENTS.md
@Adapters.GameProtocol.Red/Wire/Game.cs
@Adapters.GameProtocol.Yellow/Controllers/BookkeepingController.cs
@Adapters.GameProtocol.Yellow/Controllers/HeartbeatController.cs
@Adapters.GameProtocol.Yellow/Controllers/ChallengeCompeController.cs
@Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs
@Adapters.GameProtocol.Blue/Controllers/BanacoinPaymentController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement no-state Red route probes for IDB-known game suffixes</name>
  <files>Adapters.GameProtocol.Red/Controllers/RedRouteProbeControllers.cs</files>
  <read_first>
    - Adapters.GameProtocol.Red/Controllers/RedRouteProbeControllers.cs (planned new output path)
    - Adapters.GameProtocol.Red/Wire/Game.cs
    - Adapters.GameProtocol.Yellow/Controllers/BookkeepingController.cs
    - Adapters.GameProtocol.Yellow/Controllers/HeartbeatController.cs
    - Adapters.GameProtocol.Yellow/Controllers/ChallengeCompeController.cs
    - Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs
    - Adapters.GameProtocol.Blue/Controllers/BanacoinPaymentController.cs
    - .planning/phases/18-red-evidence-and-capability-foundation/18-RED-EVIDENCE.md
    - .planning/phases/18-red-evidence-and-capability-foundation/18-PATTERNS.md
    - AGENTS.md
  </read_first>
  <action>Create `Adapters.GameProtocol.Red/Controllers/RedRouteProbeControllers.cs` with probe controllers under `/v08r01/chassis/*` for exactly the IDB-known suffixes from D-13 and `18-RED-EVIDENCE.md`: `playresult.php`, `banacoinerrorlog.php`, `baidcheck.php`, `mydonentry.php`, `userdata.php`, `challengecompe.php`, `balancecheck.php`, `banacoinpayment.php`, `crownsdata.php`, `recommend.php`, `selfbest.php`, `heartbeat.php`, `rewardcardcheck.php`, `rewardexecution.php`, `initialdatacheck.php`, `tournamentcheck.php`, `bookkeeping.php`, `coinsetting.php`, `gettelop.php`, `getfolder.php`, `taikojuku.php`, and `headclerk2.php`. Each action should accept the generated Red request DTO via `[FromBody]`, log enough request data for manual smoke using a Red-specific structured message, and return a minimal generated Red response with all required response fields populated. Use `Result = 1` for success-shaped responses, heartbeat status fields set to `1`, Banacoin-adjacent `Personid` echoes when the generated response requires `personid`, and empty repeated arrays when semantics are unproven. Do not add `getbanacoininfo.php` or `getreitai.php` because they are proto-only candidates. Do not call `Mediator`, `GetChallengeCompeQuery`, `ITaikoDbContext`, EF, shared AC15 gameplay services, Red profile/catalog services, or payment/wallet/coupon/transaction code. This implements D-07, D-12, D-13, D-14, D-18, D-19, and protects T-18-02, T-18-03, and T-18-04.</action>
  <verify>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"</automated>
    <automated>if (rg -n "Mediator|GetChallengeCompeQuery|ITaikoDbContext|TaikoDbContext|SaveChanges|Ac15EraProfiles|Wallet|Coupon|Transaction" Adapters.GameProtocol.Red/Controllers) { exit 1 }</automated>
    <automated>if (rg -n "getbanacoininfo|getreitai" Adapters.GameProtocol.Red/Controllers) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - Build assertion: temp-output Host build succeeds.
    - Source assertion: all 22 IDB-known Red route suffixes listed in the action exist under `/v08r01/chassis/*`.
    - Source assertion: `getbanacoininfo.php` and `getreitai.php` are not present in Red controllers.
    - Source assertion: Red probe controllers use generated Red request/response DTOs and do not manually parse request bodies.
    - CLI assertion: the no-state audit finds no `Mediator`, `GetChallengeCompeQuery`, `ITaikoDbContext`, `TaikoDbContext`, `SaveChanges`, `Ac15EraProfiles`, wallet, coupon, or transaction usage in Red controllers.
    - Source assertion: Red `ChallengeCompe` route is a probe-only response and does not copy Yellow `GetChallengeCompeQuery` semantics.
  </acceptance_criteria>
  <done>Red route probes are available for request-routing smoke without adding gameplay, ChallengeCompe, Tokkun, Banacoin/payment, or EF state.</done>
</task>

<task type="auto">
  <name>Task 2: Run preservation checks and prepare the runtime smoke evidence record</name>
  <files>.planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md</files>
  <read_first>
    - .planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md (planned new output path)
    - .planning/phases/18-red-evidence-and-capability-foundation/18-VALIDATION.md
    - .planning/phases/18-red-evidence-and-capability-foundation/18-RED-EVIDENCE.md
    - Host/Program.cs
    - Host/Configurations/ServerSettings.json
    - AGENTS.md
  </read_first>
  <action>Create `18-RUNTIME-SMOKE.md` with the exact manual smoke setup, expected log evidence, route sequence table, unknown-route capture area, and explicit fields for the user-run Red RPCS3/cabinet request-routing smoke per D-20. Include commands for the implementer to run `dotnet run --project Host` or equivalent after automated verification, but do not record smoke as passed until the user supplies runtime confirmation. Run the full preservation gate from `18-VALIDATION.md`: focused Red/settings checks if present, full `dotnet test Tests/Tests.csproj`, and temp-output Host build. Record command names and pass/fail results in the summary, not inside the smoke artifact unless the command actually ran. Keep review scope lightweight and limited to touched Green, Blue, Yellow, Nijiiro, shared `/v01r00`, Host, and Red adapter surfaces per D-18 and D-21.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj</automated>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"</automated>
    <automated>Test-Path '.planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md'</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: full test suite passes before manual smoke is requested.
    - CLI assertion: temp-output Host build passes before manual smoke is requested.
    - Source assertion: `18-RUNTIME-SMOKE.md` contains setup, expected Red log lines, route sequence capture, unknown-route capture, and user confirmation fields.
    - Source assertion: `18-RUNTIME-SMOKE.md` does not claim runtime smoke has passed before user confirmation.
  </acceptance_criteria>
  <done>Automated preservation evidence is fresh and the manual Red route-routing smoke record is ready for user verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies Red request-routing smoke</name>
  <files>.planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md</files>
  <read_first>
    - .planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md
    - .planning/phases/18-red-evidence-and-capability-foundation/18-RED-EVIDENCE.md
    - .planning/phases/18-red-evidence-and-capability-foundation/18-VALIDATION.md
  </read_first>
  <action>After Task 2 automated verification passes, ask the user to run the Red cabinet/RPCS3 request-routing smoke against the local server with Red enabled. The user should confirm which `/v01r00/chassis/*` startup/version requests and `/v08r01/chassis/*` game-route probes appeared in logs, whether missing-content-type direct protobuf binding worked, whether any unknown routes appeared, and whether any minimal response shape blocked the client before enough routing evidence was captured. Update `18-RUNTIME-SMOKE.md` with the user-supplied result before closing the plan.</action>
  <verify>
    <automated>Test-Path '.planning/phases/18-red-evidence-and-capability-foundation/18-RUNTIME-SMOKE.md'</automated>
    <human-check>User confirms the Red request-routing smoke result or provides issues to fix.</human-check>
  </verify>
  <acceptance_criteria>
    - Manual-only gate: user confirms Red route-probe logging is sufficient for Phase 18 close, or reports concrete routing/response-shape issues.
    - Source assertion: `18-RUNTIME-SMOKE.md` records the manual result with concrete dates, route observations, and unresolved gaps.
    - Source assertion: the plan is not summarized as complete while this checkpoint is unresolved.
  </acceptance_criteria>
  <done>Manual Red route-routing smoke evidence is recorded, or concrete issues are available for a follow-up fix before close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cabinet/RPCS3 -> Red route probes | Untrusted protobuf requests reach new Red adapter controllers. |
| Red route probes -> logs | Full request objects are logged for evidence capture. |
| Manual smoke -> planning closeout | External runtime evidence is recorded as phase acceptance data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of privilege / Information disclosure | Red route exposure | mitigate | Depends on Plan 18-03 Host gating; manual smoke uses Red enabled local config only. |
| T-18-02 | Tampering | Red probe controllers | mitigate | Probes log and return minimal generated responses only; no Mediator, EF, SaveChanges, shared gameplay service, or profile/catalog call is allowed. |
| T-18-03 | Tampering / Denial of service | Request body parsing | mitigate | Use generated Red wire DTOs via `[FromBody]` and existing direct-protobuf fallback; no manual binary parsing. |
| T-18-04 | Repudiation / Information disclosure | Banacoin/payment-adjacent probes | mitigate | Balance/payment/error routes are compatibility probes only, echo required fields, and create no wallet, payment, coupon, settlement, receipt, or transaction state. |
| T-18-05 | Tampering | Dumped proto evidence | mitigate | This plan does not modify `proto/red`; generated wire was produced in Plan 18-02. |
</threat_model>

<verification>
Run:
- `dotnet test Tests/Tests.csproj`
- `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"`
- no-state `rg` audits from Task 1
- user-run Red RPCS3/cabinet request-routing smoke checkpoint
</verification>

<success_criteria>
- Red no-state route probes compile and are gated by Red enabled settings.
- Full automated tests and temp-output Host build pass.
- Manual Red request-routing smoke is recorded before the plan is closed.
- Existing supported-era behavior remains unchanged except for covered Red foundation additions.
</success_criteria>

<output>
Create `.planning/phases/18-red-evidence-and-capability-foundation/18-04-SUMMARY.md` when done.
</output>
