---
phase: 01-blue-a6-item-shop-and-unlocking
plan: "01-03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Application/Handlers/GetItemShopInfoQuery.cs
  - Application/Handlers/GetItemShopInfoQuery.Green.cs
  - Application/Handlers/GetItemShopInfoQuery.Blue.cs
  - Application/Handlers/GetInitialDataQuery.Blue.cs
  - Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
  - Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs
  - Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
  - Tests/Blue/BlueItemShopProtocolTests.cs
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-08
requirements_addressed:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-08
user_setup: []
must_haves:
  truths:
    - "SHOP-01 D-16: Blue initialdatacheck advertises item shop only for enabled active seasons with rows and removes active shop songs from default flags."
    - "SHOP-02: Blue getitemshopinfo returns the configured active Blue season fields and ordered protocol item_no rows."
    - "D-03: Blue mapper preserves omitted item_type, item_id, and item_price as null command values for purchase preflight."
  artifacts:
    - path: "Application/Handlers/GetItemShopInfoQuery.Blue.cs"
      provides: "Blue active-season item shop info query"
    - path: "Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs"
      provides: "Blue wire/common item-shop mapper"
    - path: "Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs"
      provides: "Mediator-backed Blue getitemshopinfo.php endpoint"
    - path: "Tests/Blue/BlueItemShopProtocolTests.cs"
      provides: "Blue initialdata, shop-info, and optional-field protocol tests"
  key_links:
    - from: "Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs"
      to: "Application/Handlers/GetItemShopInfoQuery.Blue.cs"
      via: "Mediator.Send(ItemShopMappers.Map(request))"
      pattern: "GameEra.Blue"
---

<objective>
Replace Blue shop-info success-only behavior with active-season advertisement and mapper behavior.

Purpose: Cabinets need accurate active-season metadata before purchase can safely validate against catalog rows.
Output: Era-aware shop-info query dispatch, Blue mapper/controller, initialdata advertisement checks, and protocol tests for SHOP-01/SHOP-02.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-CONTEXT.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-RESEARCH.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-VALIDATION.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-PATTERNS.md
@Application/Handlers/GetItemShopInfoQuery.cs
@Application/Handlers/GetItemShopInfoQuery.Green.cs
@Application/Handlers/GetInitialDataQuery.Blue.cs
@Application/Handlers/UpdatePlayResultCommand.cs
@Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
@Adapters.GameProtocol.Green/Controllers/GetItemShopInfoController.cs
@Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
@Adapters.GameProtocol.Blue/Wire/Game.cs
@proto/blue/taiko.proto
@Tests/Green/GreenItemShopProtocolTests.cs
@Tests/Blue/BlueHandlerFixture.cs
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make shop-info query era-aware and wire Blue getitemshopinfo</name>
  <read_first>
    Application/Handlers/GetItemShopInfoQuery.cs
    Application/Handlers/GetItemShopInfoQuery.Green.cs
    Application/Handlers/UpdatePlayResultCommand.cs
    Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
    Adapters.GameProtocol.Green/Controllers/GetItemShopInfoController.cs
    Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
    Adapters.GameProtocol.Blue/Wire/Game.cs
    Tests/Green/GreenItemShopProtocolTests.cs
  </read_first>
  <files>
    Application/Handlers/GetItemShopInfoQuery.cs
    Application/Handlers/GetItemShopInfoQuery.Green.cs
    Application/Handlers/GetItemShopInfoQuery.Blue.cs
    Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
    Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs
    Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
    Tests/Blue/BlueItemShopProtocolTests.cs
  </files>
  <action>
    Add `GameEra Era` to `GetItemShopInfoQuery`, dispatch to `HandleGreen` and `HandleBlue`, and preserve Green behavior by passing `GameEra.Green` from Green mapper/controller paths. Implement `GetItemShopInfoQuery.Blue.cs` to read `gameDataService.Blue().ItemShopCatalog.ActiveSeason`; return success with empty rows when disabled or inactive, and return only the active Blue season when enabled. Add `Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs` for Blue wire types and `CommonItemShopInfoResponse`, mapping `GameEra.Blue`, season envelope fields, ordered `ItemNo`, `ItemType`, `ItemId`, and `ItemPrice`. Replace the Blue `GetItemShopInfoController` success-only response with `Mediator.Send` plus the Blue mapper.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueItemShopProtocolTests</automated>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~GreenItemShopProtocolTests</automated>
  </verify>
  <acceptance_criteria>
    - `GetItemShopInfoQuery.cs` dispatches `GameEra.Blue` to `HandleBlue` and throws for unsupported eras.
    - Blue protocol tests prove disabled/inactive shop returns success without advertised rows and enabled shop returns the active season envelope plus ordered rows.
    - Green item-shop protocol tests still pass after shared dispatch.
  </acceptance_criteria>
  <done>Blue `getitemshopinfo.php` is mediator-backed and returns active Blue season data without breaking Green.</done>
</task>

<task type="auto">
  <name>Task 2: Lock initialdata advertisement and Blue optional-field mapping</name>
  <read_first>
    Application/Handlers/GetInitialDataQuery.Blue.cs
    Application/Common/BlueProtocolBytes.cs
    Application/Catalog/Blue/BlueItemShopCatalog.cs
    Adapters.GameProtocol.Blue/Wire/Game.cs
    Tests/Green/GreenItemShopProtocolTests.cs
    Tests/Blue/BlueHandlerFixture.cs
  </read_first>
  <files>
    Application/Handlers/GetInitialDataQuery.Blue.cs
    Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs
    Tests/Blue/BlueItemShopProtocolTests.cs
  </files>
  <action>
    Verify and adjust `GetInitialDataQuery.Blue.cs` so `IsItemshop` is true only when `blue.ItemShopCatalog.IsEnabled`, `ActiveSeason` is non-null, and `ActiveSeason.Items.Count > 0`; `AryBlueItemShopDatas` must be empty otherwise. Ensure default song flags are built from Blue catalog songs and active-season shop songs are removed per D-16 using `BlueProtocolBytes` widths. In the Blue item-shop mapper, map `ItempurchaseRequest` optional `item_type`, `item_id`, and `item_price` through `ShouldSerialize*()` so D-03 preflight remains distinguishable from zero-valued fields.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueItemShopProtocolTests</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove `initialdatacheck.php` does not advertise shop when disabled, missing active season, or active season has no rows.
    - Tests prove enabled active-season metadata contains the expected Blue season id and `VerupNo`.
    - Tests prove active-season shop songs are cleared from `DefaultSongFlg`.
    - Tests prove `item_no == 0` preflight maps omitted optional fields to null command values.
  </acceptance_criteria>
  <done>Blue initialdata advertises only valid active shop seasons and the mapper preserves preflight optional-field semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blue catalog -> protocol response | Filesystem-loaded shop catalog becomes cabinet-visible protobuf data. |
| Blue adapter -> Application mediator | Blue wire requests are translated into common queries with explicit era ownership. |
| shared query dispatch -> Green/Blue partials | Shared handler files must route by `GameEra` without crossing era state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | `GetInitialDataQuery.Blue.cs` | mitigate | Clear active-season shop songs and suppress advertisement unless enabled active rows exist. |
| T-01-08 | Tampering | `GetItemShopInfoQuery.Blue.cs` | mitigate | Return only `BlueItemShopCatalog.ActiveSeason`; never accept season selection from the client. |
| T-01-09 | Tampering | `Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs` | mitigate | Map Blue wire fields to `GameEra.Blue`; tests cover ordered rows and optional field presence. |
| T-01-SC | Tampering | package installs | accept | No new packages or package-manager installs are needed. |
</threat_model>

<verification>
Run after this plan:
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueItemShopProtocolTests`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~GreenItemShopProtocolTests`
</verification>

<success_criteria>
- Blue `getitemshopinfo.php` returns active-season data from Blue catalog only.
- Blue `initialdatacheck.php` advertises shop only under enabled active-season conditions and hides active-season shop songs.
- Blue optional purchase fields preserve D-03 preflight semantics.
</success_criteria>

<output>
Create `.planning/phases/01-blue-a6-item-shop-and-unlocking/01-03-SUMMARY.md` when done.
</output>
