---
phase: 01-blue-a6-item-shop-and-unlocking
plan: "01-02"
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - Application/Handlers/GetItemShopInfoQuery.cs
  - Application/Handlers/GetItemShopInfoQuery.Green.cs
  - Application/Handlers/GetItemShopInfoQuery.Blue.cs
  - Application/Handlers/GetInitialDataQuery.Blue.cs
  - Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
  - Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs
  - Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
  - Tests/Blue/BlueItemShopProtocolTests.cs
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-08
requirements_addressed:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-08
user_setup: []
must_haves:
  truths:
    - "SHOP-01 D-16: Blue initialdatacheck advertises item shop only for enabled active seasons with rows and removes active shop songs from default flags."
    - "SHOP-02: Blue getitemshopinfo returns the configured active Blue season fields and ordered protocol item_no rows."
    - "D-03: itempurchase preflight optional fields remain distinguishable through Blue mapper presence checks, even though purchase execution is completed in plan 01-03."
  artifacts:
    - path: "Application/Handlers/GetItemShopInfoQuery.Blue.cs"
      provides: "Blue active-season item shop info query"
    - path: "Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs"
      provides: "Blue wire/common item-shop mapper"
    - path: "Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs"
      provides: "Mediator-backed Blue getitemshopinfo.php endpoint"
    - path: "Tests/Blue/BlueItemShopProtocolTests.cs"
      provides: "Blue initialdata, shop-info, and optional-field protocol tests"
  key_links:
    - from: "Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs"
      to: "Application/Handlers/GetItemShopInfoQuery.Blue.cs"
      via: "Mediator.Send(ItemShopMappers.Map(request))"
      pattern: "GameEra.Blue"
    - from: "Application/Handlers/GetInitialDataQuery.Blue.cs"
      to: "Application/Catalog/Blue/BlueItemShopCatalog.cs"
      via: "Blue catalog active season"
      pattern: "ItemShopCatalog"
---

<objective>
Replace the Blue shop-info stub with Blue-owned advertisement and mapper behavior.

Purpose: Cabinets need accurate active-season shop metadata before purchase can be safe, and Blue protocol files must not import Green wire models or Green shop state.
Output: Era-aware shop-info query dispatch, Blue getitemshopinfo mapper/controller, initialdata regression coverage, and protocol tests for SHOP-01, SHOP-02, SHOP-03, and SHOP-08.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-CONTEXT.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-RESEARCH.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-VALIDATION.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-PATTERNS.md
@Application/Handlers/GetItemShopInfoQuery.cs
@Application/Handlers/GetItemShopInfoQuery.Green.cs
@Application/Handlers/GetInitialDataQuery.Blue.cs
@Application/Handlers/UpdatePlayResultCommand.cs
@Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
@Adapters.GameProtocol.Green/Controllers/GetItemShopInfoController.cs
@Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
@Adapters.GameProtocol.Blue/Wire/Game.cs
@proto/blue/taiko.proto
@Tests/Green/GreenItemShopProtocolTests.cs
@Tests/Blue/BlueHandlerFixture.cs
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-01-SUMMARY.md
</context>

<must_haves>
- SHOP-01 is satisfied when `GetInitialDataQuery.Blue.cs` returns `IsItemshop = true` and one `AryBlueItemShopDatas` row only when Blue shop is enabled, the active season exists, and active season items are nonempty.
- SHOP-02 is satisfied when Blue `getitemshopinfo.php` returns only the active Blue season with `VerupNo`, `SeasonId`, `Telop`, `StartDatetime`, `EndDatetime`, `AfterstartDays`, `BeforecloseDays`, and ordered `AryItemshopDatas`.
- SHOP-03 remains protected by using the `BlueItemShopCatalog` produced by plan 01-01 and not adding alternate runtime data sources.
- SHOP-08 remains protected by keeping Blue mappers/controllers free of Green protocol namespaces, Green shop state, and Green protocol constants.
- D-03 is preserved in mapper tests: omitted `item_type`, `item_id`, and `item_price` must map to null command values for `item_no == 0` preflight.
- D-16 is preserved in initialdata tests: active-season shop songs are cleared from default song flags before response mapping.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Make shop-info query era-aware and add Blue getitemshopinfo mapping</name>
  <read_first>
    .planning/phases/01-blue-a6-item-shop-and-unlocking/01-CONTEXT.md
    .planning/phases/01-blue-a6-item-shop-and-unlocking/01-RESEARCH.md
    .planning/phases/01-blue-a6-item-shop-and-unlocking/01-PATTERNS.md
    Application/Handlers/GetItemShopInfoQuery.cs
    Application/Handlers/GetItemShopInfoQuery.Green.cs
    Application/Handlers/UpdatePlayResultCommand.cs
    Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
    Adapters.GameProtocol.Green/Controllers/GetItemShopInfoController.cs
    Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
    Adapters.GameProtocol.Blue/Wire/Game.cs
    proto/blue/taiko.proto
    Tests/Green/GreenItemShopProtocolTests.cs
  </read_first>
  <files>
    Application/Handlers/GetItemShopInfoQuery.cs
    Application/Handlers/GetItemShopInfoQuery.Green.cs
    Application/Handlers/GetItemShopInfoQuery.Blue.cs
    Adapters.GameProtocol.Green/Mappers/ItemShopMappers.cs
    Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs
    Adapters.GameProtocol.Blue/Controllers/GetItemShopInfoController.cs
    Tests/Blue/BlueItemShopProtocolTests.cs
  </files>
  <action>
    Add `GameEra Era` to `GetItemShopInfoQuery`, route through `HandleGreen` and `HandleBlue` partial methods, and preserve existing Green behavior by passing `GameEra.Green` from Green mapper/controller paths. Implement `GetItemShopInfoQuery.Blue.cs` to read `gameDataService.Blue().ItemShopCatalog.ActiveSeason`; return success with an empty item list when disabled or inactive, and return only the configured active Blue season when enabled. Add `Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs` for Blue wire types and `CommonItemShopInfoResponse`; the mapper must use `GameEra.Blue`, must not import `Adapters.GameProtocol.Green`, and must preserve ordered `ItemNo`, `ItemType`, `ItemId`, and `ItemPrice` rows. Replace the Blue `GetItemShopInfoController` success stub with `Mediator.Send` plus the Blue mapper.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueItemShopProtocolTests</automated>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~GreenItemShopProtocolTests</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `GetItemShopInfoQuery.cs` dispatches `GameEra.Blue` to `HandleBlue` and throws for unsupported eras.
    - Source assertion: `Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs` references Blue wire types and `GameEra.Blue`, with no Green adapter namespace imports.
    - Behavior assertion: Blue protocol tests prove disabled/inactive shop returns success without advertised rows and enabled shop returns the active season envelope plus ordered rows.
    - Regression assertion: Green item-shop protocol tests still pass after adding era dispatch.
  </acceptance_criteria>
  <done>Blue `getitemshopinfo.php` is mediator-backed and returns active Blue season data without breaking Green.</done>
</task>

<task type="auto">
  <name>Task 2: Lock initialdata advertisement to active Blue shop rows</name>
  <read_first>
    .planning/phases/01-blue-a6-item-shop-and-unlocking/01-CONTEXT.md
    .planning/phases/01-blue-a6-item-shop-and-unlocking/01-VALIDATION.md
    .planning/phases/01-blue-a6-item-shop-and-unlocking/01-PATTERNS.md
    Application/Handlers/GetInitialDataQuery.Blue.cs
    Application/Common/BlueProtocolBytes.cs
    Application/Common/BlueShopStateExtensions.cs
    Application/Catalog/Blue/BlueItemShopCatalog.cs
    Tests/Green/GreenItemShopProtocolTests.cs
    Tests/Blue/BlueHandlerFixture.cs
  </read_first>
  <files>
    Application/Handlers/GetInitialDataQuery.Blue.cs
    Tests/Blue/BlueItemShopProtocolTests.cs
  </files>
  <action>
    Verify and adjust `GetInitialDataQuery.Blue.cs` so `IsItemshop` is true only when `blue.ItemShopCatalog.IsEnabled`, `ActiveSeason` is non-null, and `ActiveSeason.Items.Count > 0`; `AryBlueItemShopDatas` must be empty otherwise. Ensure default song flags are built from Blue catalog songs and then active-season shop songs are removed per D-16, using `BlueProtocolBytes` widths and Blue-owned helpers only. Keep this as normal Track A behavior and do not add battle, Banacoin, Tokkun, AdminApi, or WebUI work.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueItemShopProtocolTests</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: protocol tests prove `initialdatacheck.php` does not advertise shop when disabled, when no active season exists, or when active season has no rows.
    - Behavior assertion: protocol tests prove enabled active-season metadata contains the Blue `InfoId`/season id and `VerupNo` expected by the Blue wire mapper.
    - Behavior assertion: protocol tests prove active-season shop songs are cleared from `DefaultSongFlg` before purchase.
    - Source assertion: `GetInitialDataQuery.Blue.cs` does not reference Green shop state, Green protocol constants, or Green wire models.
  </acceptance_criteria>
  <done>Blue initialdata advertises only valid active shop seasons and hides active shop songs until purchase unlocks them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blue catalog -> protocol response | Filesystem-loaded shop catalog becomes cabinet-visible protobuf data. |
| Blue adapter -> Application mediator | Blue wire requests are translated into common queries with explicit era ownership. |
| shared query dispatch -> Green/Blue partials | Shared handler files must route by `GameEra` without crossing era state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | `GetInitialDataQuery.Blue.cs` | mitigate | Clear active-season shop songs from default flags and suppress shop advertisement unless enabled active rows exist. |
| T-01-06 | Tampering | `GetItemShopInfoQuery.Blue.cs` | mitigate | Return only `BlueItemShopCatalog.ActiveSeason` rows and never accept season selection from the client. |
| T-01-07 | Tampering | `Adapters.GameProtocol.Blue/Mappers/ItemShopMappers.cs` | mitigate | Map Blue wire fields to `GameEra.Blue`; tests cover ordered rows and optional field presence. |
| T-01-08 | Repudiation | Blue shop-info logs/controllers | mitigate | Keep controller request logging and route through Mediator so behavior is testable and consistent with Green shape. |
| T-01-SC | Tampering | package installs | accept | No new packages or package-manager installs are needed. |
</threat_model>

<verification>
Run after this plan:
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueItemShopProtocolTests`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~GreenItemShopProtocolTests`
- `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~Blue|FullyQualifiedName~Ac15|FullyQualifiedName~GreenItemShop"`
</verification>

<success_criteria>
- Blue `getitemshopinfo.php` is no longer a stub and returns active-season data from Blue catalog only.
- Blue `initialdatacheck.php` advertises item shop only under enabled active-season conditions and hides active-season shop songs.
- Green protocol tests continue passing after shared era dispatch.
- Blue adapter/application files introduced here have no Green shop or Green wire dependencies.
</success_criteria>

<output>
Create `.planning/phases/01-blue-a6-item-shop-and-unlocking/01-02-SUMMARY.md` when done.
</output>
