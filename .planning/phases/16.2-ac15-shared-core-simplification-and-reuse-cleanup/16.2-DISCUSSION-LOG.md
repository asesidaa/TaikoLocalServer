# Phase 16.2: AC15 Shared Core Simplification and Reuse Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves the alternatives considered.

**Date:** 2026-06-10
**Phase:** 16.2-ac15-shared-core-simplification-and-reuse-cleanup
**Areas discussed:** Playresult core, foundational aliases and helpers, Dani workflow, shop reuse, normal play adapter internals, catalog and DTO cleanup, non-reuse boundaries, sequencing and proof

---

## Playresult Core

| Option | Description | Selected |
|--------|-------------|----------|
| Save shell | Move duplicated medal totals, tutorial flags, play time, area, profile counters, costume/unlock bits, and normal-save orchestration behind shared AC15 service/adapters. | Yes |
| Dani first | Extract repeated Dan score, stage score, packed flags, GotDanMax, display-Dan, and Dan costume flow before broader playresult cleanup. | |
| Minimal helpers | Only collapse obvious utility duplication now and leave most playresult side effects in era partial handlers. | |

**User's choice:** Save shell.
**Notes:** Special-mode classification remains era-local before the shared save shell. Unrecognized shapes should log the existing payload, skip mutation, and return success because deterministic clients do not produce invalid payloads; unexpected shapes mean a missing server case.

## Special-Mode Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Era gate first | Blue battle/Tokkun, Yellow Tokkun, and Green AI/ghost rules remain explicit era-local gates before the shared normal-save shell runs. | Yes |
| Core hook gate | The shared core receives every playresult and calls hooks to handle or continue special modes. | |
| Core validates | The shared core owns stage/mode validation broadly, with eras only supplying limits and persistence. | |

**User's choice:** Era gate first.
**Notes:** Shapes still need classification because different modes require different handling.

## Playresult Proof

| Option | Description | Selected |
|--------|-------------|----------|
| Mode matrix | Focused behavior tests cover normal, Dan, Blue battle, Blue/Yellow Tokkun, Green AI/ghost, and unrecognized log-success/no-write paths by era. | |
| Shared core tests | Emphasize AC15 service unit tests, with only thin era smoke tests around handlers. | |
| Regression only | Avoid new test surfaces unless something breaks; rely on existing Green/Blue/Yellow suites plus full build. | Yes |

**User's choice:** Regression only.
**Notes:** Later broadened to regression slices for the full phase, with targeted observable tests only when a touched behavior is not otherwise covered.

## Scope Correction

| Option | Description | Selected |
|--------|-------------|----------|
| All decision groups | Discuss and lock every major group: aliases/helpers, Dani, shop, normal play, catalog/DTO cleanup, and non-reuse boundaries. | Yes |
| Recommended batch | Lock the handoff's first refactor batch only: decisions 1-4, 7-11, and 15. | |
| Pick subset | Present numbered decision groups and wait for a comma-separated selection. | |

**User's choice:** All decision groups.
**Notes:** User corrected that playresult cleanup is only a small part of the phase and pointed to `C:/Users/10614/AppData/Local/Temp/taikolocalserver-ac15-reuse-handoff-2026-06-10T05-44-15+08-00.md`.

## Foundational Aliases And Helpers

| Option | Description | Selected |
|--------|-------------|----------|
| Canonical AC15 types | Replace duplicate Dan clear-grade enums, shop status enums, crown state aliases, protocol bit helpers, and profile counter helpers where values and behavior match. | Yes |
| Era wrappers | Keep Blue/Green/Yellow names as thin wrappers over shared AC15 helpers to reduce call-site churn. | |
| Helpers only | Share pure helper methods but keep all existing era enum/type names. | |

**User's choice:** Canonical AC15 types.
**Notes:** Canonicalization is approved where current values and behavior match.

## Dani Workflow

| Option | Description | Selected |
|--------|-------------|----------|
| Shared Dani service | Extract Dan validation, score/stage updates, packed flags, GotDanMax, display-Dan, and Dan costume flow behind typed era persistence/save adapters. | Yes |
| Helper extraction | Only collapse Dan helper logic and packed-grade code; leave Dan save/readback workflow in each era handler. | |
| Leave local | Keep Dani workflow local to Blue/Green/Yellow handlers for now. | |

**User's choice:** Shared Dani service.
**Notes:** Per-era Dan EF tables and save data remain separate.

## Shop Reuse

| Option | Description | Selected |
|--------|-------------|----------|
| Shared with policies | Unify shop status and common season/purchase/status plumbing, with explicit seed and unlock policies for Blue, Green, and Yellow. | Yes |
| Status/helpers only | Unify the duplicate status enum and simple helpers but leave adapters and purchase flow mostly as-is. | |
| No shop cleanup | Keep shop state and purchase cleanup out of Phase 16.2. | |

**User's choice:** Shared with policies.
**Notes:** Blue starts shop season totals at zero; Green and Yellow seed from save data. Unlock policy differences remain explicit.

## Normal Play Adapter Internals

| Option | Description | Selected |
|--------|-------------|----------|
| Factor internals | Keep typed era adapters, but factor repeated best/favorite/recent/crown-rank/user-exists/save helpers where behavior is identical. | Yes |
| Shell only | Extract the playresult save-shell orchestration but leave adapter internals duplicated for auditability. | |
| Full generic adapter | Build a more generic EF adapter abstraction across SongBest/SongPlay/Favorite/Recent tables. | |

**User's choice:** Factor internals.
**Notes:** The user did not approve a broad table-generic EF adapter.

## Catalog And DTO Cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| Canonical AC15 DTOs | Use AC15 catalog value objects and generic catalog handler helpers, and move shared AC15 fields out of misleading Green/Blue/Yellow-named Common DTO partials. | Yes |
| Handlers only | Unify simple catalog handlers but leave Common DTO partial naming untouched. | |
| Defer DTOs | Leave catalog/DTO cleanup for a later phase and focus Phase 16.2 on runtime services. | |

**User's choice:** Canonical AC15 DTOs.
**Notes:** Wire mappers still own generated DTO placement per era.

## Non-Reuse Boundaries

| Option | Description | Selected |
|--------|-------------|----------|
| Strict boundaries | No shared EF tables, no shared generated wire assembly, no route inference/stubs, and no generic special-mode handling. | Yes |
| State-only strict | Forbid shared EF tables but allow broader shared routing/helper conventions where convenient. | |
| Document only | Record boundaries in context but let planner decide exact restrictions. | |

**User's choice:** Strict boundaries.
**Notes:** Unsupported feature absence remains the contract.

## Sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Low-risk first | Start with canonical enums/helpers/DTO aliases, then shop/Dani services, then normal-play adapter internals and save-shell cleanup. | Yes |
| Runtime first | Start with Dani/shop/normal-play behavior extraction, then clean up aliases and DTO naming afterward. | |
| Single sweep | Plan one broad refactor pass over all surfaces before verification. | |

**User's choice:** Low-risk first.
**Notes:** This sequence should guide plan splitting.

## Full-Phase Proof

| Option | Description | Selected |
|--------|-------------|----------|
| Regression slices | Use existing AC15/Green/Blue/Yellow focused suites and Host build; add or adjust tests only for uncovered observable behavior, never source-shape assertions. | Yes |
| Existing only | Do not add tests unless required to fix a failing existing regression. | |
| New matrix | Add a new comprehensive AC15 reuse regression matrix across all touched modules. | |

**User's choice:** Regression slices.
**Notes:** Do not add source-shape tests or a broad reuse matrix just to prove refactoring.

## the agent's Discretion

- Exact class names, helper record names, and plan slicing are downstream discretion as long as the captured boundaries and sequencing are followed.

## Deferred Ideas

- None.
