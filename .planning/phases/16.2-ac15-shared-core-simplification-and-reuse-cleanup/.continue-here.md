---
context: phase
phase: 16.2-ac15-shared-core-simplification-and-reuse-cleanup
task: architecture-correction
total_tasks: 1
status: blocked
last_updated: 2026-06-10T19:51:43.179Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No repository-shaped AC15 persistence layer - Phase 16.2 plans 03/05/06/07 introduced `IAc15*Persistence` plus Blue/Green/Yellow adapter classes. EF already provides the repository/unit-of-work surface here, so resumption must first remove those interfaces/adapters and make shared AC15 services use `ITaikoDbContext` directly.
- [ ] CONSTRAINT: No adapter indirection for identical era code - The new Dani/item-shop/normal-play adapters differ mostly by `DbSet`/entity type while duplicating mapping and update logic. Resumption must collapse duplicated logic into real shared code, keeping only explicit era branches or hooks for proven behavior differences.
- [ ] CONSTRAINT: Mapperly is mandatory for mapping projections - The new Application-level mapper code was hand-written even though Phase 16.1 established Mapperly as the AC15 mapper direction. Resumption must use Mapperly for the Application/Ac15 value/entity projections introduced by this correction.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Repository wrapper over EF | `IAc15DaniPersistence`, `IAc15ItemShopPersistence`, and `IAc15NormalPlayPersistence` hid database access behind a redundant repository layer. This made flow harder to trace and contradicted the user's explicit architecture preference. | blocking | Execute `16.2-00-PLAN.md` first; acceptance requires no AC15 Dani/item-shop/normal-play persistence interfaces or instantiated era persistence adapters remain. |
| Adapter-shaped duplication | Blue/Green/Yellow adapter classes repeated nearly identical logic with only table/entity names changed. | blocking | Move common behavior into shared services over `ITaikoDbContext` and use small explicit era branches only where behavior truly differs. |
| Hand-written mapping after Mapperly rewrite | New adapters manually mapped EF rows to AC15 records and records back to rows instead of using Mapperly. | blocking | Add Mapperly to `Application` and create Application/Ac15 Mapperly mappers for value/entity projections touched by the correction. |

<current_state>
Phase 16.2 execution was halted after plans 01, 02, 03, 04, 05, 06, and 07 completed. Plan 11 was spawned but interrupted before edits; it reported no changes and no commits. The user reviewed the completed implementation and blocked further execution due to major architecture issues.
</current_state>

<completed_work>

Completed Phase 16.2 plans:
- 16.2-01 - canonical AC15 Dan/shop enum values.
- 16.2-02 - canonical Dan helpers and crown alias cleanup.
- 16.2-03 - profile counter updater.
- 16.2-04 - shop season seed policy.
- 16.2-05 - item-shop purchased item plumbing, but with bad persistence adapter shape.
- 16.2-06 - Dani persistence contracts/adapters, now rejected.
- 16.2-07 - Dani save workflow service, but still using rejected persistence adapters.
</completed_work>

<remaining_work>

- Execute `16.2-00-PLAN.md` before any normal Phase 16.2 continuation.
- Then resume remaining incomplete plans: 16.2-08, 16.2-09, 16.2-10, 16.2-11, 16.2-12, 16.2-13.
</remaining_work>

<decisions_made>

- User decision: do not use repository patterns for AC15 EF persistence. EF itself is the repository/unit-of-work abstraction.
- User decision: do not preserve adapter layers that only duplicate logic by era/entity name.
- User decision: use Mapperly for mapping instead of hand-written mapper projections.
- Coordinator decision: add `16.2-00-PLAN.md` as a blocking correction plan so GSD can resume safely after compaction.
</decisions_made>

<blockers>
- Architecture blocker: completed 16.2 plans introduced rejected persistence interfaces/adapters and hand-written mapping. This must be corrected before continuing the phase.
</blockers>

## Required Reading (in order)
1. `AGENTS.md` - repo architecture/testing rules and AC15 boundaries.
2. `.planning/phases/16.2-ac15-shared-core-simplification-and-reuse-cleanup/.continue-here.md` - this blocking handoff.
3. `.planning/phases/16.2-ac15-shared-core-simplification-and-reuse-cleanup/16.2-00-PLAN.md` - correction plan to execute first.
4. `.planning/phases/16.2-ac15-shared-core-simplification-and-reuse-cleanup/16.2-CONTEXT.md` - original phase decisions, now overridden where this handoff records user correction.
5. `Application/Ac15/Ac15DaniService.cs`, `Application/Ac15/Ac15ItemShopService.cs`, `Application/Ac15/Ac15NormalPlayService.cs` - current shared service surfaces.
6. `Application/Ac15/*Ac15DaniAdapter.cs`, `Application/Ac15/*Ac15ItemShopAdapter.cs`, `Application/Ac15/*Ac15NormalPlayAdapter.cs`, `Application/Ac15/IAc15*Persistence.cs` - rejected abstraction layer to remove.
7. `Application/Handlers/UpdatePlayResultCommand.*.cs`, `Application/Handlers/ItemPurchaseCommand.*.cs`, `Application/Handlers/GetDanScoreQuery.*.cs` - handler call sites that must use corrected shared services.

## Critical Anti-Patterns (do NOT repeat these)
- Hiding EF behind new repository interfaces -> services must use `ITaikoDbContext` directly.
- Creating Blue/Green/Yellow adapter classes where only the `DbSet` differs -> use shared methods and explicit era branches.
- Writing source-shape unit tests to prove the refactor -> use source audit commands as plan acceptance, but behavior tests must remain observable EF/handler behavior.

## Infrastructure State
- No dev server or watcher is intentionally running.
- `workflow.use_worktrees=false`; Codex execute-phase must run sequentially on the main checkout.
- Untracked coordinator files at pause time: `.planning/agent-history.json` and the phase `.gitkeep`.

## Pre-Execution Critique Required
- Design artifact: `.planning/phases/16.2-ac15-shared-core-simplification-and-reuse-cleanup/16.2-00-PLAN.md`
- Critique focus: verify the plan really removes repository-like persistence wrappers, materially reduces adapter duplication, and uses Mapperly for Application/Ac15 mappings.
- Gate: Do not resume normal Phase 16.2 waves until `16.2-00-SUMMARY.md` exists and the correction acceptance checks pass.

<context>
The previous coordinator was low on context and stopped ad hoc correction. The safe next move is to execute the dedicated correction plan with a fresh `gsd-executor`, then resume the original phase plan set.
</context>

<next_action>
Start with: execute `.planning/phases/16.2-ac15-shared-core-simplification-and-reuse-cleanup/16.2-00-PLAN.md` using a fresh `gsd-executor`. Do not run `16.2-08` or later first.
</next_action>
