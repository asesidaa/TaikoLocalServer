---
phase: "08-stateless-banacoin-compatibility-and-availability"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs"
  - "Tests/Blue/BlueRouteSkeletonTests.cs"
autonomous: true
requirements:
  - TKBC-01
  - TKBC-02
  - TKBC-03
requirements_addressed:
  - TKBC-01
  - TKBC-02
  - TKBC-03
must_haves:
  truths:
    - "D-01 / TKBC-01: Blue getbanacoininfo.php is added as a stateless route with Result = 1 so Banacoin-adjacent route availability cannot block Tokkun entry."
    - "D-02: Response parsing and value use are the evidence gate; 08-RESEARCH.md records IDA/client parser-use findings before implementation chooses values."
    - "D-03: 08-RESEARCH.md contains the required GetbanacoininfoResponse field-use table before the plan chooses only Result = 1."
    - "D-04: Optional GetbanacoininfoResponse fields are omitted; the new controller must not echo identity fields or populate neutral optional values."
    - "D-05: Existing heartbeat.php, balancecheck.php, banacoinpayment.php, and banacoinerrorlog.php values are preserved from trace-driven research, not from field-name guessing."
    - "D-06: BalancecheckResponse.CoinCoupon remains 0 because IDA tracing found success callback use but no zero-specific fail branch; change it only if later cabinet/RPCS3 proof identifies a valid stateless value."
    - "D-07 / TKBC-02: BnidResult and Chid stay empty/default and are never persisted; no BNID, CHID, wallet, balance, coupon, deduction, receipt, transaction, AdminApi, WebUI, EF, or Mediator state path is added."
    - "D-08: 08-RESEARCH.md records fail-map notes for each result/status field that affects success or failure."
    - "D-09: Existing per-controller request logs remain the operational visibility surface; no separate Banacoin sequence logger is added."
    - "D-10: Request logging stays within the accepted project context and introduces no unrelated secrets, API keys, passwords, tokens, or external transmission."
    - "D-11: Phase 8 makes no cabinet/RPCS3 sequence or live Tokkun proof claim; artifacts are client evidence, parser evidence, and implementation/build evidence only."
    - "D-12: Banacoin endpoints remain independent stateless routes; no sequence/order tests or sequence-dependent behavior are added."
    - "D-13: Phase 8 completion requires IDA/client parser-use evidence, valid stateless response values, route/controller implementation, and build verification."
    - "D-14: Automated verification is build-centered; route ownership test is included because the route set changes, while low-value response-constant and call-order tests are skipped."
    - "D-15: Cabinet/RPCS3 proof remains Phase 11 scope and must be stated as not run in the execution summary."
    - "D-16: Parsing/downstream use for every success-affecting Banacoin response field is traced in 08-RESEARCH.md before final response values are used."
  artifacts:
    - path: "Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs"
      provides: "Blue-owned stateless getbanacoininfo.php compatibility route"
      contains: "GetbanacoininfoResponse { Result = 1 }"
    - path: "Tests/Blue/BlueRouteSkeletonTests.cs"
      provides: "Route ownership contract updated for the new Blue getbanacoininfo.php route"
      contains: "/v10r03/chassis/getbanacoininfo.php"
  key_links:
    - from: ".planning/phases/08-stateless-banacoin-compatibility-and-availability/08-CONTEXT.md"
      to: "Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs"
      via: "D-01 through D-04 require adding the route but omitting unproven optional response fields"
      pattern: "getbanacoininfo.php|Result = 1|omit optional"
    - from: ".planning/phases/08-stateless-banacoin-compatibility-and-availability/08-RESEARCH.md"
      to: "Tests/Blue/BlueRouteSkeletonTests.cs"
      via: "Research identified the route skeleton test as the exact route ownership contract"
      pattern: "BlueRouteSkeletonTests|getbanacoininfo.php"
---

<objective>
Add Blue stateless Banacoin compatibility for `getbanacoininfo.php` and preserve the existing Banacoin-adjacent stubs without adding any wallet/payment state.

Purpose: Ensure Blue Tokkun entry is not blocked by Banacoin-adjacent route availability while keeping response values evidence-backed and side-effect free.
Output: One new Blue direct-protobuf controller plus a route ownership test update.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-stateless-banacoin-compatibility-and-availability/08-CONTEXT.md
@.planning/phases/08-stateless-banacoin-compatibility-and-availability/08-RESEARCH.md
@.planning/phases/08-stateless-banacoin-compatibility-and-availability/08-PATTERNS.md
@.planning/phases/07-tokkun-evidence-contract-and-guardrail-reset/07-01-TOKKUN-EVIDENCE-CONTRACT.md
@proto/blue/taiko.proto
@Adapters.GameProtocol.Blue/Wire/Game.cs
@Adapters.GameProtocol.Blue/Controllers/HeartbeatController.cs
@Adapters.GameProtocol.Blue/Controllers/BalanceCheckController.cs
@Adapters.GameProtocol.Blue/Controllers/BanacoinPaymentController.cs
@Adapters.GameProtocol.Blue/Controllers/BanacoinErrorLogController.cs
@Tests/Blue/BlueRouteSkeletonTests.cs
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL | Blue Tokkun entry is not blocked by Banacoin-adjacent endpoints, and no Banacoin state is stored. | Task 1 adds the missing compatibility route; Task 2 updates route ownership; Task 3 verifies stateless/no-state behavior and build. |
| REQ | TKBC-01 required Banacoin-adjacent endpoints return stateless, permissive success-shaped responses. | Task 1 returns `GetbanacoininfoResponse { Result = 1 }`; existing routes keep their success-shaped values. |
| REQ | TKBC-02 no balance, coupon, payment, deduction, transaction, CHID, BNID, or wallet-like state. | Task 1 avoids Mediator/EF/state writes; Task 3 runs source checks for forbidden stateful surfaces. |
| REQ | TKBC-03 `getbanacoininfo.php` route should be evidence-handled. | Context D-01 permits adding the route; research proves optional fields are unproven, so Task 1 adds only required `Result = 1`. |
| CONTEXT | D-01 add Blue `getbanacoininfo.php` with at least `Result = 1`. | Task 1 creates the controller. |
| CONTEXT | D-02 and D-03 response parsing/value evidence gate. | Task 1 reads `08-RESEARCH.md` and omits all optional fields; Task 3 verifies optional fields are not set. |
| CONTEXT | D-04 omit optional `getbanacoininfo.php` fields until proven. | Task 1 response contains only `Result = 1`. |
| CONTEXT | D-05 through D-08 trace-driven existing response values. | Task 1 preserves existing Banacoin-adjacent controllers unchanged; Task 3 verifies they were not broadened. |
| CONTEXT | D-09 and D-10 logging boundary. | Task 1 uses the existing per-controller `request.Stringify()` logging surface and adds no separate sequence logger. |
| CONTEXT | D-11 through D-15 no live-game claim; Phase 11 owns cabinet/RPCS3 proof. | Verification is route test plus temp-output Host build; summary must state no cabinet/RPCS3 proof was run. |
| RESEARCH | IDA found route strings for existing Banacoin routes but not for `getbanacoininfo.php`; descriptors exist only. | Plan records compatibility route addition as context-approved but keeps optional fields omitted. |
| PATTERNS | New controller should mirror stateless Blue Banacoin controllers. | Task 1 follows `BanacoinPaymentController`/`BanacoinErrorLogController` style. |

No deferred Phase 9-11 Tokkun mapper, persistence, or cabinet smoke work is planned.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add stateless Blue getbanacoininfo.php controller</name>
  <files>Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs</files>
  <read_first>
    AGENTS.md
    .planning/phases/08-stateless-banacoin-compatibility-and-availability/08-CONTEXT.md
    .planning/phases/08-stateless-banacoin-compatibility-and-availability/08-RESEARCH.md
    .planning/phases/08-stateless-banacoin-compatibility-and-availability/08-PATTERNS.md
    Adapters.GameProtocol.Blue/Controllers/BanacoinPaymentController.cs
    Adapters.GameProtocol.Blue/Controllers/BanacoinErrorLogController.cs
    Adapters.GameProtocol.Blue/Controllers/BalanceCheckController.cs
    Adapters.GameProtocol.Blue/Wire/Game.cs
  </read_first>
  <action>Create `Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs` using the existing Blue stateless direct-protobuf controller pattern. The controller must be in namespace `TaikoLocalServer.Adapters.GameProtocol.Blue.Controllers`, inherit `BaseProtocolController<GetBanacoinInfoController>`, use `[ApiController]`, route exactly `[Route("/v10r03/chassis/getbanacoininfo.php")]`, expose one `[HttpPost]` action with `[Produces("application/protobuf")]`, accept `[FromBody] GetbanacoininfoRequest request`, log `Logger.LogInformation("Blue GetBanacoinInfo request: {Request}", request.Stringify());`, and return `Ok(new GetbanacoininfoResponse { Result = 1 });`.

Do not set `PlayerType`, `ComSvrResult`, `MbId`, `Baid`, `AccessCode`, `IsPublish`, `CardOwnNum`, `RegCountryId`, `PurposeId`, `RegionId`, or `Personid`. `08-RESEARCH.md` found only descriptors for those optional fields and no route-specific response consumer. Do not echo request identity fields. Do not modify existing `HeartbeatController`, `BalanceCheckController`, `BanacoinPaymentController`, or `BanacoinErrorLogController` unless a compile error requires a mechanical namespace/import adjustment, which is not expected. Do not add Mediator, EF, `ITaikoDbContext`, migrations, options, AdminApi, WebUI, or persistence code.</action>
  <verify>
    <automated>$path = 'Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs'; if (-not (Test-Path $path)) { throw 'GetBanacoinInfoController.cs missing' }; $source = Get-Content $path -Raw; foreach ($required in @('/v10r03/chassis/getbanacoininfo.php','GetbanacoininfoRequest','GetbanacoininfoResponse','Result = 1','request.Stringify()')) { if ($source -notmatch [regex]::Escape($required)) { throw "Missing expected controller content: $required" } }; foreach ($forbidden in @('PlayerType','ComSvrResult','MbId','Baid','AccessCode','IsPublish','CardOwnNum','RegCountryId','PurposeId','RegionId','Personid =','Mediator.Send','ITaikoDbContext','DbContext','SaveChanges')) { if ($source -match [regex]::Escape($forbidden)) { throw "Forbidden getbanacoininfo controller content: $forbidden" } }</automated>
  </verify>
  <acceptance_criteria>
    - The route exists at `/v10r03/chassis/getbanacoininfo.php`.
    - The response serializes only required `Result = 1`.
    - The controller logs through the existing request log pattern.
    - No optional identity/account/payment fields are set.
    - No stateful dependency or cross-era abstraction is introduced.
  </acceptance_criteria>
  <done>The new route is a Blue-owned stateless compatibility stub with no persistence or invented optional response semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Update Blue route ownership tests</name>
  <files>Tests/Blue/BlueRouteSkeletonTests.cs</files>
  <read_first>
    Tests/Blue/BlueRouteSkeletonTests.cs
    .planning/phases/08-stateless-banacoin-compatibility-and-availability/08-CONTEXT.md
    .planning/phases/08-stateless-banacoin-compatibility-and-availability/08-RESEARCH.md
  </read_first>
  <action>Update `Tests/Blue/BlueRouteSkeletonTests.cs` so `BlueGameRoutes_AreOwnedByBlueAdapter` expects `/v10r03/chassis/getbanacoininfo.php` in `ExpectedBlueGameRoutes`. Remove `[InlineData("/v10r03/chassis/getbanacoininfo.php")]` from `BlueAdapter_DoesNotOwnExcludedOrSharedRoutes`, because Phase 8 now owns the route. Preserve exclusions for `getreitai.php`, `startupauth.php`, `verupauth.php`, and `verupcomplete.php`. Do not add the new controller to `mediatorBackedControllers`; the existing `BlueControllers_DoNotCallMediatorOutsideImplementedEndpoints` guard should continue to prove the Banacoin route is stateless.</action>
  <verify>
    <automated>$source = Get-Content 'Tests/Blue/BlueRouteSkeletonTests.cs' -Raw; if ($source -notmatch [regex]::Escape('/v10r03/chassis/getbanacoininfo.php')) { throw 'Expected route missing from route skeleton tests' }; if ($source -match '(?s)BlueAdapter_DoesNotOwnExcludedOrSharedRoutes.*getbanacoininfo\.php') { throw 'getbanacoininfo.php still listed as excluded route' }; if ($source -match 'mediatorBackedControllers[\s\S]*GetBanacoinInfoController\.cs') { throw 'GetBanacoinInfoController must not be mediator backed' }</automated>
  </verify>
  <acceptance_criteria>
    - The exact Blue route set includes `getbanacoininfo.php`.
    - The excluded/shared route theory no longer excludes `getbanacoininfo.php`.
    - The no-Mediator guard still applies to the new controller.
  </acceptance_criteria>
  <done>The route ownership contract matches Phase 8 Blue compatibility scope.</done>
</task>

<task type="auto">
  <name>Task 3: Verify statelessness and build</name>
  <files>Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs, Tests/Blue/BlueRouteSkeletonTests.cs</files>
  <read_first>
    .planning/phases/08-stateless-banacoin-compatibility-and-availability/08-CONTEXT.md
    .planning/phases/08-stateless-banacoin-compatibility-and-availability/08-RESEARCH.md
    Tests/Blue/BlueRouteSkeletonTests.cs
  </read_first>
  <action>Run focused verification for the route and statelessness contract. First run source checks that no Banacoin runtime state was added: search the new controller and the existing Blue Banacoin controllers for `Mediator.Send`, `ITaikoDbContext`, `DbContext`, `SaveChanges`, `Migration`, `DbSet`, `Balance`, `Coupon`, `Deduction`, `Transaction`, `Receipt`, and new `Chid`/`Bnid` persistence-style assignments. Then run the focused route test and a temp-output Host build. Use the temp output path because a running server can lock `Host/bin/Debug/net10.0`.

Record in the summary that Phase 8 did not run cabinet/RPCS3 and makes no live Tokkun proof claim. Phase 11 owns cabinet/RPCS3 proof. If the focused route test or build fails because of unrelated locked build outputs, rerun sequentially once before escalating.</action>
  <verify>
    <automated>$files = @('Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs','Adapters.GameProtocol.Blue/Controllers/HeartbeatController.cs','Adapters.GameProtocol.Blue/Controllers/BalanceCheckController.cs','Adapters.GameProtocol.Blue/Controllers/BanacoinPaymentController.cs','Adapters.GameProtocol.Blue/Controllers/BanacoinErrorLogController.cs'); foreach ($file in $files) { $source = Get-Content $file -Raw; foreach ($forbidden in @('Mediator.Send','ITaikoDbContext','DbContext','SaveChanges','Migration','DbSet','TransactionHistory','Wallet','Deduction','Receipt')) { if ($source -match [regex]::Escape($forbidden)) { throw "Stateful Banacoin surface detected in $file: $forbidden" } } }</automated>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueRouteSkeletonTests</automated>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build-phase8"</automated>
  </verify>
  <acceptance_criteria>
    - Focused source checks find no stateful Banacoin code.
    - `BlueRouteSkeletonTests` passes.
    - Temp-output Host build passes.
    - Summary explicitly states no cabinet/RPCS3 verification was performed in Phase 8.
  </acceptance_criteria>
  <done>Route behavior, statelessness, and compile wiring are verified without claiming live game proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cabinet/client request -> local server | Blue cabinet requests are accepted through unauthenticated local protocol routes and must not create payment authority state. |
| Wire DTO -> controller response | Generated Blue wire DTOs include optional identity/account-looking fields that must not be populated from names alone. |
| Route compatibility -> persistence | A compatibility route may be needed for Tokkun entry, but it must not cross into EF, shared identity, AdminApi, WebUI, or shop/medal state. |
| Planning evidence -> implementation | IDA descriptors and route strings are evidence, but only proven parser/fail branches may drive response values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `GetbanacoininfoResponse` optional fields | mitigate | Return only `Result = 1`; source check forbids optional field assignments. |
| T-08-02 | Tampering | Banacoin wallet/payment state | mitigate | No EF/Mediator/AdminApi/WebUI work; source checks reject stateful symbols. |
| T-08-03 | Spoofing | Request identity fields (`access_code`, `chip_id`, `personid`) | mitigate | Log request through existing surface but do not echo or persist identity fields in `getbanacoininfo.php`. |
| T-08-04 | Repudiation | Banacoin route observability | mitigate | Use existing per-controller `request.Stringify()` logs; no separate sequence logger or state ledger. |
| T-08-05 | Information Disclosure | Banacoin request logs | accept | Phase 8 context D-10 accepts raw client/request excerpts in this project context, but implementation must not introduce unrelated secrets or external transmission. |
| T-08-06 | Denial of Service | Route absence blocking Tokkun entry | mitigate | Add stateless `getbanacoininfo.php` route despite unproven route string, per Phase 8 context D-01. |
| T-08-SC | Supply Chain | Packages/dependencies | accept | No package install, dependency update, code generator, or external service is planned. |
</threat_model>

<verification>
Run focused verification:

- `$path = 'Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs'; if (-not (Test-Path $path)) { throw 'GetBanacoinInfoController.cs missing' }; $source = Get-Content $path -Raw; foreach ($required in @('/v10r03/chassis/getbanacoininfo.php','GetbanacoininfoRequest','GetbanacoininfoResponse','Result = 1','request.Stringify()')) { if ($source -notmatch [regex]::Escape($required)) { throw "Missing expected controller content: $required" } }; foreach ($forbidden in @('PlayerType','ComSvrResult','MbId','Baid','AccessCode','IsPublish','CardOwnNum','RegCountryId','PurposeId','RegionId','Personid =','Mediator.Send','ITaikoDbContext','DbContext','SaveChanges')) { if ($source -match [regex]::Escape($forbidden)) { throw "Forbidden getbanacoininfo controller content: $forbidden" } }`
- `$source = Get-Content 'Tests/Blue/BlueRouteSkeletonTests.cs' -Raw; if ($source -notmatch [regex]::Escape('/v10r03/chassis/getbanacoininfo.php')) { throw 'Expected route missing from route skeleton tests' }; if ($source -match '(?s)BlueAdapter_DoesNotOwnExcludedOrSharedRoutes.*getbanacoininfo\.php') { throw 'getbanacoininfo.php still listed as excluded route' }`
- `$files = @('Adapters.GameProtocol.Blue/Controllers/GetBanacoinInfoController.cs','Adapters.GameProtocol.Blue/Controllers/HeartbeatController.cs','Adapters.GameProtocol.Blue/Controllers/BalanceCheckController.cs','Adapters.GameProtocol.Blue/Controllers/BanacoinPaymentController.cs','Adapters.GameProtocol.Blue/Controllers/BanacoinErrorLogController.cs'); foreach ($file in $files) { $source = Get-Content $file -Raw; foreach ($forbidden in @('Mediator.Send','ITaikoDbContext','DbContext','SaveChanges','Migration','DbSet','TransactionHistory','Wallet','Deduction','Receipt')) { if ($source -match [regex]::Escape($forbidden)) { throw "Stateful Banacoin surface detected in $file: $forbidden" } } }`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueRouteSkeletonTests`
- `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build-phase8"`

Optional if local outputs are not locked:

- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~Blue`
</verification>

<success_criteria>
- TKBC-01: Blue has stateless success-shaped responses for the Banacoin-adjacent route surface, including `getbanacoininfo.php`.
- TKBC-02: No Blue, Green, Nijiiro, or shared wallet-like balance, payment, coupon, deduction, CHID, BNID, receipt, transaction, AdminApi, WebUI, EF, or Mediator state path is added.
- TKBC-03: `getbanacoininfo.php` is present as a context-approved compatibility route, while all unproven optional fields remain omitted and the research artifact records the IDA descriptor-only evidence.
- Existing Banacoin-adjacent controllers remain stateless and preserve their current response values.
- Focused route tests and temp-output Host build pass.
- Phase 8 summary states that live cabinet/RPCS3 Tokkun proof was not run and remains Phase 11 scope.
</success_criteria>

<output>
Create `.planning/phases/08-stateless-banacoin-compatibility-and-availability/08-01-SUMMARY.md` when execution completes.
</output>
