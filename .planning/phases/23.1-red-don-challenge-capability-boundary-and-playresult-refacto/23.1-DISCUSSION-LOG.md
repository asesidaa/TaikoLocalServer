# Phase 23.1: Red Don Challenge capability boundary and playresult refactor - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 23.1-red-don-challenge-capability-boundary-and-playresult-refactor
**Areas discussed:** Boundary sweep

---

## Boundary Sweep

| Question | Option | Description | Selected |
|----------|--------|-------------|----------|
| Which gray areas should be discussed? | Boundary sweep | Discuss naming, writer scope, config compatibility, AdminApi projection, and Tokkun dispatch in one focused pass. | yes |
| Which gray areas should be discussed? | Naming only | Lock only the Don Challenge versus ChallengeCompe naming boundary, then let planning infer the rest. | |
| Which gray areas should be discussed? | Writer/dispatch only | Focus on playresult mutation, shared writer boundaries, and Tokkun dispatch cleanup. | |

**User's choice:** Boundary sweep.
**Notes:** The user wanted the prior todo followed through discuss, plan, and execute, so the pending todo was folded into this inserted phase.

---

## Naming

| Option | Description | Selected |
|--------|-------------|----------|
| Domain Rename | Rename shared Application/Ac15 ChallengeCompe model/evaluator/reward code to DonChallenge while preserving wire/route ChallengeCompe names. | yes |
| Wrapper Alias | Add DonChallenge-facing wrappers but leave most shared internals named ChallengeCompe. | |
| Document Only | Keep current names and clarify the boundary in comments/docs only. | |

**User's choice:** Domain Rename.
**Notes:** This locks Don Challenge as the shared stateful capability name and reserves ChallengeCompe for real protocol route/wire surfaces.

---

## Config And Schema Compatibility

| Option | Description | Selected |
|--------|-------------|----------|
| Alias, No Migration | Add DonChallenge-facing config/catalog aliases while preserving existing settings keys and Red table names for compatibility. | |
| Config Rename | Rename settings/catalog names now but leave EF table names as-is. | |
| Schema Rename | Rename EF entities/tables too, accepting migration and compatibility churn. | yes |

**User's choice:** Schema Rename.
**Notes:** A follow-up confirmed the migration must preserve existing Red challenge progress and raw facts.

---

## Schema Migration Bar

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve Data | Use data-preserving EF migrations/table renames from RedChallengeCompe* to RedDonChallenge* and keep existing progress. | yes |
| Fresh Schema | Allow dropping/recreating challenge tables because this is local/dev-only state. | |
| Code Names Only | Rename entities/classes but map them to old table names to avoid DB migration churn. | |

**User's choice:** Preserve Data.
**Notes:** The plan must not drop/recreate challenge state.

---

## Shared Writer

| Option | Description | Selected |
|--------|-------------|----------|
| Shared Writer | Create an Ac15DonChallengeWriter that owns raw facts, progress, and rewards through explicit era bindings. | yes |
| Evaluator Only | Rename/share the evaluator but keep Red handler responsible for persistence and rewards. | |
| Broad Service | Create a wider generic service that hides more persistence and handler details. | |

**User's choice:** Shared Writer.
**Notes:** The writer should be explicit and era-bound, not a repository-shaped persistence layer.

---

## AdminApi Projection

| Option | Description | Selected |
|--------|-------------|----------|
| Mapper Helpers | Move mechanical task/reward DTO building out of GetDonChallengeQueryHandler into Mapperly or focused projection helpers. | yes |
| Keep Handler | Leave DTO construction in the query handler and only move mutation code. | |
| Wait For White | Defer AdminApi projection cleanup until White needs a Don Challenge readback surface. | |

**User's choice:** Mapper Helpers.
**Notes:** This keeps the handler focused on loading era/capability data and calling projection.

---

## Tokkun Dispatch

| Option | Description | Selected |
|--------|-------------|----------|
| Shared Classifier | Centralize Tokkun classification/early dispatch policy while keeping era save, recent-song, and history hooks explicit. | yes |
| Leave Dispatch | Keep Blue/Yellow/Red handler dispatch as-is; only preserve current behavior with tests. | |
| Full Pipeline | Create a broader shared AC15 playresult pipeline around Tokkun, Dani, Don Challenge, and normal play ordering. | |

**User's choice:** Shared Classifier.
**Notes:** The cleanup is bounded to Tokkun classification and dispatch policy, not a full playresult pipeline.

---

## the agent's Discretion

None.

## Deferred Ideas

- White Don Challenge runtime/readback binding remains deferred to Phase 26 and requires White-specific evidence.
- Community, user-created, BNG/official, schedule-management, operator-authored, and global challenge simulation behavior remains deferred until runtime evidence proves those contracts.
- A broad shared AC15 playresult pipeline is out of scope for this phase.
