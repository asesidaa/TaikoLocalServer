---
phase: 23-white-evidence-and-era-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "23-01"
  - "23-02"
files_modified:
  - "Adapters.GameProtocol.White/Controllers/WhiteScaffoldControllers.cs"
  - "Host/Program.cs"
  - "Host/Host.csproj"
  - "Host/Configurations/ServerSettings.json"
  - "Tests/White/WhiteServerSettingsValidationTests.cs"
  - "Tests/White/WhiteHostRouteGatingTests.cs"
autonomous: true
requirements:
  - WFND-02
  - WFND-03
user_setup: []
must_haves:
  truths:
    - "WFND-02/D-06: White route controllers are thin no-state scaffolds for evidence-approved suffixes only."
    - "WFND-02/D-07: Missing-content-type protobuf fallback adds only exact /v07r00/chassis after Plan 01 route-prefix proof."
    - "WFND-02/D-08: Host settings, registration, application-part gating, and data-root handling stop at foundation work."
    - "WFND-03: Disabled White routes are absent and existing Blue/Green/Yellow/Red/Nijiiro/shared route behavior remains preserved."
  artifacts:
    - path: "Adapters.GameProtocol.White/Controllers/WhiteScaffoldControllers.cs"
      provides: "Evidence-backed no-state White route scaffolds"
    - path: "Host/Program.cs"
      provides: "Enabled-era registration, disabled application-part removal, exact fallback"
    - path: "Host/Configurations/ServerSettings.json"
      provides: "White era settings block"
    - path: "Tests/White/WhiteServerSettingsValidationTests.cs"
      provides: "White settings boundary regression"
    - path: "Tests/White/WhiteHostRouteGatingTests.cs"
      provides: "Behavior-facing Host route gating and fallback preservation checks"
  key_links:
    - from: ".planning/phases/23-white-evidence-and-era-foundation/23-WHITE-EVIDENCE.md"
      to: "Adapters.GameProtocol.White/Controllers/WhiteScaffoldControllers.cs"
      via: "SCAFFOLD_APPROVED route suffix table"
      pattern: "SCAFFOLD_APPROVED"
    - from: ".planning/phases/23-white-evidence-and-era-foundation/23-WHITE-EVIDENCE.md"
      to: "Host/Program.cs"
      via: "/v07r00/chassis prefix proof"
      pattern: "/v07r00/chassis"
---

<objective>
Wire White into Host and add evidence-backed no-state route scaffolds while preserving disabled-era safety and existing supported-era behavior.

Purpose: WFND-02 needs White Host settings, DI registration, application-part gating, exact content-type fallback, and route ownership. WFND-03 requires preservation checks for shared Host/settings changes.

Output: White no-state scaffold controllers only for Plan 01-approved route suffixes, Host registration/gating/fallback, White settings, Host project data-root handling, and focused tests/checks.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/23-white-evidence-and-era-foundation/23-CONTEXT.md
@.planning/phases/23-white-evidence-and-era-foundation/23-VALIDATION.md
@.planning/phases/23-white-evidence-and-era-foundation/23-PATTERNS.md
@.planning/phases/23-white-evidence-and-era-foundation/23-WHITE-EVIDENCE.md
@Adapters.GameProtocol.Red/Controllers/BookkeepingController.cs
@Adapters.GameProtocol.Red/Controllers/HeartbeatController.cs
@Adapters.GameProtocol.Red/Controllers/RewardExecutionController.cs
@Adapters.GameProtocol.Shared/Controllers/StartupAuthController.cs
@Adapters.GameProtocol.Shared/Controllers/VerupAuthController.cs
@Adapters.GameProtocol.Shared/Controllers/VerupCompleteController.cs
@Host/Program.cs
@Host/Host.csproj
@Host/Configurations/ServerSettings.json
@Application/Settings/ServerSettingsOptionsValidationExtensions.cs
@Tests/Red/RedServerSettingsValidationTests.cs
@Tests/Yellow/YellowServerSettingsValidationTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add White Settings And Route-Gating Preservation Tests</name>
  <files>Tests/White/WhiteServerSettingsValidationTests.cs, Tests/White/WhiteHostRouteGatingTests.cs</files>
  <read_first>
    .planning/phases/23-white-evidence-and-era-foundation/23-VALIDATION.md
    .planning/phases/23-white-evidence-and-era-foundation/23-WHITE-EVIDENCE.md
    Tests/Red/RedServerSettingsValidationTests.cs
    Tests/Yellow/YellowServerSettingsValidationTests.cs
    Host/Program.cs
    Application/Settings/ServerSettingsOptionsValidationExtensions.cs
  </read_first>
  <behavior>
    - White enabled settings do not require `EnableShop`, `ActiveShopSeasonId`, `EnableChallengeCompe`, or `ActiveChallengeCompeBundleId` in Phase 23.
    - White route feature exposure is controlled by enabled-era application-part filtering, not merely by Host referencing the adapter assembly.
    - Exact protobuf fallback remains limited to known prefixes and, after Plan 01 proof, `/v07r00/chassis`; unrelated `/v07r00` paths and unsupported White surfaces are not broadened.
  </behavior>
  <action>
    Create `WhiteServerSettingsValidationTests` by adapting the Red settings validation pattern for `GameEra.White`. Create `WhiteHostRouteGatingTests` using existing xUnit and ASP.NET Core MVC application-part types already available through the shared framework references; do not add `Microsoft.AspNetCore.Mvc.Testing`, TestHost, Moq, or any new package. The route-gating test must be behavior-facing: it should exercise controller feature/application-part exposure or an equivalent route-feature surface after adding/removing the White adapter assembly, not assert controller attribute lists, source text, project-file strings, or enum numeric values. Tests should fail before production edits and pass after Task 2 and Task 3.
  </action>
  <acceptance_criteria>
    The tests protect the Wave 0 validation obligations from `23-VALIDATION.md` without checking generated type/property existence, source text, controller attribute inventories, DI registration shape, or superficial `Result = 1` echoes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~WhiteServerSettingsValidationTests|FullyQualifiedName~WhiteHostRouteGatingTests" -x</automated>
  </verify>
  <done>White settings and Host route-gating tests exist and encode the Phase 23 preservation boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add Evidence-Backed No-State White Route Scaffolds</name>
  <files>Adapters.GameProtocol.White/Controllers/WhiteScaffoldControllers.cs</files>
  <read_first>
    .planning/phases/23-white-evidence-and-era-foundation/23-WHITE-EVIDENCE.md
    .planning/phases/23-white-evidence-and-era-foundation/23-WHITE-FEATURE-INVENTORY.md
    Adapters.GameProtocol.White/Wire/Game.cs
    Adapters.GameProtocol.Red/Controllers/BookkeepingController.cs
    Adapters.GameProtocol.Red/Controllers/HeartbeatController.cs
    Adapters.GameProtocol.Red/Controllers/RewardExecutionController.cs
    Adapters.GameProtocol.Shared/Controllers/BaseProtocolController.cs
  </read_first>
  <action>
    If `23-WHITE-EVIDENCE.md` does not mark `/v07r00/chassis` and at least one route suffix `SCAFFOLD_APPROVED`, stop this plan as blocked and do not create controller routes. Otherwise, create no-state White controllers only for the approved suffixes. Each approved route must use exactly `/v07r00/chassis/{suffix}.php`, direct White wire request/response DTOs, `[Produces("application/protobuf")]`, and safe success/default response fields that are justified by the evidence artifact. Implement D-06 by keeping controllers thin: deserialize, log bounded scalar request context such as route suffix, Baid, ChassisId, ShopId, or request type where those fields exist, and return a generated White response. Do not log full request objects or repeated arrays. Do not call Mediator, add Application handlers, read catalogs, add EF writes, mutate profile/state, infer reward/ChallengeCompe behavior, or duplicate shared `/v01r00/chassis` startup/version controllers.
  </action>
  <acceptance_criteria>
    White scaffold controllers contain only evidence-approved route suffixes, bounded logging, and no runtime behavior. Shared startup/version remains in `Adapters.GameProtocol.Shared`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Mediator|ITaikoDbContext|DbContext|SaveChanges|GetRequiredService&lt;IGameDataCatalog&gt;|ChallengeCompe|Tokkun|Battle|ItemShop|Banacoin|GameEra\\.Red|GameEra\\.Yellow" Adapters.GameProtocol.White/Controllers</automated>
    <automated>dotnet build Adapters.GameProtocol.White/Adapters.GameProtocol.White.csproj</automated>
  </verify>
  <done>Evidence-backed White no-state route scaffolds compile and do not add runtime state or unsupported surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Host Settings, Registration, Fallback, And Data Root Handling</name>
  <files>Host/Program.cs, Host/Host.csproj, Host/Configurations/ServerSettings.json</files>
  <read_first>
    .planning/phases/23-white-evidence-and-era-foundation/23-WHITE-EVIDENCE.md
    Host/Program.cs
    Host/Host.csproj
    Host/Configurations/ServerSettings.json
    Adapters.GameProtocol.White/DependencyInjection.cs
    Tests/White/WhiteServerSettingsValidationTests.cs
    Tests/White/WhiteHostRouteGatingTests.cs
  </read_first>
  <action>
    Add `using TaikoLocalServer.Adapters.GameProtocol.White` to `Host/Program.cs`, update the no-enabled-era fatal message to include White, register `AddGameProtocolWhite()` only when `enabledEras.Contains(GameEra.White)`, and remove `TaikoLocalServer.Adapters.GameProtocol.White` from MVC application parts when White is disabled. Implement D-07 only if Plan 01 proved `/v07r00/chassis`: add exactly `path.StartsWithSegments("/v07r00/chassis", StringComparison.OrdinalIgnoreCase)` to `ShouldAssumeProtobufRequest`; do not add `/v07r00` broadly or a generic AC15 predicate. Add the White adapter project reference to `Host/Host.csproj`, exclude `wwwroot\data\white\data\**` from content/publish, and add a `CreateWhiteGameDataSymlinkForDebug` target mirroring Red. Add a `White` block to `Host/Configurations/ServerSettings.json` with `Enabled` false, `AutoExtractCatalog` true, `GameDataPath` `wwwroot/data/white/data`, empty `CustomizationNameDataPath`, and no shop/challenge settings. Do not add Infrastructure White catalog registration, AC15 profile, runtime persistence, AdminApi/WebUI, or server-authored White sidecars in Phase 23.
  </action>
  <acceptance_criteria>
    White Host wiring is explicit, disabled-safe, exact-prefix scoped, and foundation-only. Existing Green/Blue/Yellow/Red/Nijiiro/shared prefixes remain present and unchanged except for additive White entries.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~WhiteServerSettingsValidationTests|FullyQualifiedName~WhiteHostRouteGatingTests|FullyQualifiedName~RedServerSettingsValidationTests|FullyQualifiedName~YellowServerSettingsValidationTests" -x</automated>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~StartupAuthController" -x</automated>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"</automated>
  </verify>
  <done>Host compiles with White adapter references, White remains disabled-safe, and shared startup/version and existing-era settings checks still pass.</done>
</task>

</tasks>

<must_haves>
- Plan 03 must not execute route scaffolding unless Plan 01 evidence marks route prefix and suffixes approved.
- D-06 no-state controllers have bounded logs and no Mediator/EF/catalog/runtime behavior.
- D-07 fallback is exact `/v07r00/chassis` only.
- D-08 Host wiring is foundation-only; catalog/profile/runtime work remains outside Phase 23.
- WFND-03 preservation checks cover existing settings/startup behavior and Host build output.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cabinet HTTP -> White controllers | Untrusted protobuf bodies hit no-state route scaffolds. |
| Host config -> MVC application parts | Enabled-era settings decide whether White controllers are routable. |
| missing content type -> protobuf formatter | Host middleware mutates request content type for exact protocol prefixes. |
| local operator data -> build output | Host project rules decide whether operator White data is copied or debug-linked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | Disabled White adapter routable because Host references assembly | mitigate | Remove `TaikoLocalServer.Adapters.GameProtocol.White` application part when `GameEra.White` is disabled and verify route-feature exposure. |
| T-23-02 | Spoofing/Tampering | Missing-content-type protobuf fallback | mitigate | Add only exact `/v07r00/chassis` after Plan 01 proof; test unrelated paths do not broaden. |
| T-23-03 | Information Disclosure | No-state scaffold request logging | mitigate | Controllers log bounded scalar identifiers and omit full request objects/repeated payloads. |
| T-23-04 | Tampering | Generated wire or proto files manually edited | mitigate | This plan does not edit `proto/white` or generated wire; final phase verification still runs `git status --porcelain -- proto/white`. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned. |
</threat_model>

<verification>
Run after this plan:
- `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~WhiteServerSettingsValidationTests|FullyQualifiedName~WhiteHostRouteGatingTests|FullyQualifiedName~RedServerSettingsValidationTests|FullyQualifiedName~YellowServerSettingsValidationTests" -x`
- `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~StartupAuthController" -x`
- `dotnet test Tests/Tests.csproj`
- `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"`
- `git status --porcelain -- proto/white`
- Mapperly generated-source inspection is required only if nontrivial White mappers are added despite this plan's no-mapper scope: `dotnet build /p:EmitCompilerGeneratedFiles=true`, then inspect `Adapters.GameProtocol.White/obj/.../generated/.../Riok.Mapperly/*.g.cs`.
</verification>

<success_criteria>
Plan 03 is complete when White route scaffolds are evidence-backed and no-state, Host exposes White only when enabled, disabled White routes remain absent, the protobuf fallback is exact-prefix scoped, and existing-era/shared startup behavior still passes focused checks plus Host build.
</success_criteria>

<output>
Create `.planning/phases/23-white-evidence-and-era-foundation/23-03-SUMMARY.md` when done.
</output>
