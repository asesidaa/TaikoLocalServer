---
phase: 12-yellow-evidence-and-era-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - Adapters.GameProtocol.Yellow/Controllers/*.cs
  - Host/Host.csproj
  - Host/Program.cs
  - Host/Configurations/ServerSettings.json
  - Tests/Yellow/YellowRouteSkeletonTests.cs
  - Tests/Yellow/YellowHostProgramSourceTests.cs
autonomous: false
requirements:
  - YFND-02
  - YFND-03
requirements_addressed:
  - YFND-02
  - YFND-03
user_setup: []
must_haves:
  truths:
    - "D-02/D-03: Yellow game routes are owned by the Yellow adapter and shared startup/version routes remain owned by `Adapters.GameProtocol.Shared`."
    - "D-04/D-06: A human-provided or local evidence-backed Yellow game route prefix is recorded before concrete Yellow controller routes and Host protobuf fallback are hardcoded."
    - "D-09/D-10/D-12: Yellow Phase 12 controllers are thin direct-protobuf no-state scaffolds using existing Blue/Green/shared controller patterns."
    - "D-11/YFND-03: Host registration and application-part filtering expose Yellow routes only when `ServerSettings:Eras:Yellow:Enabled` is true."
    - "YFND-02/YFND-03: Focused route/source/build tests prove Yellow adapter ownership, Host registration, settings parseability, and disabled-route filtering guardrails."
  artifacts:
    - path: "Adapters.GameProtocol.Yellow/Controllers"
      provides: "Yellow direct-protobuf game route controllers"
      contains: "Controller"
    - path: "Host/Program.cs"
      provides: "Yellow Host DI, application-part filtering, and scoped protobuf fallback"
      contains: "AddGameProtocolYellow"
    - path: "Host/Host.csproj"
      provides: "Host reference to Yellow adapter and Yellow data-root handling"
      contains: "Adapters.GameProtocol.Yellow"
    - path: "Host/Configurations/ServerSettings.json"
      provides: "Parseable Yellow era settings block"
      contains: "Yellow"
    - path: "Tests/Yellow/YellowRouteSkeletonTests.cs"
      provides: "Yellow route ownership and thin-controller guard"
      contains: "YellowRouteSkeletonTests"
    - path: "Tests/Yellow/YellowHostProgramSourceTests.cs"
      provides: "Host registration, disabled filtering, and protobuf fallback guard"
      contains: "YellowHostProgramSourceTests"
  key_links:
    - from: "Host/Program.cs"
      to: "Adapters.GameProtocol.Yellow/DependencyInjection.cs"
      via: "enabledEras.Contains(GameEra.Yellow) calls AddGameProtocolYellow"
      pattern: "AddGameProtocolYellow"
    - from: "Host/Program.cs"
      to: "Adapters.GameProtocol.Yellow"
      via: "RemoveApplicationPart removes Yellow adapter assembly when disabled"
      pattern: "TaikoLocalServer.Adapters.GameProtocol.Yellow"
    - from: "Tests/Yellow/YellowRouteSkeletonTests.cs"
      to: "Adapters.GameProtocol.Yellow/Controllers"
      via: "ProtocolRouteTestHelper reflects Yellow controller route attributes"
      pattern: "FindPostRoutes"
---

<objective>
Wire Yellow route scaffolding into Host behind an evidence-backed route prefix.

Purpose: Once the exact Yellow game route prefix is supplied, Phase 12 must expose Yellow-owned direct-protobuf game route scaffolds only when Yellow is enabled and keep disabled Yellow routes absent from route discovery.
Output: Yellow no-state controller scaffolds for proto-backed supported endpoints, Host/project/settings wiring, and focused route/Host tests covering YFND-02 and YFND-03 while honoring D-02, D-03, D-04, D-06, D-09, D-10, D-11, and D-12.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-yellow-evidence-and-era-foundation/12-CONTEXT.md
@.planning/phases/12-yellow-evidence-and-era-foundation/12-RESEARCH.md
@.planning/phases/12-yellow-evidence-and-era-foundation/12-VALIDATION.md
@.planning/phases/12-yellow-evidence-and-era-foundation/12-PATTERNS.md
@.planning/phases/12-yellow-evidence-and-era-foundation/12-YELLOW-EVIDENCE.md
@Adapters.GameProtocol.Yellow/Adapters.GameProtocol.Yellow.csproj
@Adapters.GameProtocol.Yellow/DependencyInjection.cs
@Adapters.GameProtocol.Yellow/Wire/Game.cs
@Adapters.GameProtocol.Blue/Controllers/BookkeepingController.cs
@Adapters.GameProtocol.Blue/Controllers/HeartbeatController.cs
@Adapters.GameProtocol.Blue/Controllers/InitialDataCheckController.cs
@Adapters.GameProtocol.Blue/Controllers/PlayResultController.cs
@Adapters.GameProtocol.Blue/Controllers/BattleUserDataController.cs
@Tests/Blue/BlueRouteSkeletonTests.cs
@Tests/Blue/ProtocolRouteTestHelper.cs
@Tests/Blue/BlueHostProgramSourceTests.cs
@Host/Program.cs
@Host/Host.csproj
@Host/Configurations/ServerSettings.json
@proto/yellow/yellow.proto
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm Yellow game route prefix evidence</name>
  <files>.planning/phases/12-yellow-evidence-and-era-foundation/12-YELLOW-EVIDENCE.md</files>
  <action>Before editing controller route attributes or Host protobuf fallback, obtain the exact Yellow game route prefix from concrete local evidence or an explicit user decision. Acceptable evidence is a cabinet/RPCS3 HTTP log, Yellow client binary string scan, existing local route artifact, or the user's explicit statement naming the exact prefix. Record the chosen prefix and evidence source in `12-YELLOW-EVIDENCE.md` under the route-prefix gap row. If no prefix is supplied, stop this plan and leave Phase 12 with Plan 12-01 complete only; do not guess from Blue `/v10r03`, Green `/v11r01`, `ST9100-1`, or public wiki text.</action>
  <verify>
    <automated>$evidence = Get-Content '.planning/phases/12-yellow-evidence-and-era-foundation/12-YELLOW-EVIDENCE.md' -Raw; if ($evidence -match 'Exact Yellow game route prefix.*UNRESOLVED') { throw 'Yellow game route prefix remains unresolved; do not create concrete route attributes yet.' }</automated>
  </verify>
  <done>The evidence artifact names the exact Yellow game route prefix and cites the local/user-approved evidence source; execution can continue to concrete controller and Host route work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Yellow no-state game route controllers and route tests</name>
  <files>Adapters.GameProtocol.Yellow/Controllers/*.cs, Tests/Yellow/YellowRouteSkeletonTests.cs</files>
  <read_first>
    .planning/phases/12-yellow-evidence-and-era-foundation/12-CONTEXT.md
    .planning/phases/12-yellow-evidence-and-era-foundation/12-RESEARCH.md
    .planning/phases/12-yellow-evidence-and-era-foundation/12-PATTERNS.md
    .planning/phases/12-yellow-evidence-and-era-foundation/12-YELLOW-EVIDENCE.md
    Adapters.GameProtocol.Yellow/Wire/Game.cs
    Adapters.GameProtocol.Blue/Controllers/BookkeepingController.cs
    Adapters.GameProtocol.Blue/Controllers/HeartbeatController.cs
    Adapters.GameProtocol.Blue/Controllers/InitialDataCheckController.cs
    Adapters.GameProtocol.Blue/Controllers/PlayResultController.cs
    Tests/Blue/BlueRouteSkeletonTests.cs
    Tests/Blue/ProtocolRouteTestHelper.cs
    proto/yellow/yellow.proto
  </read_first>
  <behavior>
    - Test: `YellowRouteSkeletonTests` reflects all Phase 12-supported Yellow game routes under the evidence-backed prefix and proves they are owned by `TaikoLocalServer.Adapters.GameProtocol.Yellow`.
    - Test: `YellowRouteSkeletonTests` proves Yellow adapter does not own `startupauth.php`, `verupauth.php`, `verupcomplete.php`, `getreitai.php`, or `battleuserdata.php` under the Yellow game prefix per D-03, D-06, D-07, and YFND-04.
    - Test: `YellowRouteSkeletonTests` proves Phase 12 Yellow scaffold controllers do not call `Mediator.Send`, `ITaikoDbContext`, `SaveChanges`, or Yellow persistence/runtime behavior per D-09 and D-11.
  </behavior>
  <action>Create Yellow controllers under `Adapters.GameProtocol.Yellow/Controllers` for proto-backed supported endpoint suffixes from `12-RESEARCH.md`: `initialdatacheck.php`, `tournamentcheck.php`, `bookkeeping.php`, `coinsetting.php`, `gettelop.php`, `getfolder.php`, `taikojuku.php`, `getitemshopinfo.php`, `headclerk2.php`, `playresult.php`, `baidcheck.php`, `mydonentry.php`, `userdata.php`, `challengecompe.php`, `balancecheck.php`, `banacoinpayment.php`, `banacoinerrorlog.php`, `getbanacoininfo.php`, `crownsdata.php`, `recommend.php`, `selfbest.php`, `heartbeat.php`, `itempurchase.php`, `rewardcardcheck.php`, and `rewardexecution.php`. Use the exact Yellow game prefix recorded by Task 1 followed by `/chassis/{suffix}`. Each controller must use `[ApiController]`, absolute `[Route]`, `[HttpPost]`, `[Produces("application/protobuf")]`, derive from `BaseProtocolController<T>`, accept the generated Yellow `[FromBody]` request type, log one bounded Yellow request message, and return a generated Yellow response with success-shaped `Result = 1` only when that response type exposes `Result`. For response types without a `Result` property, return an empty generated response and record that in the evidence file. Do not create `getreitai.php`, `battleuserdata.php`, startup/version controllers, Mediator-backed handlers, EF entities, migrations, catalog reads, normal score writes, Tokkun persistence, shop/medal behavior, Dani behavior, Banacoin wallet/payment state, AdminApi/WebUI code, or Blue battle fallback. Add `Tests/Yellow/YellowRouteSkeletonTests.cs` by reusing `Tests/Blue/ProtocolRouteTestHelper.cs`; expected Yellow routes must exactly match the implemented scaffold list and explicitly exclude `getreitai.php`, shared startup/version routes, and `battleuserdata.php`.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~YellowRouteSkeletonTests"</automated>
  </verify>
  <acceptance_criteria>
    YFND-02 route ownership is covered: Yellow owns only evidence-backed proto-supported game route scaffolds under the confirmed Yellow prefix; shared/version, `getreitai.php`, and battle routes are absent; route controllers remain no-state scaffolds.
  </acceptance_criteria>
  <done>Yellow route skeleton tests pass and no Yellow route controller contains runtime business behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register Yellow in Host and settings behind enabled-era filtering</name>
  <files>Host/Host.csproj, Host/Program.cs, Host/Configurations/ServerSettings.json, Tests/Yellow/YellowHostProgramSourceTests.cs</files>
  <read_first>
    .planning/phases/12-yellow-evidence-and-era-foundation/12-CONTEXT.md
    .planning/phases/12-yellow-evidence-and-era-foundation/12-RESEARCH.md
    .planning/phases/12-yellow-evidence-and-era-foundation/12-PATTERNS.md
    .planning/phases/12-yellow-evidence-and-era-foundation/12-YELLOW-EVIDENCE.md
    Host/Host.csproj
    Host/Program.cs
    Host/Configurations/ServerSettings.json
    Tests/Blue/BlueHostProgramSourceTests.cs
    Tests/Yellow/YellowEraFoundationTests.cs
    Adapters.GameProtocol.Yellow/DependencyInjection.cs
  </read_first>
  <behavior>
    - Test: `YellowHostProgramSourceTests` proves `Program.cs` imports Yellow, calls `AddGameProtocolYellow()` only when `enabledEras.Contains(GameEra.Yellow)`, removes `TaikoLocalServer.Adapters.GameProtocol.Yellow` when disabled, and assumes protobuf only for the exact Yellow `.../chassis` prefix.
    - Test: `YellowHostProgramSourceTests` proves `Program.cs` does not use a broad Yellow version prefix fallback without `/chassis`.
    - Test: `YellowEraFoundationTests` proves shipped `ServerSettings:Eras:Yellow` exists, binds, and includes `Enabled` plus `GameDataPath = wwwroot/data/yellow/data`.
  </behavior>
  <action>Add `..\Adapters.GameProtocol.Yellow\Adapters.GameProtocol.Yellow.csproj` to `Host/Host.csproj` next to Blue/Green adapter references. Add Yellow raw data exclusion for `wwwroot\data\yellow\data\**` and a debug-build Yellow data junction target mirroring the Blue/Green targets if `Host/wwwroot/data/yellow/data` exists. Update `Host/Program.cs` with `using TaikoLocalServer.Adapters.GameProtocol.Yellow;`, an `if (enabledEras.Contains(GameEra.Yellow)) { builder.Services.AddGameProtocolYellow(); }` branch, `RemoveApplicationPart(apm, "TaikoLocalServer.Adapters.GameProtocol.Yellow");` when disabled, and a scoped `path.StartsWithSegments("{confirmed-yellow-prefix}/chassis", StringComparison.OrdinalIgnoreCase)` entry in `ShouldAssumeProtobufRequest`. Use the exact prefix recorded by Task 1. Update `Host/Configurations/ServerSettings.json` with a `Yellow` era block containing `Enabled`, `AutoExtractCatalog`, and `GameDataPath: "wwwroot/data/yellow/data"`; do not add Yellow shop settings or add Yellow to AC15 shop validation in Phase 12 because shop runtime is Phase 15. Add `Tests/Yellow/YellowHostProgramSourceTests.cs` following `Tests/Blue/BlueHostProgramSourceTests.cs` plus the negative broad-prefix assertion. Do not add per-request enabled-era checks to controllers.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~YellowHostProgramSourceTests|FullyQualifiedName~YellowEraFoundationTests"</automated>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build-phase12-yellow"</automated>
  </verify>
  <acceptance_criteria>
    YFND-03 is covered: Host references/registers Yellow only through enabled-era configuration, disabled Yellow application parts are removed, missing-content-type protobuf fallback is scoped to the exact Yellow `/chassis` prefix, settings bind without shop validation side effects, and temp-output Host build passes.
  </acceptance_criteria>
  <done>Host/settings wiring tests and temp-output Host build pass with Yellow enabled-era filtering in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human/local evidence to route attributes | Concrete HTTP routes become server contract only after the Yellow prefix is proven or explicitly user-approved. |
| Yellow adapter to Host application parts | Disabled-era filtering must prevent Yellow controllers from being routable when Yellow is disabled. |
| Untrusted cabinet request to no-state scaffold | Phase 12 routes deserialize direct protobuf but must not mutate state or infer runtime semantics. |
| Missing Content-Type fallback to request parsing | Fallback must be scoped to the exact Yellow `/chassis` prefix to avoid widening protobuf assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing / Denial of Service | Yellow route prefix | mitigate | Blocking decision task requires local/user-approved prefix evidence before concrete route attributes. |
| T-12-06 | Elevation of privilege / Information disclosure | Host application parts | mitigate | Add Yellow application-part removal when disabled and source tests for the exact assembly name. |
| T-12-07 | Tampering | Yellow scaffold controllers | mitigate | Route skeleton tests forbid Mediator/EF/persistence calls in Phase 12 scaffold controllers. |
| T-12-08 | Spoofing / Denial of Service | `ShouldAssumeProtobufRequest` | mitigate | Host source tests require exact `.../chassis` fallback and forbid broad Yellow prefix fallback. |
| T-12-SC | Supply chain | packages | mitigate | No package install is planned. |
</threat_model>

<verification>
Run these gates after all tasks complete:

1. `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~YellowRouteSkeletonTests|FullyQualifiedName~YellowHostProgramSourceTests|FullyQualifiedName~YellowEraFoundationTests"`
2. `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build-phase12-yellow"`
3. Inspect `Adapters.GameProtocol.Yellow/Controllers/*.cs` and confirm there are no `Mediator.Send`, `ITaikoDbContext`, `DbContext`, `SaveChanges`, `BlueBattle`, or Yellow persistence references.
4. Inspect `Host/Program.cs` and confirm the Yellow protobuf fallback uses the exact confirmed Yellow `/chassis` prefix and not a broad version prefix.

## Source Audit

| Source | ID | Requirement / Decision / Constraint | Plan | Status | Notes |
|--------|----|-------------------------------------|------|--------|-------|
| GOAL | Phase 12 | Add first-class Yellow adapter scaffolding and route enablement. | 12-02 | COVERED IN PLAN SET | Concrete routes and Host wiring after Plan 12-01 foundation. |
| REQ | YFND-02 | Yellow cabinet routes are served by first-class adapter with route ownership tests. | 12-02 | COVERED | Tasks 1-2. |
| REQ | YFND-03 | Yellow game routes present only when Yellow is enabled. | 12-02 | COVERED | Task 3 Host registration/filtering tests. |
| CONTEXT | D-02 | Yellow adapter routes/settings/host registration are era-owned. | 12-02 | COVERED | Controllers, Host, settings. |
| CONTEXT | D-03 | Shared startup/version routes remain under `/v01r00/chassis/*`. | 12-02 | PARTIAL IN PLAN SET | Route exclusions here; shared-version proof in Plan 12-03. |
| CONTEXT | D-04 | Yellow game routes use direct protobuf where evidence supports. | 12-02 | COVERED | Direct `[FromBody]` generated Yellow request controllers. |
| CONTEXT | D-06 | Record unresolved client-evidence gaps instead of inventing route behavior. | 12-02 | COVERED | Blocking route-prefix decision. |
| CONTEXT | D-09 | Controllers are thin; business behavior later. | 12-02 | COVERED | No-state scaffold and source guard. |
| CONTEXT | D-10 | Reuse Blue/Green/shared patterns where matching. | 12-02 | COVERED | Blue route/Host tests and controller patterns. |
| CONTEXT | D-11 | No Yellow persistence/runtime behavior in this phase unless compile scaffold requires no-state placeholder. | 12-02 | COVERED | Source guards forbid persistence and business behavior. |
| CONTEXT | D-12 | Smallest scaffold and tests that prove success criteria. | 12-02 | COVERED | No catalog/profile/shop/Tokkun runtime behavior. |
</verification>

<success_criteria>
- `12-YELLOW-EVIDENCE.md` records the exact Yellow game route prefix before route code is written.
- Yellow adapter owns only the proto-backed Phase 12-supported game route scaffolds under the confirmed prefix.
- Yellow adapter does not own startup/version, `getreitai.php`, or `battleuserdata.php` routes.
- Host registers Yellow services and removes Yellow application part when disabled.
- Host protobuf fallback is scoped to the exact confirmed Yellow `/chassis` prefix.
- Yellow settings bind without adding deferred shop validation or runtime data behavior.
- Focused Yellow route/Host tests and temp-output Host build pass.
</success_criteria>

<output>
Create `.planning/phases/12-yellow-evidence-and-era-foundation/12-02-SUMMARY.md` when execution completes.
</output>
