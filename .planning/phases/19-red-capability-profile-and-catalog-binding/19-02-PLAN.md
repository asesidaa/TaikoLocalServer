---
phase: 19
plan: 02
title: Red catalog-backed metadata routes
status: pending
wave: 2
requirements: [RCAT-01, RCAT-02]
---

# Plan 19-02 - Red Catalog-Backed Metadata Routes

## Goal

Replace Phase 18 Red no-state probes with catalog-backed behavior only for Phase 19-owned metadata routes.

## Tasks

1. Add Red Application handler partials.
   - `GetInitialDataQuery.Red`
   - `GetFolderQuery.Red`
   - `GetTelopQuery.Red`
   - `GetRecommendQuery.Red`
   - `GetTaikojukuQuery.Red`
   - `GetStartupMovieDataQuery` HDD major version 8 mapping

2. Add Red adapter mappers.
   - Mirror the thin Mapperly shape used by Yellow, mapping common DTOs to Red generated wire DTOs.
   - Keep generated wire untouched.

3. Update Red controllers for Phase 19-owned routes.
   - `initialdatacheck.php`
   - `getfolder.php`
   - `gettelop.php`
   - `recommend.php`
   - `taikojuku.php`
   - Leave all other Red probes as no-state Phase 18 probes.

## Verification

- Focused Red protocol tests for catalog-backed mapping and empty sidecar behavior.
- Startup movie query test for HDD major version 8 when Red is enabled.
- Temp-output Host build.
