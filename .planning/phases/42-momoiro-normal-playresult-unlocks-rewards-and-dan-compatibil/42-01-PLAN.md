---
phase: 42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - "Tests/Momoiro/MomoiroHandlerFixture.cs"
  - "Tests/Momoiro/MomoiroPlayResultHandlerTests.cs"
  - "Tests/Momoiro/MomoiroPlayResultControllerTests.cs"
  - ".planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-VALIDATION.md"
autonomous: true
requirements:
  - MORUN-01
  - MORUN-02
  - MORUN-03
  - MORUN-04
  - MORUN-05
requirements_addressed:
  - MORUN-01
  - MORUN-02
  - MORUN-03
  - MORUN-04
  - MORUN-05
user_setup: []
must_haves:
  truths:
    - "MORUN-01: Wave 0 tests define Momoiro normal playresult writes for play history, best/crown source rows, counters, recents, and favorites."
    - "MORUN-02: Wave 0 tests define release-song, Don Point, and reward mutation on Momoiro save fields only, with no shop or wallet authority."
    - "MORUN-03: Wave 0 tests define bounded Momoiro Dan playresult persistence and BAID/userdata readback without Taikojuku route behavior."
    - "MORUN-04: Wave 0 tests define challenge-shaped arrays as accepted/logged/dropped with no Don Challenge, ChallengeCompe, or reward-management state."
    - "MORUN-05: Wave 0 tests seed adjacent-era and unsupported state and require Momoiro playresult handling to leave it untouched."
  artifacts:
    - path: "Tests/Momoiro/MomoiroPlayResultHandlerTests.cs"
      provides: "Handler RED contracts for Momoiro playresult mutation and no-cross-era boundaries"
    - path: "Tests/Momoiro/MomoiroPlayResultControllerTests.cs"
      provides: "Controller/Mapperly RED contracts for direct protobuf playresult mapping"
    - path: "Tests/Momoiro/MomoiroHandlerFixture.cs"
      provides: "Momoiro test catalog, raw future table helpers, and row-count helpers"
    - path: ".planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-VALIDATION.md"
      provides: "Phase 42 validation ownership matrix"
  key_links:
    - from: "Tests/Momoiro/MomoiroPlayResultHandlerTests.cs"
      to: "Application/Handlers/UpdatePlayResultCommand.Momoiro.cs"
      via: "Tests call UpdateAc15PlayResultCommand with GameEra.Momoiro and fail until the Momoiro handler exists."
      pattern: "UpdateAc15PlayResultCommand.*GameEra\\.Momoiro"
    - from: "Tests/Momoiro/MomoiroPlayResultControllerTests.cs"
      to: "Adapters.GameProtocol.Momoiro/Mappers/PlayResultMappers.cs"
      via: "Tests post Momoiro wire requests through the controller and require mapped Application facts."
      pattern: "ReleaseSongNoes|DanResult|AryChallengeIds"
    - from: "Tests/Momoiro/MomoiroHandlerFixture.cs"
      to: "Application/Handlers/UserDataQuery.Momoiro.cs"
      via: "Fixture seeds Momoiro save/list/best rows so playresult writes can be read back through Phase 41 handlers."
      pattern: "DisplayOrder|SongBestDatum_Momoiro|UserSaveData_Momoiro"
---

<objective>
Create Phase 42 Wave 0 RED contracts for Momoiro normal playresult mutation, unlock/reward updates, bounded Dan compatibility, challenge no-state behavior, and no-cross-era persistence.

Purpose: Force implementation plans to satisfy MORUN-01 through MORUN-05 before claiming Momoiro runtime mutation behavior.
Output: Focused failing tests and a validation matrix that compile before Momoiro play-history and Dan production symbols exist.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/gsd-core/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-CONTEXT.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-PATTERNS.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-RESEARCH.md
@.planning/phases/41-momoiro-identity-userdata-self-best-and-crown-readback/41-05-SUMMARY.md
@Tests/Momoiro/MomoiroHandlerFixture.cs
@Tests/Momoiro/MomoiroReadbackHandlerTests.cs
@Tests/Momoiro/MomoiroControllerReadbackTests.cs
@Tests/Murasaki/MurasakiRuntimeHandlerTests.cs
@Tests/Ac15/Ac15NormalPlayWriterTests.cs
@Tests/Ac15/Ac15DaniCapabilityTests.cs
</context>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 42 | Momoiro normal play can persist and read back only evidence-backed Momoiro-owned gameplay mutations. | 42-01..42-07 | COVERED | Tests, schema, Application, adapter, and final gates all target this outcome. |
| REQ | MORUN-01 | Normal playresults persist scores, self-best, crowns, counters, recents, favorites, and normal-play state only to Momoiro-owned tables. | 42-01, 42-02, 42-03, 42-05, 42-07 | COVERED | Includes play-history table and DisplayOrder-preserving favorites. |
| REQ | MORUN-02 | Song unlock, Don Point, and reward behavior mutates only evidence-backed Momoiro fields without shop or wallet authority. | 42-01, 42-03, 42-05, 42-07 | COVERED | Uses Momoiro `ReleaseSongFlg`, `TotalGetDonpoint`, `RewardPtn`, and `RewardProgress`; no spend/shop state. |
| REQ | MORUN-03 | Dan/Dani fields persist and read back only for the normal Dan contract; Taikojuku practice-folder behavior remains absent. | 42-01, 42-02, 42-04, 42-05, 42-07 | COVERED | Implements bounded playresult/BAID/userdata Dan compatibility with Momoiro-owned Dan tables. |
| REQ | MORUN-04 | Challenge-shaped arrays follow Momoiro evidence and do not create Don Challenge, ChallengeCompe, or reward-management behavior. | 42-01, 42-05, 42-06, 42-07 | COVERED | Arrays are mapped for diagnostics and dropped from persistence. |
| REQ | MORUN-05 | Runtime writes do not touch adjacent eras or unsupported feature state. | 42-01, 42-02, 42-05, 42-07 | COVERED | Tests and source gates cover adjacent-era and unsupported-state absence. |
| RESEARCH | R-01 | `playresult.php` is a binary-proven Momoiro route under `/v04r00/chassis`. | 42-06, 42-07 | COVERED | Adapter plan replaces the scaffold route only. |
| RESEARCH | R-02 | Existing AC15 playresult helpers should be wired to Momoiro-owned state. | 42-02, 42-03, 42-05 | COVERED | Shared algorithms, Momoiro tables. |
| RESEARCH | R-03 | Dan field evidence supports bounded playresult/BAID compatibility but no Taikojuku route. | 42-02, 42-04, 42-05, 42-07 | COVERED | Uses `DaniFileOrder`; no `taikojuku.php` or folder route behavior. |
| RESEARCH | R-04 | Challenge arrays have descriptors but no stateful route semantics. | 42-01, 42-05, 42-07 | COVERED | Log/drop with no persistence. |
| CONTEXT | CTX-01 | No proto edits, no unsupported route families, no `Host/.gitignore` touch. | 42-01..42-07 | COVERED | Every plan includes proto and Host/.gitignore gates where relevant. |
| CONTEXT | CTX-02 | Preserve Phase 41 `MomoiroFavoriteSongs.DisplayOrder`. | 42-01, 42-03, 42-05, 42-07 | COVERED | Favorite mutation appends after current max display order. |
| CONTEXT | CTX-03 | Release-song, crown-source, Don Point, reward, counters, recents, favorites, scores, and best rows must be Momoiro-owned and read back through Phase 41 routes. | 42-01, 42-02, 42-03, 42-05, 42-07 | COVERED | Tests verify Phase 41 readback after playresult writes. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Momoiro fixture for future playresult state</name>
  <files>Tests/Momoiro/MomoiroHandlerFixture.cs</files>
  <behavior>
    - Tests can seed and count future `SongPlayDatum_Momoiro`, `DanScoreDatum_Momoiro`, and `DanStageScoreDatum_Momoiro` tables without referencing production C# types that do not exist yet.
    - The fixture `TestMomoiroCatalog` exposes a bounded Dan/Dani file-order list for RED tests without introducing Taikojuku route behavior.
    - Row-count helpers can verify adjacent-era and unsupported feature tables remain unchanged.
  </behavior>
  <action>Extend `MomoiroHandlerFixture` with raw SQL helpers for `SongPlayDatum_Momoiro`, `DanScoreDatum_Momoiro`, and `DanStageScoreDatum_Momoiro` that mirror adjacent AC15 table shapes. Add seed/count helpers for play rows, Dan rows, favorites with `DisplayOrder`, recents, and selected adjacent unsupported state row counts used by the tests. Extend `TestMomoiroCatalog` with a `DaniFileOrder`/bounded Dan list over `Ac15TaikojukuEntry` entries loaded in-memory for tests only; do not add Taikojuku route helpers, practice-folder helpers, proto edits, or source-text route assertions. Keep fixture code compile-safe before production Momoiro play-history and Dan entity symbols are added.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroReadbackHandlerTests" --no-restore -- RunConfiguration.DisableParallelization=true</automated>
  </verify>
  <done>The existing Momoiro readback tests still pass or fail only for current production behavior; the fixture compiles with future-table helpers and no future-symbol errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handler RED contracts for MORUN runtime mutation</name>
  <files>Tests/Momoiro/MomoiroPlayResultHandlerTests.cs</files>
  <behavior>
    - MORUN-01: A normal playresult for an existing Momoiro user creates one Momoiro play-history row, updates `SongBestDatum_Momoiro`, writes a recent row, writes a favorite row with `DisplayOrder` after the previous max, increments profile counters, and feeds Phase 41 userdata/selfbest/crown readback.
    - MORUN-02: `ReleaseSongNoes`, `GetDonpoint`, `RewardPtn`, and `RewardProgress` mutate only `UserSaveData_Momoiro`; `TotalUseDonpoint`, shop-season, wallet, purchase, payment, and coupon-style state remain unchanged or absent.
    - MORUN-03: A Dan-mode playresult with one valid `PlayDan` value and `DanResult` creates only Momoiro Dan score/stage rows, updates Momoiro save Dan flags/display fields for BAID/userdata readback, and does not add Taikojuku routes or practice-folder state.
    - MORUN-04: Challenge-shaped stage arrays are accepted with a normal play row but create no Red/White Don Challenge, ChallengeCompe, or reward-management rows.
    - MORUN-05: Adjacent era play, best, Dan, Tokkun, battle, shop-season, Banacoin, Don Challenge, and Nijiiro state remains untouched.
  </behavior>
  <action>Create `MomoiroPlayResultHandlerTests` using `UpdatePlayResultCommandHandler` and `Ac15PlayResultTestFactory.Command`. Seed shared identity and Momoiro save state, plus conflicting adjacent-era rows where needed. Tests must assert observable database state and Phase 41 readback after handler execution, not source text or private methods. Include a specific favorite-order case where existing favorites have non-sorted song numbers and display orders, then a playresult favorite appends with `DisplayOrder = current max + 1`; assert readback remains ordered by display order, not raw song number. Include a Dan case using fixture catalog Dan level 1, `PlayMode.DanMode`, `PlayDan = 1`, and a bounded nonzero `DanResult`; assert BAID readback sees Momoiro Dan flags through `BaidQuery.Momoiro`. Include a challenge-array case that proves no challenge tables are created or updated.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroPlayResultHandlerTests" --no-restore -- RunConfiguration.DisableParallelization=true</automated>
  </verify>
  <done>The command compiles and fails only because Momoiro playresult schema/dispatch/write behavior is not implemented yet; no future-symbol compile failures are allowed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add controller RED contracts and validation matrix</name>
  <files>Tests/Momoiro/MomoiroPlayResultControllerTests.cs, .planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-VALIDATION.md</files>
  <behavior>
    - The Momoiro `playresult.php` controller maps direct protobuf request fields into the AC15 Application envelope and sends `UpdateAc15PlayResultCommand` with `GameEra.Momoiro`.
    - The mapped request preserves `release_song_no`, `get_donpoint`, `reward_ptn`, `reward_progress`, `dan_result`, stage `play_dan`, and challenge-shaped arrays.
    - Unsupported route families remain absent from the Momoiro adapter.
  </behavior>
  <action>Create controller tests that instantiate the Momoiro `PlayResultController` through the existing test controller pattern, post a generated Momoiro `PlayResultRequest`, and verify state through the handler/database after the route returns `Result = 1`. Include a mapper-visible request with release-song IDs, Don Point/reward values, Dan result, stage PlayDan, and all three challenge-array families. Do not test generated wire property existence by itself; assert the end-to-end public controller path effects and no unsupported route/state effects. Update existing `42-VALIDATION.md` rows `42-W0-01` through `42-W0-05` from pending to expected RED status as Wave 0 contracts are added.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroPlayResultControllerTests|FullyQualifiedName~MomoiroRouteSurfaceTests" --no-restore -- RunConfiguration.DisableParallelization=true</automated>
  </verify>
  <done>Controller tests compile and fail only on scaffolded/missing Momoiro playresult behavior, while unsupported-route tests remain green; `42-VALIDATION.md` records Wave 0 RED ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cabinet -> `playresult.php` | Untrusted direct protobuf request body enters the Momoiro adapter. |
| adapter -> Application | Wire DTO fields become common AC15 mutation facts. |
| Application -> SQLite | Playresult facts mutate persistent Momoiro-owned rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | Momoiro playresult handler | mitigate | RED tests seed adjacent eras and unsupported feature rows, then assert only Momoiro-owned rows change. |
| T-42-02 | Elevation of Privilege | reward and unlock mutation | mitigate | RED tests require release/Don Point/reward writes to stay on Momoiro save fields and exclude shop, wallet, payment, and season state. |
| T-42-03 | Repudiation | challenge-shaped fields | accept | Tests require challenge arrays to be accepted/logged/dropped without persistence because stateful semantics are unproven. |
| T-42-SC | Tampering | package installs | accept | No package install tasks are present. |
</threat_model>

<verification>
Wave 0 is complete when the focused Momoiro playresult test commands compile and fail only for missing production behavior, `MomoiroRouteSurfaceTests` still keeps unsupported routes absent, `git status --porcelain -- proto\momoiro` is empty, and `git diff --cached --name-only -- Host/.gitignore` is empty.
</verification>

<success_criteria>
MORUN-01 through MORUN-05 have executable RED contracts before production schema, Application, or adapter implementation begins.
</success_criteria>

<output>
Create `.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-01-SUMMARY.md` when done.
</output>
