---
phase: 42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil
plan: "06"
type: execute
wave: 4
depends_on:
  - "42-05"
files_modified:
  - "Adapters.GameProtocol.Momoiro/Mappers/PlayResultMappers.cs"
  - "Adapters.GameProtocol.Momoiro/Controllers/PlayResultController.cs"
  - "Tests/Momoiro/MomoiroPlayResultControllerTests.cs"
autonomous: true
requirements:
  - MORUN-01
  - MORUN-02
  - MORUN-03
  - MORUN-04
  - MORUN-05
requirements_addressed:
  - MORUN-01
  - MORUN-02
  - MORUN-03
  - MORUN-04
  - MORUN-05
user_setup: []
must_haves:
  truths:
    - "MORUN-01: Momoiro `playresult.php` routes direct protobuf requests through the AC15 Application pipeline instead of returning scaffold success."
    - "MORUN-02: Mapperly request projection preserves release-song, Don Point, reward pattern, and reward progress facts for Momoiro save mutation."
    - "MORUN-03: Mapperly request projection preserves `dan_result` and stage `play_dan` for bounded Dan handling."
    - "MORUN-04: Mapperly request projection accepts challenge-shaped arrays but the controller does not add challenge persistence or route behavior."
    - "MORUN-05: The adapter exposes no unsupported Momoiro route families and does not touch proto or generated Wire files."
  artifacts:
    - path: "Adapters.GameProtocol.Momoiro/Mappers/PlayResultMappers.cs"
      provides: "Mapperly projection from Momoiro wire PlayResultRequest to Ac15PlayResultEnvelope"
    - path: "Adapters.GameProtocol.Momoiro/Controllers/PlayResultController.cs"
      provides: "Mediator-backed Momoiro playresult protocol route"
    - path: "Tests/Momoiro/MomoiroPlayResultControllerTests.cs"
      provides: "Adapter-level verification over the public controller path"
  key_links:
    - from: "Adapters.GameProtocol.Momoiro/Controllers/PlayResultController.cs"
      to: "Application/Handlers/UpdatePlayResultCommand.Momoiro.cs"
      via: "Controller sends UpdateAc15PlayResultCommand with GameEra.Momoiro."
      pattern: "UpdateAc15PlayResultCommand\\(request\\.Baid, GameEra\\.Momoiro"
    - from: "Adapters.GameProtocol.Momoiro/Mappers/PlayResultMappers.cs"
      to: "Application/Dtos/Ac15/Ac15PlayResultInput.cs"
      via: "Mapperly maps wire request into common AC15 envelope, profile facts, normal stages, and Dani facts."
      pattern: "Ac15PlayResultEnvelope|Ac15ProfileMutationFacts|Ac15DaniPlayResult"
---

<objective>
Replace the Momoiro `playresult.php` scaffold with a thin direct-protobuf controller and Mapperly request mapping.

Purpose: Connect the binary-proven Momoiro route to the Application behavior implemented in plan 42-05 while keeping adapter code free of business logic.
Output: Momoiro PlayResult mapper, Mediator-backed controller, green controller tests, and generated-source inspection evidence.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/gsd-core/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-CONTEXT.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-PATTERNS.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-RESEARCH.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-05-SUMMARY.md
@Adapters.GameProtocol.Momoiro/Controllers/PlayResultController.cs
@Adapters.GameProtocol.Kimidori/Controllers/PlayResultController.cs
@Adapters.GameProtocol.Kimidori/Mappers/PlayResultMappers.cs
@Adapters.GameProtocol.Murasaki/Mappers/PlayResultMappers.cs
@Adapters.GameProtocol.Momoiro/Wire/Game.cs
@Application/Dtos/Ac15/Ac15PlayResultInput.cs
@Tests/Momoiro/MomoiroPlayResultControllerTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Momoiro PlayResult Mapperly mapper</name>
  <files>Adapters.GameProtocol.Momoiro/Mappers/PlayResultMappers.cs</files>
  <behavior>
    - Momoiro `PlayResultRequest` maps into `Ac15PlayResultEnvelope` with metadata, profile mutation facts, normal stages, and Dani facts.
    - Profile mapping preserves `ReleaseSongNoes`, `GetDonpoint`, `RewardPtn`, `RewardProgress`, `IsDevil`, `IsExplain`, current costume, and bounded customization flags where the generated wire exposes them.
    - Stage mapping preserves score/crown inputs, favorite/recent flags, `PlayDan`, soul gauge/hit count optional presence, and all three challenge-shaped arrays.
    - Tokkun, BlueBattle, and GreenGhost envelope fields are explicitly null.
  </behavior>
  <action>Create `Adapters.GameProtocol.Momoiro/Mappers/PlayResultMappers.cs` by copying the Kimidori/Murasaki Mapperly shape into the Momoiro namespace and using `Adapters.GameProtocol.Momoiro.Wire` types. Keep `[Mapper(AllowNullPropertyAssignment = false, ThrowOnMappingNullMismatch = false, ThrowOnPropertyMappingNullMismatch = false)]`. Use Mapperly `MapProperty`, `MapPropertyFromSource`, `MapValue`, `MapperIgnoreTarget`, and small helper conversions; do not hand-write the full mapper body. Include mapping for Momoiro `release_song_no`, `get_donpoint`, `reward_ptn`, `reward_progress`, `dan_result`, stage `play_dan`, and challenge arrays. Do not edit `proto/momoiro` or generated `Wire` files.</action>
  <verify>
    <automated>dotnet build Adapters.GameProtocol.Momoiro/Adapters.GameProtocol.Momoiro.csproj /p:EmitCompilerGeneratedFiles=true --no-restore</automated>
    <automated>rg -n "ReleaseSongNoes|GetDonpoint|RewardPtn|RewardProgress|DanResult|PlayDan|ChallengeIds|UserCompeIds|BngCompeIds" Adapters.GameProtocol.Momoiro/obj/Debug/net10.0/generated/Riok.Mapperly Adapters.GameProtocol.Momoiro/Mappers/PlayResultMappers.cs</automated>
  </verify>
  <done>Adapter build succeeds and generated Mapperly source shows the required Momoiro playresult facts being assigned into common AC15 DTOs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace playresult scaffold with Mediator controller</name>
  <files>Adapters.GameProtocol.Momoiro/Controllers/PlayResultController.cs</files>
  <behavior>
    - `POST /v04r00/chassis/playresult.php` logs the request, maps it, sends `UpdateAc15PlayResultCommand(request.Baid, GameEra.Momoiro, playResult)`, and returns `PlayResultMappers.Map(result)`.
    - Controller code deserializes, maps, calls Mediator, and maps back; no business logic or EF access is added to the adapter.
    - Direct protobuf transport remains unchanged.
  </behavior>
  <action>Update `PlayResultController` to the Kimidori controller shape with Momoiro route prefix, Momoiro mapper, and `GameEra.Momoiro`. Change the action to async and use `HttpContext.RequestAborted`. Keep logging bounded to the request object or key request metadata. Do not add controller-local filtering, save logic, route gating, unsupported route families, or challenge persistence.</action>
  <verify>
    <automated>dotnet build Adapters.GameProtocol.Momoiro/Adapters.GameProtocol.Momoiro.csproj --no-restore</automated>
  </verify>
  <done>The Momoiro playresult route is Mediator-backed and the adapter project builds cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Green controller tests and adapter source gates</name>
  <files>Tests/Momoiro/MomoiroPlayResultControllerTests.cs</files>
  <behavior>
    - Controller tests prove the public Momoiro route path persists normal state through the Application handler.
    - Controller tests prove mapped release/reward/Dan/challenge fields reach the Application behavior from plan 42-05.
    - Unsupported Momoiro route families stay absent.
  </behavior>
  <action>Run and fix `MomoiroPlayResultControllerTests` without weakening their assertions. The tests should observe database/readback effects through the public controller path, not by directly calling mapper private methods. Keep `MomoiroRouteSurfaceTests` in the verification command so unsupported/proto-only route families remain absent. Record any Mapperly generated-source observations in the summary.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroPlayResultControllerTests|FullyQualifiedName~MomoiroPlayResultHandlerTests|FullyQualifiedName~MomoiroRouteSurfaceTests" --no-restore -- RunConfiguration.DisableParallelization=true</automated>
    <automated>$status = git status --porcelain -- proto\momoiro; if ($status) { $status; exit 1 }; $staged = git diff --cached --name-only -- Host/.gitignore; if ($staged) { $staged; exit 1 }; exit 0</automated>
  </verify>
  <done>Momoiro playresult controller and handler tests pass together, unsupported routes remain absent, proto inputs are clean, and `Host/.gitignore` is not staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cabinet -> adapter | Untrusted direct protobuf request enters the Momoiro controller. |
| adapter mapper -> Application DTO | Generated Mapperly code converts wire fields into common mutation facts. |
| adapter -> Mediator | Controller dispatches the mutation command into Application handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-17 | Tampering | PlayResult mapper | mitigate | Mapperly generated-source inspection verifies required fields and explicit null unsupported sections. |
| T-42-18 | Elevation of Privilege | controller | mitigate | Controller contains no business logic and only dispatches `GameEra.Momoiro` command. |
| T-42-19 | Tampering | unsupported routes | mitigate | Route surface tests remain in the verification command. |
| T-42-SC | Tampering | package installs | accept | No package install tasks are present. |
</threat_model>

<verification>
This plan is verified by Momoiro adapter build with Mapperly generated-source emission, focused controller/handler/route tests, generated-source grep for mapped fields, proto cleanliness, and `Host/.gitignore` staged cleanliness.
</verification>

<success_criteria>
Momoiro `playresult.php` is a thin Mediator-backed route whose Mapperly-generated request projection feeds the Phase 42 Application mutation pipeline.
</success_criteria>

<output>
Create `.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-06-SUMMARY.md` when done.
</output>
