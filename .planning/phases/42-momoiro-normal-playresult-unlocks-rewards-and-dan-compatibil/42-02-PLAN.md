---
phase: 42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil
plan: "02"
type: execute
wave: 1
depends_on:
  - "42-01"
files_modified:
  - "Domain/Entities/SongPlayDatumMomoiro.cs"
  - "Domain/Entities/DanScoreDatumMomoiro.cs"
  - "Domain/Entities/DanStageScoreDatumMomoiro.cs"
  - "Application/Abstractions/ITaikoDbContext.Momoiro.cs"
  - "Infrastructure/Persistence/TaikoDbContext.Momoiro.cs"
  - "Infrastructure/Persistence/Migrations/<generated>_AddMomoiroPlayResultState.cs"
  - "Infrastructure/Persistence/Migrations/<generated>_AddMomoiroPlayResultState.Designer.cs"
  - "Infrastructure/Persistence/Migrations/TaikoDbContextModelSnapshot.cs"
  - "Tests/Green/GreenAuthConfigTests.cs"
  - "Tests/Momoiro/MomoiroHandlerFixture.cs"
autonomous: true
requirements:
  - MORUN-01
  - MORUN-03
  - MORUN-05
requirements_addressed:
  - MORUN-01
  - MORUN-03
  - MORUN-05
user_setup: []
must_haves:
  truths:
    - "MORUN-01: Momoiro play-history rows have a Momoiro-owned table and DbSet separate from every adjacent AC15 era."
    - "MORUN-03: Momoiro Dan score and stage rows have Momoiro-owned tables for bounded playresult/BAID compatibility."
    - "MORUN-05: The migration adds only Momoiro playresult/Dan schema and no unsupported route, Tokkun, battle, shop, Banacoin, Don Challenge, or ChallengeCompe state."
  artifacts:
    - path: "Domain/Entities/SongPlayDatumMomoiro.cs"
      provides: "Momoiro-owned normal play-history entity"
    - path: "Domain/Entities/DanScoreDatumMomoiro.cs"
      provides: "Momoiro-owned Dan score aggregate entity"
    - path: "Domain/Entities/DanStageScoreDatumMomoiro.cs"
      provides: "Momoiro-owned Dan stage score child entity"
    - path: "Infrastructure/Persistence/Migrations/<generated>_AddMomoiroPlayResultState.cs"
      provides: "EF migration adding only Phase 42 Momoiro mutation tables"
  key_links:
    - from: "Application/Abstractions/ITaikoDbContext.Momoiro.cs"
      to: "Infrastructure/Persistence/TaikoDbContext.Momoiro.cs"
      via: "DbSet properties expose the same Momoiro tables through the Application port and EF implementation."
      pattern: "SongPlayDataMomoiro|DanScoreDataMomoiro|DanStageScoreDataMomoiro"
    - from: "Domain/Entities/DanScoreDatumMomoiro.cs"
      to: "Domain/Entities/DanStageScoreDatumMomoiro.cs"
      via: "Dan score rows own stage rows through the same composite key pattern as Kimidori/Murasaki."
      pattern: "DanStageScoreData|Parent"
---

<objective>
Add the Momoiro-owned persistence surface required for Phase 42 normal playresult and bounded Dan compatibility.

Purpose: Give later Application plans real Momoiro tables for play history and Dan rows without borrowing adjacent-era gameplay state.
Output: Domain entities, DbContext/ITaikoDbContext surfaces, and an EF migration adding only Phase 42 Momoiro mutation tables.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/gsd-core/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-CONTEXT.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-PATTERNS.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-RESEARCH.md
@.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-01-SUMMARY.md
@Domain/Entities/SongPlayDatumKimidori.cs
@Domain/Entities/DanScoreDatumKimidori.cs
@Domain/Entities/DanStageScoreDatumKimidori.cs
@Domain/Entities/UserSaveDataMomoiro.cs
@Application/Abstractions/ITaikoDbContext.Momoiro.cs
@Infrastructure/Persistence/TaikoDbContext.Momoiro.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Momoiro play-history and Dan entities</name>
  <files>Domain/Entities/SongPlayDatumMomoiro.cs, Domain/Entities/DanScoreDatumMomoiro.cs, Domain/Entities/DanStageScoreDatumMomoiro.cs</files>
  <behavior>
    - `SongPlayDatumMomoiro` implements `IAc15SongPlayDatum` and mirrors the adjacent AC15 play-history shape, including score, crown, play mode, stage mode, favorite/recent booleans, `PlayDan`, and `PlayTime`.
    - `DanScoreDatumMomoiro` implements `IAc15DanScoreDatum` and owns `List<DanStageScoreDatumMomoiro> DanStageScoreData`.
    - `DanStageScoreDatumMomoiro` implements `IAc15DanStageScoreDatum` and is keyed by BAID, Dan id, extra flag, and stage index through EF config.
  </behavior>
  <action>Create the three Momoiro entities by copying the Kimidori/Murasaki shapes and replacing only type names and navigation types. Keep the entities in `Domain/Entities`; do not add any Tokkun, battle, shop, Banacoin, ChallengeCompe, Don Challenge, AdminApi/WebUI, or proto-derived state. `SongPlayDatumMomoiro` must be a normal play-history table, not a score/best replacement; best/crown source remains `SongBestDatumMomoiro` from Phase 41.</action>
  <verify>
    <automated>dotnet build Domain/Domain.csproj --no-restore</automated>
  </verify>
  <done>Domain builds with the new Momoiro play-history and Dan entity types and no adjacent-era type references inside their declarations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Momoiro DbContext surfaces and migration</name>
  <files>Application/Abstractions/ITaikoDbContext.Momoiro.cs, Infrastructure/Persistence/TaikoDbContext.Momoiro.cs, Infrastructure/Persistence/Migrations/<generated>_AddMomoiroPlayResultState.cs, Infrastructure/Persistence/Migrations/<generated>_AddMomoiroPlayResultState.Designer.cs, Infrastructure/Persistence/Migrations/TaikoDbContextModelSnapshot.cs, Tests/Green/GreenAuthConfigTests.cs</files>
  <behavior>
    - `ITaikoDbContext.Momoiro.cs` and `TaikoDbContext.Momoiro.cs` expose `SongPlayDataMomoiro`, `DanScoreDataMomoiro`, and `DanStageScoreDataMomoiro` DbSets.
    - EF config maps tables `SongPlayDatum_Momoiro`, `DanScoreDatum_Momoiro`, and `DanStageScoreDatum_Momoiro` with BAID cascade FKs to shared `UserData`.
    - Dan stage rows cascade from their Momoiro Dan score parent and do not share tables with Kimidori or Murasaki.
  </behavior>
  <action>Add Momoiro DbSets to the Application port and EF partial. Configure `SongPlayDatum_Momoiro` like `SongPlayDatum_Kimidori`: identity `Id`, `(Baid, SongId, Difficulty, PlayTime)` index, `PlayTime` datetime, and unsigned enum conversions for `Difficulty` and `Crown`. Configure Momoiro Dan score/stage tables like Kimidori with the composite score key `(Baid, DanId, IsExtra)`, stage key `(Baid, DanId, IsExtra, StageIndex)`, score/stage relationship, unsigned enum conversion for `ClearGrade`, and BAID FK to shared identity. Update any existing test fake that implements `ITaikoDbContext`, including `Tests/Green/GreenAuthConfigTests.cs`, with throwing Momoiro properties so unrelated tests compile. Run `dotnet ef migrations add AddMomoiroPlayResultState --project Infrastructure --startup-project Host`; inspect the active migration body, not only EF Designer metadata.</action>
  <verify>
    <automated>dotnet build Infrastructure/Infrastructure.csproj --no-restore; dotnet ef migrations add AddMomoiroPlayResultState --project Infrastructure --startup-project Host; dotnet build Infrastructure/Infrastructure.csproj --no-restore</automated>
  </verify>
  <done>The generated migration adds exactly the three Phase 42 Momoiro tables plus relationships/indexes, and no unsupported feature tables or columns.</done>
</task>

<task type="auto">
  <name>Task 3: Align fixture helpers and run schema scope gates</name>
  <files>Tests/Momoiro/MomoiroHandlerFixture.cs</files>
  <action>Update `MomoiroHandlerFixture` so raw future-table helpers stay compatible with the production schema just added. If `EnsureCreatedAsync` already creates the tables, keep raw `CREATE TABLE IF NOT EXISTS` helpers idempotent and matching column names. Add or update count helpers used by `MomoiroPlayResultHandlerTests` for `SongPlayDatum_Momoiro`, `DanScoreDatum_Momoiro`, and `DanStageScoreDatum_Momoiro`. Run focused schema and RED test commands; tests can still fail on missing Application handler behavior, but must not fail because tables are absent or mismatched.</action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroPlayResultHandlerTests|FullyQualifiedName~MomoiroReadbackHandlerTests" --no-restore -- RunConfiguration.DisableParallelization=true</automated>
    <automated>$files = Get-ChildItem 'Infrastructure/Persistence/Migrations' -Filter '*AddMomoiroPlayResultState*.cs' | Where-Object { $_.Name -notlike '*.Designer.cs' }; $text = $files | Get-Content -Raw; foreach ($required in 'SongPlayDatum_Momoiro','DanScoreDatum_Momoiro','DanStageScoreDatum_Momoiro') { if ($text -notmatch [regex]::Escape($required)) { Write-Error "Missing $required"; exit 1 } }; foreach ($forbidden in 'Tokkun','Battle','Banacoin','ChallengeCompe','DonChallenge','ShopSeason','Payment','Wallet') { if ($text -match $forbidden) { Write-Error "Forbidden migration term $forbidden"; exit 1 } }</automated>
  </verify>
  <done>Focused tests compile through the schema layer, and the active migration-body gate proves only Momoiro play-history/Dan schema was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> SQLite | Future playresult handling will write untrusted cabinet facts into SQLite through these DbSets. |
| shared identity -> Momoiro state | New tables reference shared BAID identity while gameplay rows remain Momoiro-owned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Tampering | EF schema | mitigate | Migration-body gate allows only Momoiro play-history and Dan tables. |
| T-42-05 | Information Disclosure | DbContext surfaces | mitigate | New DbSets are Momoiro-named and do not point to adjacent-era tables. |
| T-42-06 | Denial of Service | cascade relationships | mitigate | Use established AC15 composite keys and cascade behavior, avoiding unbounded orphan rows. |
| T-42-SC | Tampering | package installs | accept | No package install tasks are present. |
</threat_model>

<verification>
Schema plan verification requires Domain and Infrastructure builds, the EF migration generation command, active migration-body inspection, focused Momoiro RED tests compiling through the schema layer, `git status --porcelain -- proto\momoiro` returning no output, and `Host/.gitignore` remaining unstaged.
</verification>

<success_criteria>
Momoiro has the required play-history and bounded Dan persistence surface, with no unsupported Momoiro feature storage added.
</success_criteria>

<output>
Create `.planning/phases/42-momoiro-normal-playresult-unlocks-rewards-and-dan-compatibil/42-02-SUMMARY.md` when done.
</output>
