---
phase: 05-blue-battle-runtime-support
plan: "05-05"
type: execute
wave: 3
depends_on:
  - "05-04"
files_modified:
  - Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.cs
  - Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.Designer.cs
  - Infrastructure/Persistence/Migrations/TaikoDbContextModelSnapshot.cs
  - Tests/Blue/BlueBattlePersistenceTests.cs
autonomous: true
requirements:
  - BTL-01
  - BTL-06
user_setup: []
must_haves:
  truths:
    - "BTL-01: BlueBattle* tables are created by migration and can persist raw/nullable battle state."
    - "Battle persistence does not create or update normal Blue score, history, Dani, recent/favorite, shop, or Green AI Battle state."
  artifacts:
    - path: "Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.cs"
      provides: "BlueBattle* table migration"
    - path: "Tests/Blue/BlueBattlePersistenceTests.cs"
      provides: "SQLite persistence and state-separation regression tests"
  key_links:
    - from: "Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.cs"
      to: "Infrastructure/Persistence/TaikoDbContext.Blue.cs"
      via: "schema matches BlueBattle* mappings"
      pattern: "BlueBattle"
---

<objective>
Add the Blue battle EF migration and persistence tests.

Purpose: The entity shape from 05-04 must become real SQLite schema with tests proving state separation.
Output: `AddBlueBattleState` migration, model snapshot update, and persistence tests.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
@.planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
@Infrastructure/Persistence/TaikoDbContext.Blue.cs
@Infrastructure/Persistence/Migrations/20260528181315_AddBlueItemShopState.cs
@Tests/Blue/BlueHandlerFixture.cs
@Tests/Blue/BlueBattlePersistenceShapeTests.cs
</context>

<scope>
In scope: migration files, model snapshot, SQLite persistence tests.

Out of scope: changing entity shape beyond defects found by migration or tests; if shape changes are needed, update 05-04-owned files and preserve the same safety rules.
</scope>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Blue battle state persistence | Covered by actual migration and SQLite tests. |
| REQ | BTL-01 | Covered by BlueBattle* table creation and persistence. |
| REQ | BTL-06 | Covered by tests that normal Blue and Green state remain untouched. |
| RESEARCH | Blue-owned persistence may store raw state while unsafe behavior stays blocked | Covered by migration-only state storage and no runtime emission. |
| CONTEXT | D-12, D-13, D-17, D-18 | Covered by persistence tests and no normal-state writes. |
</source_audit>

<tasks>

<task id="05-05-T1" type="auto" tdd="true">
  <name>Task 1: Generate migration and prove BlueBattle persistence isolation</name>
  <files>Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.cs, Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.Designer.cs, Infrastructure/Persistence/Migrations/TaikoDbContextModelSnapshot.cs, Tests/Blue/BlueBattlePersistenceTests.cs</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
    - .planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
    - Infrastructure/Persistence/TaikoDbContext.Blue.cs
    - Infrastructure/Persistence/Migrations/20260528181315_AddBlueItemShopState.cs
    - Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.cs
    - Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.Designer.cs
    - Infrastructure/Persistence/Migrations/TaikoDbContextModelSnapshot.cs
    - Tests/Blue/BlueHandlerFixture.cs
    - Tests/Blue/BlueBattlePersistenceShapeTests.cs
    - Tests/Blue/BlueBattlePersistenceTests.cs
  </read_first>
  <behavior>
    - Test 1: in-memory SQLite can create and persist each BlueBattle* table for a BAID-scoped user.
    - Test 2: creating battle state leaves `SongPlayDataBlue`, `SongBestDataBlue`, `BlueRecentSongs`, `BlueFavoriteSongs`, `DanScoreDataBlue`, `DanStageScoreDataBlue`, and Green AI Battle tables unchanged.
    - Test 3: unresolved fields remain null or absent before client-reported values are stored.
  </behavior>
  <action>
    Generate an EF migration named `AddBlueBattleState` from the 05-04 entity/mapping shape. Verify the migration only creates BlueBattle* tables and FKs to `UserData.Baid`; it must not alter Green tables or normal Blue score/crown/history/Dani/shop tables. Add SQLite persistence tests that create a user, insert representative BlueBattle user/NPC/token/stage/release rows, save, reload, and assert normal Blue and Green state are unchanged.
  </action>
  <acceptance_criteria>
    - EF migration `AddBlueBattleState` exists with migration, designer, and snapshot updates.
    - The migration creates only BlueBattle* tables and BAID/user relationships needed by 05-04.
    - SQLite tests persist and reload representative BlueBattle user, NPC, token, stage, and release rows.
    - Tests prove normal Blue score/best/recent/favorite/Dani state and Green AI Battle state are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>$migrations = dotnet ef migrations list --project Infrastructure --startup-project Host | Out-String; if ($migrations -notmatch 'AddBlueBattleState') { throw 'AddBlueBattleState migration missing' }</automated>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattlePersistenceTests</automated>
  </verify>
  <done>`AddBlueBattleState` exists and focused tests prove BlueBattle persistence without cross-era or normal Blue state writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF migration -> operator SQLite | Schema updates are applied to local durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | migration schema | mitigate | Migration creates only BlueBattle* tables and tests normal Blue/Green tables remain untouched. |
| T-05-05-02 | Denial of Service | null/default handling | mitigate | Tests assert unresolved fields are null or absent rather than unsafe defaults. |
| T-05-05-SC | Tampering | package installs | accept | No npm, pip, cargo, or NuGet package install is planned. |
</threat_model>

<verification>
- `dotnet ef migrations list --project Infrastructure --startup-project Host`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattlePersistenceTests`
</verification>

<success_criteria>
- Migration files exist and include only BlueBattle* schema changes.
- Persistence tests pass and prove normal Blue/Green separation.
</success_criteria>

<output>
Create `.planning/phases/05-blue-battle-runtime-support/05-05-SUMMARY.md` when done.
</output>
