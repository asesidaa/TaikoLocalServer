---
phase: 05-blue-battle-runtime-support
plan: "05-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
  - Domain/Entities/BlueBattleUserState.cs
  - Domain/Entities/BlueBattleNpcState.cs
  - Domain/Entities/BlueBattleTokenState.cs
  - Domain/Entities/BlueBattleStageResult.cs
  - Domain/Entities/BlueBattleReleaseState.cs
  - Application/Abstractions/ITaikoDbContext.Blue.cs
  - Infrastructure/Persistence/TaikoDbContext.Blue.cs
  - Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.cs
  - Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.Designer.cs
  - Infrastructure/Persistence/Migrations/TaikoDbContextModelSnapshot.cs
  - Application/Catalog/Blue/BlueBattleCatalog.cs
  - Application/Abstractions/IBlueCatalog.cs
  - Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs
  - Infrastructure/GameDataCatalog/Blue/BlueBattleDataLoader.cs
  - Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs
  - Tests/Blue/BlueBattleEvidenceGateTests.cs
  - Tests/Blue/BlueBattlePersistenceTests.cs
  - Tests/Blue/BlueBattleCatalogLoaderTests.cs
autonomous: true
requirements:
  - BTL-01
  - BTL-06
requirements_addressed:
  - BTL-01
  - BTL-06
user_setup: []
must_haves:
  truths:
    - "BTL-01: Blue battle state has Blue-owned schema for user state, NPC state, token state, stage-result capture, release capture, stage assignment, boss life, and last-stage fields."
    - "BTL-06: Blue battle foundations do not depend on Green AI Battle implementation truth."
    - "D-01 D-02 D-03 D-04 D-05: every Phase 4 missing-evidence row is copied into a Phase 5 resolution matrix before any runtime plan relies on it."
    - "D-08 D-15 D-16 D-17: local XML is represented only as raw candidate data; no first-stage, token, boss-life, stage 33, row-count, or reward semantics are inferred."
  artifacts:
    - path: ".planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md"
      provides: "Phase 5 row-level evidence gate for all 26 Phase 4 missing-evidence rows"
    - path: "Domain/Entities/BlueBattleUserState.cs"
      provides: "Blue-owned nullable battle user state root"
    - path: "Domain/Entities/BlueBattleNpcState.cs"
      provides: "Blue-owned NPC progress and selected-special state"
    - path: "Domain/Entities/BlueBattleTokenState.cs"
      provides: "Blue-owned token state without token reward semantics"
    - path: "Domain/Entities/BlueBattleStageResult.cs"
      provides: "Raw client-reported battle stage result capture"
    - path: "Domain/Entities/BlueBattleReleaseState.cs"
      provides: "Battle-owned release capture without normal unlock mirrors"
    - path: "Application/Catalog/Blue/BlueBattleCatalog.cs"
      provides: "Raw battle XML inventory surface with no menu/default/progression decisions"
  key_links:
    - from: ".planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md"
      to: ".planning/phases/04-blue-battle-evidence-and-design/04-03-BLUE-BATTLE-DESIGN-GATE.md"
      via: "one row per Phase 4 MISSING_EVIDENCE item"
      pattern: "MISSING_EVIDENCE"
    - from: "Application/Abstractions/ITaikoDbContext.Blue.cs"
      to: "Infrastructure/Persistence/TaikoDbContext.Blue.cs"
      via: "BlueBattle* DbSets and EF mappings"
      pattern: "BlueBattle(UserState|NpcState|TokenState|StageResult|ReleaseState)"
    - from: "Application/Abstractions/IBlueCatalog.cs"
      to: "Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs"
      via: "BlueBattleCatalog property"
      pattern: "BattleCatalog"
---

<objective>
Create the safe Blue battle foundation for Phase 05.

Purpose: Later protocol plans need a row-level evidence gate, Blue-owned persistence, and raw data boundaries before they can touch battleuserdata, initialdata, or playresult runtime behavior.
Output: `05-RESOLUTION.md`, Blue battle entities/DbSets/migration, raw battle catalog boundary, and focused tests.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-blue-battle-runtime-support/05-CONTEXT.md
@.planning/phases/05-blue-battle-runtime-support/05-RESEARCH.md
@.planning/phases/05-blue-battle-runtime-support/05-VALIDATION.md
@.planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
@.planning/phases/04-blue-battle-evidence-and-design/04-03-BLUE-BATTLE-DESIGN-GATE.md
@.planning/phases/01-blue-a6-item-shop-and-unlocking/01-02-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@Application/Abstractions/ITaikoDbContext.Blue.cs
@Infrastructure/Persistence/TaikoDbContext.Blue.cs
@Infrastructure/Persistence/Migrations/20260528181315_AddBlueItemShopState.cs
@Application/Abstractions/IBlueCatalog.cs
@Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs
@Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs
@Tests/Blue/BlueHandlerFixture.cs
@Tests/Blue/BlueA6SourceGuardTests.cs
</context>

<scope>
In scope: row-resolution artifact, Blue-owned schema and migration, raw battle data catalog boundary, tests proving omission/gating and Green separation.

Out of scope: emitting battleuserdata defaults, advertising battle availability, deriving row counts from XML, applying token rewards, assigning stages, using stage 33, computing boss-life defaults, or mirroring ReleaseBattleData into normal Blue unlocks.
</scope>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Blue battle mode has Blue-owned persistence and protocol behavior for battle userdata, initial data, battle playresult, progression, unlocks, rewards, and readback. | This plan covers the Blue-owned foundation and gate; protocol behavior is covered by 05-02 and 05-03. |
| REQ | BTL-01 | Covered by BlueBattle* entities, DbSets, EF mapping, migration, and persistence tests. |
| REQ | BTL-06 | Covered by Blue ownership, no Green AI Battle dependencies, and source-aware tests. |
| RESEARCH | Primary recommendation: 05-01 may add Blue-owned persistence/catalog foundations and raw capture surfaces while unsafe behavior remains blocked. | Implemented exactly; runtime consumers are not added here. |
| CONTEXT | D-01 through D-05 | Task 1 creates the Phase 5 resolution matrix with one row per Phase 4 missing-evidence item. |
| CONTEXT | D-08, D-15, D-16, D-17 | Task 3 blocks XML-derived byte widths, first/next/last-stage, boss-life, stage 33, token, and reward semantics. |
</source_audit>

<must_haves>
- `05-RESOLUTION.md` exists and records all 26 Phase 4 `MISSING_EVIDENCE` rows with current Phase 5 status, evidence source, allowed runtime use, and blocker text.
- Blue battle state is stored only in BlueBattle* tables and is nullable or raw-capture where defaults remain unresolved.
- Raw XML catalog code records file availability/counts/hash-oriented metadata only and has no consumer that advertises battle or computes progression.
- Focused tests run with `dotnet test Tests/Tests.csproj --filter BlueBattle`.
</must_haves>

<tasks>

<task id="05-01-T1" type="auto" tdd="true">
  <name>Task 1: Create the Phase 5 row-resolution gate artifact</name>
  <read_first>
    .planning/phases/05-blue-battle-runtime-support/05-CONTEXT.md
    .planning/phases/05-blue-battle-runtime-support/05-RESEARCH.md
    .planning/phases/05-blue-battle-runtime-support/05-VALIDATION.md
    .planning/phases/04-blue-battle-evidence-and-design/04-03-BLUE-BATTLE-DESIGN-GATE.md
    Tests/Blue/BlueDocsTests.cs
  </read_first>
  <files>
    .planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
    Tests/Blue/BlueBattleEvidenceGateTests.cs
  </files>
  <behavior>
    - Test 1: the Phase 5 resolution artifact contains exactly 26 rows copied from the Phase 4 `MISSING_EVIDENCE` table.
    - Test 2: every row has `Phase 5 Status`, `Evidence Source`, `Runtime Use`, and `Next Gate` cells.
    - Test 3: no row with `STILL_MISSING`, `NEEDS_USER_APPROVAL`, or `DEFER_RUNTIME_USE` is marked runtime-allowed.
    - Test 4: rows for D-10/D-12 battle playresult classification are the only runtime-allowed branch rows unless explicit proof/approval is recorded.
  </behavior>
  <action>
    Create `05-RESOLUTION.md` as the authoritative Phase 5 resolution matrix per D-01, D-02, D-03, D-04, and D-05. Copy all 26 rows from `04-03-BLUE-BATTLE-DESIGN-GATE.md` and add Phase 5 research statuses from `05-RESEARCH.md`, including `PROVEN` for `InitialdatacheckResponse.is_battleplay` safe optional false/omission behavior, `PROVEN` for the D-10/D-12 battle playresult branch trigger, `NEEDS_USER_APPROVAL` for `battle_bonds_lv_cap`, `STILL_MISSING_IDA_EVIDENCE` for unresolved battleuserdata widths/defaults/row counts, and `DEFER_RUNTIME_USE` for unresolved release/reward/token/progression effects. Add a `Runtime Use` value of `blocked` unless the exact row is proven or explicitly approved; for `is_battleplay`, record that field plumbing is allowed but advertising true remains blocked by D-07 until all required rows clear. Add `Tests/Blue/BlueBattleEvidenceGateTests.cs` to parse the markdown table and enforce these invariants.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleEvidenceGateTests</automated>
  </verify>
  <acceptance_criteria>
    - `05-RESOLUTION.md` cites `04-03-BLUE-BATTLE-DESIGN-GATE.md` and `05-RESEARCH.md`.
    - Exactly 26 unresolved Phase 4 rows are present.
    - D-01 through D-05 are named in the artifact.
    - Rows with missing evidence are machine-checked as blocked for runtime use.
  </acceptance_criteria>
  <done>The Phase 5 row-resolution gate exists and fails closed before runtime behavior can rely on unresolved rows.</done>
</task>

<task id="05-01-T2" type="auto" tdd="true">
  <name>Task 2: Add Blue battle persistence schema and raw state tests</name>
  <read_first>
    .planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
    .planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
    Domain/Entities/BlueShopSeasonState.cs
    Domain/Entities/BlueShopItemState.cs
    Domain/Entities/UserSaveDataBlue.cs
    Application/Abstractions/ITaikoDbContext.Blue.cs
    Infrastructure/Persistence/TaikoDbContext.Blue.cs
    Infrastructure/Persistence/Migrations/20260528181315_AddBlueItemShopState.cs
    Tests/Blue/BlueHandlerFixture.cs
  </read_first>
  <files>
    Domain/Entities/BlueBattleUserState.cs
    Domain/Entities/BlueBattleNpcState.cs
    Domain/Entities/BlueBattleTokenState.cs
    Domain/Entities/BlueBattleStageResult.cs
    Domain/Entities/BlueBattleReleaseState.cs
    Application/Abstractions/ITaikoDbContext.Blue.cs
    Infrastructure/Persistence/TaikoDbContext.Blue.cs
    Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.cs
    Infrastructure/Persistence/Migrations/{timestamp}_AddBlueBattleState.Designer.cs
    Infrastructure/Persistence/Migrations/TaikoDbContextModelSnapshot.cs
    Tests/Blue/BlueBattlePersistenceTests.cs
  </files>
  <behavior>
    - Test 1: creating a Blue battle user state row does not create or update `SongPlayDataBlue`, `SongBestDataBlue`, `BlueRecentSongs`, `BlueFavoriteSongs`, `DanScoreDataBlue`, or Green AI Battle rows.
    - Test 2: new Blue battle state stores unresolved fields as null or absent rows, not zero-filled byte arrays or guessed IDs.
    - Test 3: NPC, token, stage-result, and release rows use BAID-scoped BlueBattle* tables with `UserDatum.Baid` foreign keys.
  </behavior>
  <action>
    Add Blue-owned battle entities and EF mappings for BTL-01 without seeding defaults. `BlueBattleUserState` must key by `Baid` and expose nullable persisted fields for `ReleaseInfoFlg`, `ReleaseBattleStageFlg`, `LastBattleStageId`, `LastBossLife`, `LastNpcId`, `AssignStageId`, and timestamps. `BlueBattleNpcState` must key by `(Baid, NpcId)` and store observed `TotalExp`, `MaxDpn`, `NpcCostumeId`, `NpcCostumeFlg`, `LastSelectSpecial1`, `LastSelectSpecial2`, `LastSelectSpecial3`, `ReleaseSpecialFlg`, and `BondsLv` as nullable or raw persisted values. `BlueBattleTokenState` must key by `(Baid, TokenId)` and store `TokenValue` without assigning token reward meaning per D-17. `BlueBattleStageResult` must capture client-reported battle stage and NPC result fields, including observed mode values from D-11, without normal score/crown/history semantics. `BlueBattleReleaseState` must capture `ReleaseBattleData` release kind/id/token rows as battle-owned raw releases and must not mirror to normal unlock state per D-18. Extend `ITaikoDbContext.Blue.cs`, `TaikoDbContext.Blue.cs`, and generate `AddBlueBattleState` migration files plus model snapshot updates. Add persistence tests that verify Blue-owned table creation, null/absent defaults, and no normal Blue or Green table mutation.
  </action>
  <verify>
    <automated>$migrations = dotnet ef migrations list --project Infrastructure --startup-project Host | Out-String; if ($migrations -notmatch 'AddBlueBattleState') { throw 'AddBlueBattleState migration missing' }</automated>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattlePersistenceTests</automated>
  </verify>
  <acceptance_criteria>
    - The migration creates only `BlueBattle*` tables and does not alter Green or normal Blue score/crown/history/Dani tables.
    - All unresolved startup/default fields are nullable or absent until a later gated plan supplies proof or approval.
    - `BlueBattleReleaseState` stores release observations as battle-owned data and does not call normal unlock helpers.
    - Focused persistence tests pass.
  </acceptance_criteria>
  <done>BTL-01 has Blue-owned persistence surfaces that can store raw and approved battle state without unsafe defaults.</done>
</task>

<task id="05-01-T3" type="auto" tdd="true">
  <name>Task 3: Add raw battle catalog/data boundary without semantic consumers</name>
  <read_first>
    .planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
    .planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
    .planning/phases/04-blue-battle-evidence-and-design/04-02-BATTLE-DATA-INVENTORY.md
    Application/Abstractions/IBlueCatalog.cs
    Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs
    Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs
    Infrastructure/GameDataCatalog/Blue/BlueItemShopLoader.cs
    Tests/Blue/BlueCatalogLoaderTests.cs
  </read_first>
  <files>
    Application/Catalog/Blue/BlueBattleCatalog.cs
    Application/Abstractions/IBlueCatalog.cs
    Infrastructure/GameDataCatalog/Blue/BlueGameDataPaths.cs
    Infrastructure/GameDataCatalog/Blue/BlueBattleDataLoader.cs
    Infrastructure/GameDataCatalog/Blue/BlueEraGameDataCatalog.cs
    Tests/Blue/BlueBattleCatalogLoaderTests.cs
  </files>
  <behavior>
    - Test 1: the loader can inventory the five known local battle XML files when present and exposes only raw file/count/status metadata.
    - Test 2: missing battle XML produces a disabled/raw-unavailable catalog state and does not enable battle advertisement.
    - Test 3: no production consumer reads the battle catalog to set `is_battleplay`, battleuserdata response rows, stage assignment, tokens, boss life, stage 33, or rewards.
  </behavior>
  <action>
    Add `BlueBattleCatalog` as a raw data boundary for IDA-proven local file consumption while preserving D-15. Add `BattleRoot` and file-specific path constants to `BlueGameDataPaths`. Implement `BlueBattleDataLoader` to inspect `battleadjsetting.xml`, `battlenpcinfo.xml`, `battlestageinfo.xml`, `battlesupportinfo.xml`, and `battletokeninfo.xml`, parse enough XML to record file presence, parse success, row counts already documented by `04-02-BATTLE-DATA-INVENTORY.md`, and stable file identifiers without copying local XML into the repo. Wire the raw catalog into `IBlueCatalog` and `BlueEraGameDataCatalog` as `BattleCatalog`. Do not add any consumer in `GetInitialDataQuery.Blue.cs`, `BattleUserDataController.cs`, `BattleUserDataMappers.cs`, `PlayResultMappers.cs`, or `UpdatePlayResultCommand.Blue.cs`. Do not compute first-stage, next-stage, last-stage, stage `33`, boss-life, token reward, NPC row-count, or battle advertisement behavior from XML.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleCatalogLoaderTests</automated>
    <automated>$forbidden = 'IsBattleplay','AssignStageId','LastBossLife','BattleBondsLvCap','ReleaseBattleStageFlg','ReleaseBattleSpecialFlg'; $files = 'Application/Handlers/GetInitialDataQuery.Blue.cs','Adapters.GameProtocol.Blue/Mappers/BattleUserDataMappers.cs','Adapters.GameProtocol.Blue/Controllers/BattleUserDataController.cs','Adapters.GameProtocol.Blue/Mappers/PlayResultMappers.cs','Application/Handlers/UpdatePlayResultCommand.Blue.cs'; foreach ($file in $files) { if (Test-Path -LiteralPath $file) { $source = Get-Content -LiteralPath $file -Raw; foreach ($token in $forbidden) { if ($source -match 'BattleCatalog' -and $source -match $token) { throw "$file derives $token from BattleCatalog" } } } }</automated>
  </verify>
  <acceptance_criteria>
    - The raw battle catalog can inventory local operator-supplied XML but cannot enable battle behavior by itself.
    - No local XML file contents or derived JSON fixtures are committed.
    - Tests prove missing or malformed local battle XML does not cause battle advertisement or default emission.
    - D-15, D-16, and D-17 remain blocked for semantic runtime behavior.
  </acceptance_criteria>
  <done>Blue battle data has a safe raw catalog boundary with no runtime semantic overreach.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 4 evidence -> Phase 5 implementation | Planning artifacts decide which battle rows runtime code may use. |
| Local XML -> runtime catalog | Operator-supplied battle XML enters server catalog loading. |
| Cabinet protobuf -> SQLite | Later battle requests will write Blue battle state by BAID. |
| Blue battle state -> shared SQLite database | Blue battle tables coexist with normal Blue, Green, Nijiiro, and identity tables. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | high | row-resolution gate | mitigate | Task 1 creates a machine-checked 26-row matrix; unresolved rows are blocked for runtime use. |
| T-05-01-02 | Tampering | high | Blue battle EF schema | mitigate | Task 2 uses BlueBattle* tables and tests no writes to normal Blue or Green battle state. |
| T-05-01-03 | Denial of Service | medium | protobuf defaults | mitigate | Task 2 stores unresolved fields as null/absent; later mappers must omit unresolved optional fields. |
| T-05-01-04 | Tampering | high | local XML catalog | mitigate | Task 3 exposes raw inventory only and tests that XML cannot drive battle advertisement, stage assignment, boss life, tokens, or rewards. |
| T-05-01-05 | Information Disclosure | medium | local XML data | mitigate | Task 3 records bounded metadata and does not commit local binary/XML source data or full XML dumps. |
| T-05-01-SC | Tampering | medium | package installs | accept | No npm, pip, cargo, or NuGet package install is planned. |
</threat_model>

<verification>
- `dotnet test Tests/Tests.csproj --filter BlueBattle`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleEvidenceGateTests`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattlePersistenceTests`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleCatalogLoaderTests`
- Phase closeout still requires `dotnet test Tests/Tests.csproj` and `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build-phase05"`.
</verification>

<success_criteria>
- `05-RESOLUTION.md` fail-closed matrix exists with all 26 rows.
- Blue battle persistence exists under Blue-owned tables and stores unresolved defaults as null or absent rows.
- Raw battle XML inventory exists without deriving runtime behavior.
- Focused BlueBattle tests pass.
</success_criteria>

<output>
Create `.planning/phases/05-blue-battle-runtime-support/05-01-SUMMARY.md` when done.
</output>
