---
phase: 05-blue-battle-runtime-support
plan: "05-04"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - Domain/Entities/BlueBattleUserState.cs
  - Domain/Entities/BlueBattleNpcState.cs
  - Domain/Entities/BlueBattleTokenState.cs
  - Domain/Entities/BlueBattleStageResult.cs
  - Domain/Entities/BlueBattleReleaseState.cs
  - Application/Abstractions/ITaikoDbContext.Blue.cs
  - Infrastructure/Persistence/TaikoDbContext.Blue.cs
  - Tests/Blue/BlueBattlePersistenceShapeTests.cs
autonomous: true
requirements:
  - BTL-01
  - BTL-06
user_setup: []
must_haves:
  truths:
    - "BTL-01: Blue battle has Blue-owned entity and DbSet surfaces for user state, NPC state, token state, stage-result capture, and release capture."
    - "Unresolved defaults are nullable or absent, not zero-filled or XML-derived."
    - "BTL-06: schema shape does not use Green AI Battle or normal Blue score/history tables as battle-owned state."
  artifacts:
    - path: "Domain/Entities/BlueBattleUserState.cs"
      provides: "Blue battle user root state"
    - path: "Domain/Entities/BlueBattleNpcState.cs"
      provides: "Battle NPC raw/nullable state"
    - path: "Domain/Entities/BlueBattleTokenState.cs"
      provides: "Battle token raw state without reward semantics"
    - path: "Domain/Entities/BlueBattleStageResult.cs"
      provides: "Raw battle stage result capture"
    - path: "Domain/Entities/BlueBattleReleaseState.cs"
      provides: "Battle-owned release capture"
  key_links:
    - from: "Application/Abstractions/ITaikoDbContext.Blue.cs"
      to: "Infrastructure/Persistence/TaikoDbContext.Blue.cs"
      via: "BlueBattle* DbSets and EF mappings"
      pattern: "BlueBattle(UserState|NpcState|TokenState|StageResult|ReleaseState)"
---

<objective>
Add Blue battle entity and DbContext shape.

Purpose: Persistence plans need Blue-owned schema surfaces before migrations and protocol handlers can store raw battle state.
Output: BlueBattle* entities, DbSets, mappings, and shape tests.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-blue-battle-runtime-support/05-CONTEXT.md
@.planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
@.planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
@Domain/Entities/BlueShopSeasonState.cs
@Domain/Entities/BlueShopItemState.cs
@Domain/Entities/UserSaveDataBlue.cs
@Application/Abstractions/ITaikoDbContext.Blue.cs
@Infrastructure/Persistence/TaikoDbContext.Blue.cs
@Tests/Blue/BlueHandlerFixture.cs
</context>

<scope>
In scope: entity classes, DbSets, EF mappings, and shape tests.

Out of scope: EF migration files and database integration tests; those are in 05-05.
</scope>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Blue battle state persistence | Covered by Blue-owned entity and mapping surfaces. |
| REQ | BTL-01 | Covered by entity/DbSet shape for all battle state groups named in the requirement. |
| REQ | BTL-06 | Covered by Blue-owned surfaces and negative checks against Green AI Battle state. |
| RESEARCH | persistence foundations are allowed as raw/nullable state | Covered by nullable fields and no seeded defaults. |
| CONTEXT | D-01, D-08, D-09, D-13, D-17, D-18 | Covered by raw/nullable fields and battle-owned release/token state. |
</source_audit>

<tasks>

<task id="05-04-T1" type="auto" tdd="true">
  <name>Task 1: Add BlueBattle entity and DbContext shape</name>
  <files>Domain/Entities/BlueBattleUserState.cs, Domain/Entities/BlueBattleNpcState.cs, Domain/Entities/BlueBattleTokenState.cs, Domain/Entities/BlueBattleStageResult.cs, Domain/Entities/BlueBattleReleaseState.cs, Application/Abstractions/ITaikoDbContext.Blue.cs, Infrastructure/Persistence/TaikoDbContext.Blue.cs, Tests/Blue/BlueBattlePersistenceShapeTests.cs</files>
  <behavior>
    - Test 1: `ITaikoDbContext.Blue.cs` and `TaikoDbContext.Blue.cs` expose BlueBattle* sets and mappings.
    - Test 2: unresolved fields are nullable or represented by absent rows.
    - Test 3: entity/mapping source does not reference Green AI Battle tables or normal Blue score/history/Dani tables as battle storage.
  </behavior>
  <action>
    Add `BlueBattleUserState` keyed by `Baid` with nullable persisted fields for release flags, last/assigned stage ids, boss life, NPC id, and timestamps. Add `BlueBattleNpcState` keyed by `(Baid, NpcId)` for observed total exp, max DPN, costume flags, selected specials, release special flags, and bonds level as nullable or raw values. Add `BlueBattleTokenState` keyed by `(Baid, TokenId)` with raw `TokenValue` only per D-17. Add `BlueBattleStageResult` for client-reported battle stage/NPC result capture including observed mode values from D-11/D-14. Add `BlueBattleReleaseState` for battle-owned `ReleaseBattleData` observations per D-18. Extend Blue `ITaikoDbContext` and `TaikoDbContext` mappings with cascade FK to `UserDatum.Baid`. Add shape tests before implementation, then make them pass.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattlePersistenceShapeTests</automated>
  </verify>
  <done>BlueBattle* entity and DbContext shape exists without unsafe defaults or Green/normal Blue storage leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blue battle state -> shared SQLite database | Battle tables coexist with normal Blue, Green, Nijiiro, and identity tables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Blue battle EF shape | mitigate | Use BlueBattle* tables and tests that reject normal Blue or Green battle storage. |
| T-05-04-02 | Denial of Service | unresolved defaults | mitigate | Use nullable fields or absent rows instead of guessed zero-filled defaults. |
| T-05-04-SC | Tampering | package installs | accept | No npm, pip, cargo, or NuGet package install is planned. |
</threat_model>

<verification>
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattlePersistenceShapeTests`
</verification>

<success_criteria>
- BlueBattle* entities and DbSets exist.
- EF mapping is Blue-owned and BAID-scoped.
- No unsafe default values are encoded in schema shape.
</success_criteria>

<output>
Create `.planning/phases/05-blue-battle-runtime-support/05-04-SUMMARY.md` when done.
</output>
