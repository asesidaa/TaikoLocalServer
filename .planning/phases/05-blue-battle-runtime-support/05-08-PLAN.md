---
phase: 05-blue-battle-runtime-support
plan: "05-08"
type: execute
wave: 5
depends_on:
  - "05-07"
files_modified:
  - Application/Dtos/CommonInitialDataCheckResponse.Blue.cs
  - Application/Handlers/GetInitialDataQuery.Blue.cs
  - Adapters.GameProtocol.Blue/Mappers/InitialDataMappers.cs
  - Tests/Blue/BlueInitialDataTests.cs
autonomous: true
requirements:
  - BTL-03
  - BTL-06
user_setup: []
must_haves:
  truths:
    - "BTL-03 D-07: `initialdatacheck.php` does not advertise battle with `is_battleplay=true` until required rows clear."
    - "Initialdata battle fields use generated optional presence and exact row-approved byte/value behavior only."
  artifacts:
    - path: "Application/Dtos/CommonInitialDataCheckResponse.Blue.cs"
      provides: "Nullable battle initialdata DTO fields"
    - path: "Application/Handlers/GetInitialDataQuery.Blue.cs"
      provides: "Row-gated battle initialdata values"
    - path: "Adapters.GameProtocol.Blue/Mappers/InitialDataMappers.cs"
      provides: "Presence-aware Blue wire mapper"
  key_links:
    - from: "Application/Handlers/GetInitialDataQuery.Blue.cs"
      to: "Adapters.GameProtocol.Blue/Mappers/InitialDataMappers.cs"
      via: "nullable DTO fields and generated ShouldSerialize presence"
      pattern: "IsBattleplay|ReleaseBattleStageFlg|ReleaseBattleSpecialFlg|BattleBondsLvCap"
---

<objective>
Wire Blue initialdata battle fields behind row gates.

Purpose: Blue initialdata can carry battle fields only where 05-02 resolved exact emitted behavior; otherwise battle advertisement remains false or omitted.
Output: Initialdata DTO/query/mapper updates and tests for omitted and any approved-positive behavior.
</objective>

<user_decision_override date="2026-05-31">
The user rejected omitted/fail-closed battle initialdata for this stage and approved
data-derived "unlock all" battle content. Blue initialdata must emit concrete
false/default battle fields when the required local battle XML set is unavailable,
and must emit parsed values when all required Blue battle XML files exist and parse.
Do not hardcode the current IDs, byte values, or cap; derive them through the Blue
battle catalog boundary.
</user_decision_override>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
@.planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
@Application/Dtos/CommonInitialDataCheckResponse.Blue.cs
@Application/Handlers/GetInitialDataQuery.Blue.cs
@Adapters.GameProtocol.Blue/Mappers/InitialDataMappers.cs
@Adapters.GameProtocol.Blue/Wire/Game.cs
@Tests/Blue/BlueInitialDataTests.cs
</context>

<scope>
In scope: initialdata battle field plumbing and tests.

Out of scope: battleuserdata readback; implemented in 05-07.
</scope>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Initial data battle protocol behavior | Covered by nullable battle fields and mapper presence checks. |
| REQ | BTL-03 | Covered by battle advertisement and release flag gating. |
| REQ | BTL-06 | Covered by Blue-owned DTO/query/mapper/test files. |
| RESEARCH | `is_battleplay` is consumed but D-07 blocks true until required rows clear | Covered by false/omitted default and row-approved positive behavior only. |
| CONTEXT | D-06, D-07, D-08 | Covered by exact width/default proof or named approval requirement. |
</source_audit>

<tasks>

<task id="05-08-T1" type="auto" tdd="true">
  <name>Task 1: Keep initialdata battle advertisement row-gated</name>
  <files>Application/Dtos/CommonInitialDataCheckResponse.Blue.cs, Application/Handlers/GetInitialDataQuery.Blue.cs, Adapters.GameProtocol.Blue/Mappers/InitialDataMappers.cs, Tests/Blue/BlueInitialDataTests.cs</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
    - .planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
    - Application/Dtos/CommonInitialDataCheckResponse.Blue.cs
    - Application/Handlers/GetInitialDataQuery.Blue.cs
    - Adapters.GameProtocol.Blue/Mappers/InitialDataMappers.cs
    - Adapters.GameProtocol.Blue/Wire/Game.cs
    - Tests/Blue/BlueInitialDataTests.cs
  </read_first>
  <behavior>
    - Test 1: when rows remain blocked, Blue initialdata keeps `ShouldSerializeIsBattleplay`, `ShouldSerializeReleaseBattleStageFlg`, `ShouldSerializeReleaseBattleSpecialFlg`, and `ShouldSerializeBattleBondsLvCap` false.
    - Test 2: if a row-specific approval exists, only that exact approved field/value/byte width is emitted and tested.
    - Test 3: `is_battleplay=true` is impossible unless D-07 is satisfied by all required rows in `05-RESOLUTION.md`.
  </behavior>
  <action>
    Keep or add nullable battle initialdata DTO fields and mapper presence behavior. Update `GetInitialDataQuery.Blue.cs` only to consume row-approved battle advertisement values from 05-02. If `05-RESOLUTION.md` still blocks rows 1, 3, 4, 5, 6, 12, 15, 16, or 25, keep `IsBattleplay`, `ReleaseBattleStageFlg`, `ReleaseBattleSpecialFlg`, and `BattleBondsLvCap` null. If 05-02 approved exact values, implement only those fields and add byte-length/value assertions matching the approved row text. Do not use XML maximum IDs, XML row counts, wiki bonds cap text, or zero-filled byte arrays as a source.
  </action>
  <acceptance_criteria>
    - Blocked rows keep `IsBattleplay`, `ReleaseBattleStageFlg`, `ReleaseBattleSpecialFlg`, and `BattleBondsLvCap` null or omitted.
    - Any emitted battle initialdata field has exact row-specific proof or named approval in `05-RESOLUTION.md`.
    - `is_battleplay=true` cannot occur unless D-07 is satisfied by all required rows.
    - Tests prove XML maxima, XML row counts, wiki-only bonds cap text, and zero-filled byte arrays are not used as sources.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueInitialDataTests</automated>
    <automated>dotnet test Tests/Tests.csproj --filter BlueBattle</automated>
  </verify>
  <done>Initialdata battle fields are emitted only where exact row proof or named approval exists; unresolved battle advertisement remains omitted or false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| initialdata response -> client battle menu | Battle advertisement can expose client battle flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | `is_battleplay` | mitigate | Tests keep advertisement false/omitted until D-07 rows clear. |
| T-05-08-02 | Denial of Service | release flag byte arrays | mitigate | Emit only exact approved widths/defaults; otherwise omit. |
| T-05-08-SC | Tampering | package installs | accept | No npm, pip, cargo, or NuGet package install is planned. |
</threat_model>

<verification>
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueInitialDataTests`
- `dotnet test Tests/Tests.csproj --filter BlueBattle`
</verification>

<success_criteria>
- `is_battleplay` is not true unless D-07 is fully cleared.
- Initialdata battle fields use generated optional presence.
- Any positive emission has row-specific tests.
</success_criteria>

<output>
Create `.planning/phases/05-blue-battle-runtime-support/05-08-SUMMARY.md` when done.
</output>
