---
phase: 05-blue-battle-runtime-support
plan: "05-07"
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-05"
files_modified:
  - Application/Dtos/CommonBattleUserDataResponse.cs
  - Application/Handlers/GetBattleUserDataQuery.Blue.cs
  - Adapters.GameProtocol.Blue/Controllers/BattleUserDataController.cs
  - Adapters.GameProtocol.Blue/Mappers/BattleUserDataMappers.cs
  - Tests/Blue/BlueBattleUserDataTests.cs
  - Tests/Blue/BlueRouteSkeletonTests.cs
autonomous: true
requirements:
  - BTL-02
  - BTL-06
user_setup: []
must_haves:
  truths:
    - "BTL-02: `battleuserdata.php` returns `Result = 1` plus only row-approved persisted fields."
    - "Unresolved optional fields and repeated rows remain un-emitted."
    - "BTL-06: controller, query, mapper, DTO, and tests are Blue-owned."
  artifacts:
    - path: "Application/Dtos/CommonBattleUserDataResponse.cs"
      provides: "Application-level battleuserdata response DTO with nullable/gated fields"
    - path: "Application/Handlers/GetBattleUserDataQuery.Blue.cs"
      provides: "Blue-owned battleuserdata query"
    - path: "Adapters.GameProtocol.Blue/Mappers/BattleUserDataMappers.cs"
      provides: "Generated-presence-aware Blue wire mapper"
  key_links:
    - from: "Adapters.GameProtocol.Blue/Controllers/BattleUserDataController.cs"
      to: "Application/Handlers/GetBattleUserDataQuery.Blue.cs"
      via: "Mediator.Send(new GetBattleUserDataQuery(...))"
      pattern: "GetBattleUserDataQuery"
---

<objective>
Implement fail-closed Blue battleuserdata protocol behavior.

Purpose: Replace local success construction with a Mediator-backed Blue path that can read persisted battle state while honoring 05-02 row gates.
Output: DTO, query, controller, mapper, and focused tests.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/get-shit-done/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-blue-battle-runtime-support/05-RESOLUTION.md
@.planning/phases/05-blue-battle-runtime-support/05-PATTERNS.md
@Adapters.GameProtocol.Blue/Controllers/BattleUserDataController.cs
@Adapters.GameProtocol.Blue/Controllers/UserDataController.cs
@Adapters.GameProtocol.Blue/Wire/Game.cs
@Application/Abstractions/ITaikoDbContext.Blue.cs
@Tests/Blue/BlueRouteSkeletonTests.cs
</context>

<scope>
In scope: fail-closed battleuserdata readback path.

Out of scope: initialdata battle advertisement; split to 05-08.
</scope>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Battle userdata readback | Covered by Mediator-backed controller/query/mapper. |
| REQ | BTL-02 | Covered by result-only and row-approved optional emission behavior. |
| REQ | BTL-06 | Covered by Blue-owned surfaces and route guard update. |
| RESEARCH | battleuserdata defaults unresolved | Covered by omission unless 05-02 resolved exact rows. |
| CONTEXT | D-06, D-08, D-09 | Covered by nullable DTO fields and generated presence checks. |
</source_audit>

<tasks>

<task id="05-07-T1" type="auto" tdd="true">
  <name>Task 1: Add fail-closed battleuserdata query, mapper, and controller path</name>
  <files>Application/Dtos/CommonBattleUserDataResponse.cs, Application/Handlers/GetBattleUserDataQuery.Blue.cs, Adapters.GameProtocol.Blue/Controllers/BattleUserDataController.cs, Adapters.GameProtocol.Blue/Mappers/BattleUserDataMappers.cs, Tests/Blue/BlueBattleUserDataTests.cs, Tests/Blue/BlueRouteSkeletonTests.cs</files>
  <behavior>
    - Test 1: new-user battleuserdata response has `Result = 1` and unresolved optional fields return `ShouldSerialize*` false.
    - Test 2: `NpcDatas` and `AryTokenDatas` have no generated rows unless 05-02 row approval exists.
    - Test 3: `BattleUserDataController` sends `GetBattleUserDataQuery` through Mediator and contains no local response construction beyond mapping.
  </behavior>
  <action>
    Add `CommonBattleUserDataResponse` with nullable or collection fields matching only Blue `BattleUserDataResponse` schema names. Add `GetBattleUserDataQuery.Blue.cs` keyed by BAID and returning `Result = 1` with only persisted values whose exact row is allowed by `05-RESOLUTION.md` after 05-02. Do not read planning files at runtime; enforce row approval in tests by checking source and expected wire presence. Replace controller-local success construction with a Mediator-backed query and `BattleUserDataMappers.Map`. The mapper must set optional wire fields only when the common DTO field is non-null and must leave repeated rows empty unless the DTO explicitly contains approved rows. Update `BlueRouteSkeletonTests` for this implemented Blue endpoint only.
  </action>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleUserDataTests</automated>
    <automated>dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueRouteSkeletonTests</automated>
  </verify>
  <done>`battleuserdata.php` is Blue-owned, Mediator-backed, and emits only row-approved data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cabinet -> battleuserdata.php | Untrusted protobuf request enters Blue battle readback. |
| persisted Blue battle state -> protobuf response | Server state becomes client-visible battleuserdata fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Denial of Service | mapper optional fields | mitigate | Mapper emits optional fields only when non-null and tests unresolved rows remain omitted. |
| T-05-07-02 | Elevation of Privilege | Green AI Battle leakage | mitigate | Blue-owned files and later source guards reject Green AI Battle truth. |
| T-05-07-SC | Tampering | package installs | accept | No npm, pip, cargo, or NuGet package install is planned. |
</threat_model>

<verification>
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueBattleUserDataTests`
- `dotnet test Tests/Tests.csproj --filter FullyQualifiedName~BlueRouteSkeletonTests`
</verification>

<success_criteria>
- Battleuserdata is Mediator-backed.
- Unresolved battleuserdata fields and repeated rows are un-emitted.
- Route guard reflects only implemented Blue battle protocol behavior.
</success_criteria>

<output>
Create `.planning/phases/05-blue-battle-runtime-support/05-07-SUMMARY.md` when done.
</output>
