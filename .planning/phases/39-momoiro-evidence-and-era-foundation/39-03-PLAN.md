---
phase: 39-momoiro-evidence-and-era-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "39-02"
files_modified:
  - "Adapters.GameProtocol.Shared/GameProtocolApplicationParts.cs"
  - "Host/Program.cs"
  - "Host/Host.csproj"
  - "Host/Configurations/ServerSettings.json"
  - ".planning/phases/39-momoiro-evidence-and-era-foundation/39-VALIDATION.md"
autonomous: true
requirements:
  - MOFND-02
  - MOFND-03
requirements_addressed:
  - MOFND-02
  - MOFND-03
user_setup: []
must_haves:
  truths:
    - "MOFND-02: Host composes Momoiro only when enabled and removes the Momoiro adapter application part when disabled."
    - "MOFND-03: Shared startup/version remains in `/v01r00/chassis`; Momoiro direct-protobuf fallback is exact `/v04r00/chassis`."
  prohibitions:
    - "Do not add Momoiro controllers in this plan."
    - "Do not add Momoiro catalog registration, persistence, migrations, AdminApi, WebUI, or AC15 profile entries."
  artifacts:
    - path: "Adapters.GameProtocol.Shared/GameProtocolApplicationParts.cs"
      provides: "Momoiro disabled-application-part removal"
    - path: "Host/Program.cs"
      provides: "Momoiro DI registration and exact direct-protobuf fallback"
    - path: "Host/Host.csproj"
      provides: "Momoiro adapter reference and raw data output handling"
    - path: "Host/Configurations/ServerSettings.json"
      provides: "Momoiro enabled-era settings block"
  key_links:
    - from: "Host/Configurations/ServerSettings.json"
      to: "Host/Program.cs"
      via: "GameProtocolApplicationParts.ReadEnabledEras"
      pattern: "GameEra.Momoiro"
    - from: "Host/Program.cs"
      to: "Adapters.GameProtocol.Shared/GameProtocolApplicationParts.cs"
      via: "RemoveDisabledGameProtocolApplicationParts"
      pattern: "TaikoLocalServer.Adapters.GameProtocol.Momoiro"
---

<objective>
Wire Momoiro into Host composition and disabled-era route gating without adding route controllers.

Purpose: MOFND-02 requires Host settings, DI registration, application-part gating, and no enabled routes when disabled. MOFND-03 requires the shared startup/version routes to remain shared while Momoiro game posts use exact `/v04r00/chassis` direct-protobuf fallback.

Output: Host and shared application-part wiring that makes the Wave 0 settings/application-part tests green and prepares route exposure for Plan 39-04.
</objective>

<execution_context>
@H:/TaikoLocalServer/.codex/gsd-core/workflows/execute-plan.md
@H:/TaikoLocalServer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-momoiro-evidence-and-era-foundation/39-CONTEXT.md
@.planning/phases/39-momoiro-evidence-and-era-foundation/39-RESEARCH.md
@.planning/phases/39-momoiro-evidence-and-era-foundation/39-VALIDATION.md
@.planning/phases/39-momoiro-evidence-and-era-foundation/39-PATTERNS.md
@.planning/phases/39-momoiro-evidence-and-era-foundation/39-MOMOIRO-EVIDENCE.md
@Adapters.GameProtocol.Shared/GameProtocolApplicationParts.cs
@Adapters.GameProtocol.Momoiro/DependencyInjection.cs
@Adapters.GameProtocol.Momoiro/MomoiroRoutePrefixes.cs
@Host/Program.cs
@Host/Host.csproj
@Host/Configurations/ServerSettings.json
@Tests/Momoiro/MomoiroApplicationPartTests.cs
@Tests/Momoiro/MomoiroServerSettingsValidationTests.cs
</context>

## Artifacts This Phase Produces

This plan produces the Host and shared-gating artifacts for Momoiro: `GameProtocolApplicationParts` removal branch, Host enabled-era DI, exact direct-protobuf fallback, Host project reference/raw-data output handling, and the Momoiro settings block.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Momoiro Application-Part Gating</name>
  <files>Adapters.GameProtocol.Shared/GameProtocolApplicationParts.cs</files>
  <read_first>
    Adapters.GameProtocol.Shared/GameProtocolApplicationParts.cs
    Tests/Momoiro/MomoiroApplicationPartTests.cs
  </read_first>
  <behavior>
    - Disabled Momoiro removes the `TaikoLocalServer.Adapters.GameProtocol.Momoiro` application part.
    - Enabled Momoiro keeps the Momoiro application part.
    - Other era removal branches remain additive and unchanged.
  </behavior>
  <action>
    Add a `GameEra.Momoiro` branch to `RemoveDisabledGameProtocolApplicationParts` per D-07 that removes assembly name `TaikoLocalServer.Adapters.GameProtocol.Momoiro` when Momoiro is not enabled. Keep the helper centralized in `Adapters.GameProtocol.Shared`; do not add per-controller enablement checks. Preserve existing Green, Blue, Yellow, Red, White, Murasaki, Kimidori, and Nijiiro removal behavior.
  </action>
  <acceptance_criteria>
    The Wave 0 application-part tests pass through the production helper, and the disabled-route boundary is not duplicated elsewhere.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroApplicationPart"</automated>
  </verify>
  <done>Momoiro disabled-era application-part removal is implemented and covered by focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Host Registration And Exact Protobuf Fallback</name>
  <files>Host/Program.cs</files>
  <read_first>
    Host/Program.cs
    Adapters.GameProtocol.Momoiro/DependencyInjection.cs
    Adapters.GameProtocol.Momoiro/MomoiroRoutePrefixes.cs
    .planning/phases/39-momoiro-evidence-and-era-foundation/39-MOMOIRO-EVIDENCE.md
  </read_first>
  <behavior>
    - Host imports Momoiro only for adapter composition.
    - Host registers `AddGameProtocolMomoiro()` only when `enabledEras.Contains(GameEra.Momoiro)`.
    - `ShouldAssumeProtobufRequest` treats no-content-type posts under exactly `/v04r00/chassis` as protobuf and does not broaden other paths.
  </behavior>
  <action>
    Add `using TaikoLocalServer.Adapters.GameProtocol.Momoiro` to `Host/Program.cs`. Update the no-enabled-era fatal message to include Momoiro. Add conditional `builder.Services.AddGameProtocolMomoiro()` per D-07. Add `path.StartsWithSegments(MomoiroRoutePrefixes.Game, StringComparison.OrdinalIgnoreCase)` to `ShouldAssumeProtobufRequest` per D-06. Keep shared `/v01r00/chassis` fallback unchanged per D-02. Do not add generic `/v04r00` handling and do not touch AdminApi/WebUI composition.
  </action>
  <acceptance_criteria>
    Host composition is additive, exact-prefix scoped, and does not expose Momoiro when disabled without the application-part gate.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"</automated>
  </verify>
  <done>Host recognizes enabled Momoiro adapter services and exact `/v04r00/chassis` protobuf fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Add Host Project And Settings Entries</name>
  <files>Host/Host.csproj, Host/Configurations/ServerSettings.json, .planning/phases/39-momoiro-evidence-and-era-foundation/39-VALIDATION.md</files>
  <read_first>
    Host/Host.csproj
    Host/Configurations/ServerSettings.json
    Tests/Momoiro/MomoiroServerSettingsValidationTests.cs
    .planning/phases/39-momoiro-evidence-and-era-foundation/39-VALIDATION.md
  </read_first>
  <action>
    Add `Adapters.GameProtocol.Momoiro` as a Host project reference. Add Host content removal for `wwwroot\data\momoiro\data\**` and a `CreateMomoiroGameDataSymlinkForDebug` target mirroring KIMIDORI's root-level raw data handling; this is build-output hygiene only and does not load catalogs in Phase 39. Add a `Momoiro` block to `Host/Configurations/ServerSettings.json` with `Enabled` true, `AutoExtractCatalog` true, `GameDataPath` `wwwroot/data/momoiro/data`, and empty `CustomizationNameDataPath`. Do not add shop, Don Challenge, catalog sidecar, AdminApi, or WebUI settings. Update `39-VALIDATION.md` Wave 0 rows made green by this plan.
  </action>
  <acceptance_criteria>
    Momoiro is enabled in settings for the milestone foundation, can be disabled through the same settings model, and Host build output does not copy raw operator data.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroApplicationPart|FullyQualifiedName~MomoiroServerSettingsValidation"</automated>
    <automated>dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"</automated>
    <automated>rg -n "Momoiro|wwwroot/data/momoiro/data|CreateMomoiroGameDataSymlinkForDebug|Adapters.GameProtocol.Momoiro" Host/Configurations/ServerSettings.json Host/Host.csproj .planning/phases/39-momoiro-evidence-and-era-foundation/39-VALIDATION.md</automated>
  </verify>
  <done>Host project/settings entries exist, focused Wave 0 tests pass, and validation rows reflect green status for app-part/settings checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host configuration -> adapter registration | `ServerSettings:Eras` decides whether Momoiro services are registered. |
| Host configuration -> MVC application parts | Enabled-era set decides whether Momoiro controllers can be discovered. |
| no-content-type HTTP -> protobuf formatter | Host middleware normalizes direct protobuf requests for exact game prefixes. |
| raw operator data -> build output | Host project rules prevent copying raw Momoiro data into output and create debug junctions only when local data exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | Elevation of Privilege | Disabled Momoiro route leakage | mitigate | Remove the Momoiro adapter application part when disabled and prove through focused tests. |
| T-39-10 | Tampering | Direct-protobuf fallback too broad | mitigate | Add only `MomoiroRoutePrefixes.Game` (`/v04r00/chassis`) and leave shared `/v01r00/chassis` unchanged. |
| T-39-11 | Tampering | Copied cross-era runtime behavior | mitigate | Host/settings only; no controllers, handlers, EF, catalog, AdminApi, or WebUI changes. |
| T-39-12 | Denial of Service | Malformed protobuf handling | mitigate | Reuse existing protobuf-net ASP.NET Core formatter path; do not add custom request parsers. |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned. |
</threat_model>

<verification>
Run after this plan:
- `dotnet test Tests/Tests.csproj --filter "FullyQualifiedName~MomoiroApplicationPart|FullyQualifiedName~MomoiroServerSettingsValidation"`
- `dotnet build Host/Host.csproj -o "$env:TEMP\TaikoLocalServer-host-build"`
- `git status --porcelain -- proto/momoiro`
</verification>

<success_criteria>
Plan 39-03 is complete when Momoiro Host wiring, settings, exact protobuf fallback, and disabled application-part removal are implemented and the Wave 0 tests are green, while no Momoiro route controllers or runtime state behavior exist yet.
</success_criteria>

<output>
Create `.planning/phases/39-momoiro-evidence-and-era-foundation/39-03-SUMMARY.md` when done.
</output>
