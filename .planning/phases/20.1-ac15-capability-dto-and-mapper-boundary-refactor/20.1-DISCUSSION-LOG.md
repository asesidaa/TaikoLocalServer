# Phase 20.1: AC15 Capability DTO and Mapper Boundary Refactor - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves the alternatives considered.

**Date:** 2026-06-13
**Phase:** 20.1-ac15-capability-dto-and-mapper-boundary-refactor
**Areas discussed:** scope selection, BAID shape, userdata shape, implementation scope

---

## Scope Selection

| Option | Description | Selected |
|--------|-------------|----------|
| All hotspots | Capture BAID, userdata, mapper rules, and a nearby DTO audit so planning can refactor without overreaching. | yes |
| BAID + userdata | Focus only on the two confirmed dirty DTO families and defer broader DTO audit to later. | |
| Mapper rules only | Lock the Mapperly/naming/business-logic boundary first, leaving implementation scope narrower. | |

**User's choice:** All hotspots.
**Notes:** The user had already clarified that mapper attributes may rename wire fields into better application names, and that DTOs should split by behavior/capability.

---

## BAID Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Capability records | Split identity/profile/customization/shop/reward facts so mappers project owned capabilities without era-named DTO fields. | yes |
| Era DTOs | Use Blue/Green/Yellow/Red application DTOs to remove union fields, accepting more duplication. | |
| Minimal rename | Rename fields canonically but keep one broad CommonBaidResponse union. | |

**User's choice:** Capability records.
**Notes:** BAID should follow the same boundary direction as the playresult refactor: capability facts, not era-union fields.

---

## Userdata Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Capability records | Keep shared snapshot facts plus optional capability segments for tutorial, display flags, Don points, rewards, and challenge flags. | yes |
| One DTO | Use one canonical DTO with neutral names, relying on mappers to ignore unsupported wire targets. | |
| Era DTOs | Use per-era application response DTOs and map each directly to its wire response. | |

**User's choice:** Capability records.
**Notes:** The current era-named fields such as `IsDevilBlue`, `IsDevilYellow`, and `IsDevilRed` are part of the smell this phase should remove.

---

## Implementation Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Fix + audit | Refactor BAID/userdata and audit nearby DTOs, documenting clean ones like initialdata without unnecessary rewrites. | yes |
| All AC15 DTOs | Refactor every AC15 Common DTO touched by protocol mappers in one pass, with higher churn. | |
| Context only | Write the planning context now and defer implementation planning until a later command. | |

**User's choice:** Fix + audit.
**Notes:** Initialdata currently appears mostly clean, so the phase should avoid broad churn where mapper projection is already mechanical.

---

## the agent's Discretion

- Choose exact response record/type names during planning.
- Decide whether BAID and userdata are separate plans or one plan plus audit, as long as the phase keeps the scope focused.

## Deferred Ideas

None.
